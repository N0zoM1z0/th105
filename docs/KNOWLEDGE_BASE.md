# Verified knowledge base

This is the small stable layer that new sessions should load before historical
notes or source hypotheses.

## Observed

- The CharacterObjectManager runtime continuation adds **13 authored
  canonical-exact roots / 492 bytes**, moving the tracked checkpoint to
  **1,222 exact / 199,048 authored bytes / 419 units; 1,094 excluded; 1,693
  review-pending**.  The focused unit compares 15/15 physical bodies exactly:
  the 13 authored roots plus two five-byte compiler-generated virtual
  member-pointer thunks at `0x004938A0/0x004938B0`.  The thunks are deliberately
  excluded from the authored numerator even though their fresh VC8 bytes are
  exact.  A complete aggregate rebuild passes **419/419 cold units** with rc=0.
- All fifteen RTTI-owned `CharacterObjectManager<Fighter,Object>` primary
  vtables share slots 2..9 through linker folding.  The eight physical override
  bodies are release `0x0056DDA0`, slot-40 iteration `0x005D2A60`, gated update
  `0x0058BF30`, render-group iteration `0x0064B560`, slot-30 iteration
  `0x0058BF60`, render iteration `0x0053B1A0`, parent replacement
  `0x0053B180`, and checked-list access `0x004B9DA0`.  Their shared authored
  callees are object update `0x004937D0`, parent replacement `0x00493880`, and
  checked-list member-pointer iterators `0x0058BEE0/0x0053B130/0x0064B510`.
- This lane is a positive VC8 multiple-inheritance/member-pointer reference.
  `CharacterObjectManagerRuntime` has its manager base at **+0x04**, tracked
  checked `std::list<CharacterObject*>` at **+0x58**, and owner at **+0x64**.
  `CharacterObject` has the non-polymorphic handle state at **+0x330/+0x334**,
  linked callback **+0x338**, hitstop override **+0x344**, related Fighter
  **+0x16C**, parent **+0x170**, and update delay **+0x186**.  Expressing the
  callbacks as real MSVC `{code,this-adjustor}` member pointers naturally emits
  both virtual thunks and the target secondary-base adjustment; do not replace
  them with raw function pointers or hand-written dispatch.
- Keep `CharacterObjectManagerBase_update_and_prune @ 0x0058BE10` as a bounded
  non-exact result.  Ordinary checked-list C++ closes all semantics and the
  complete **207/207-byte** extent; the first residual is at **+0xA5**, solely
  in the final inlined `list::erase` EAX/ECX/EDX schedule.  It is source-present
  authored evidence, not an exact claim, and must not be forced with register,
  volatile, manual-node, assembly, or fake-liveness techniques.

- The character round-reset / BGM hash continuation adds **13 authored canonical-exact roots / 800 bytes**, moving the tracked checkpoint to **1,209 exact / 198,556 authored bytes / 418 units; 1,092 excluded; 1,709 review-pending**. Twelve roster Fighter slot-17 reset bodies contribute 764 bytes and `current_info_effect_key @ 0x0043BB90` contributes 36. The two focused units fresh-compare all configured functions zero-difference under the SHA-pinned VC8 compiler, and the complete aggregate rebuild passes **418/418 cold units** with rc=0.
- Current RTTI-owned Reimu/Marisa/Sakuya/Alice/Patchouli/Yuyuko/Suika/Udonge/Komachi/Remilia/Aya/Iku/Tenshi vtables establish slot 17 as the character-tail reset override. Their common Fighter prefix ends at **+0x7A4**; ordinary typed fields after that boundary plus exact `Fighter::reset_extended_state @ 0x004776D0` reproduce all twelve accepted code bodies. Remilia and Aya share the same 17-byte body through legitimate linker folding. Alice's four-element short/float/float/flag lane and Tenshi's paired twelve-float lanes must remain real loops; expanding or hand-writing stores is neither necessary nor stronger evidence.
- `g_bgm_source_path @ 0x006E68A8` now has a complete exact-backed VC8 String28 read contract: storage union **+0x04**, size **+0x14**, capacity **+0x18**, with initial capacity 15. `current_info_effect_key` loads the heap pointer candidate, selects the inline buffer when capacity is below 16, and calls exact `case_insensitive_crc32(bytes,size)`. Because the three aggregate addends hold different canonical dwords, relocation replay uses independently attested zero-field and capacity keys rather than pretending one literal validates every addend.
- Keep the adjacent negative/ABI evidence. Yukari slot 17 `0x0058A470` reaches the exact **125-byte** length and complete state semantics, but standalone VC8 moves an independent zero-register setup across the `gravity_138 = -6.0f` store; leave it pending instead of forcing allocation. Fighter **+0x49E is signed `short`**, as current general-hit instructions use signed short conversion before floating-point scaling; unsigned source introduces the unsigned-conversion control-word path. This repairs the shared physical ABI but does not promote the still-pending 1,579-byte general-hit resolver, whose remaining differences are TU action-block and x87 scheduling.

- The ScenarioRenderEntry/exact-TU-origin continuation adds **one authored canonical-exact root / 85 bytes** and excludes **23 compiler-generated functions / 4,510 bytes**, moving the tracked checkpoint to **1,196 exact / 197,756 authored bytes / 417 units; 1,072 excluded; 1,742 review-pending**. `ScenarioRenderEntry9C::~ScenarioRenderEntry9C @ 0x00434C60` is 85/85, its focused companion remains 242/242, and the complete accepted graph passes **417/417 cold units** with rc=0.
- The 0x9C ScenarioRenderEntry owns a title-resource handle at +0 and a real polymorphic `UiSprite94` at +4. Its explicit destructor releases the handle through manager `0x00702798` / service `0x00404D70`, then the implicit sprite/base lifetime writes IColor vptr `0x006C0624`. Natural `/GS` C++ reproduces handler `0x006B839B`, cookie `0x006F7A88`, the complete SEH frame, and all 85 bytes; the exact result is source/lifetime evidence rather than a manual vptr or EH construction.
- Exact-object orphan mining must cover non-`std` symbols as well as obvious container COMDATs. In this pass, the explicit application destructor is authored, but implicit `CSpriteEx::operator= @ 0x00450810` is compiler-synthesized and VC8 `??_H @ 0x00401220` is a compiler helper. Namespace, class name, and residence in an authored exact TU are therefore not origin proof. Use the object scan only to generate candidates; accept an exclusion only after rebuilding the pinned source/compiler profile and proving its relocation-masked fingerprint unique over all 4,010 target spans.
- The new replayable origin families are ProfileDeckResource checked-map leaves (4/951), EventEffectStateLifetime map erase (1/731), MenuConnectHistory deque/copy leaves (5/1,062), SharedSceneBootstrap tree insertion (2/694), ScenarioRenderEntry list erase (1/102), SceneRenderClusterRuntime checked-tree leaves (5/314), InputMerge deque indexing (1/96), SceneRenderClusterCleanup list/tree teardown (2/115), implicit phase-render sprite assignment (1/395), and the NetworkBase constructor helper (1/50). Keep these source/profile-specific anchor families separate rather than generalizing an STL spelling across unrelated TUs.

- The CsvReader/input/thread/ProfileEditor/Story-data continuation adds **13 authored canonical-exact roots / 1,019 bytes**, moving the tracked checkpoint to **1,176 exact / 195,224 authored bytes / 408 units; 1,049 excluded; 1,785 review-pending**. The roots are `CsvReader::next_int @ 0x0040F480` (249), synchronized-input `flush_to @ 0x0042B540` (262), five `BattleThreadHandle` lifetime/control leaves `0x004145F0/0x00414600/0x00414640/0x00414690/0x004146B0` (5/49/73/17/36), profile-block assignment `0x00409D60` (52), `CScenarioData::load_story_page @ 0x004583E0` (92), ProfileEditor read/shutdown/destructor `0x0044C0E0/0x00427C40/0x00427BC0` (42/32/15), and `ScoreData::record_story_event @ 0x0042E750` (95). All seven new units fresh-compare zero-difference, and the complete **408/408 cold replay** passes after rebuilding the whole accepted graph.
- `CsvReader::next_int` closes a reusable native checked-container layout instead of an opaque 0x30 reader: `deque<deque<string>> rows @ +0x00`, 0x0C outer-row iterator `+0x14`, 0x0C inner-field iterator `+0x20`, and parser flag bytes beginning `+0x2C`. The exact 249-byte source checks row/end then field/end, copies the current field iterator, advances the stored iterator, and passes `c_str()` to `atoi`. Do not overgeneralize this success into private entry points: `CsvReader::load @ 0x0040F150` reaches helpers `0x0040F200` with a target-private `EDI=CsvReader` receiver and `0x0040F730` with `ESI=this`; those boundaries remain unsuitable for fabricated standard C++ ABIs.
- Synchronized input now has both enqueue/publication and suffix-flush ownership evidence. `NetworkInputSink::flush_to @ 0x0042B540` proves the same input pointer `+0x104` and `CriticalSectionWrapper +0x124`; a real scoped lock makes VC8 emit the target `/GS` EH lifetime, then native checked `deque<short>` indexing copies only elements from `destination.size()` through the source end. This is a positive RAII reference: keep the lock lifetime semantic rather than hand-writing enter/leave solely to shape the frame.
- `BattleThreadHandle` is an ordinary **8-byte `{HANDLE, thread_id}` owner**. Exact `start/wait_for_exit/set_priority/is_running` plus the five-byte destructor prove `CreateThread(...,&thread_id)`, priority 15, polling `GetExitCodeThread` while the code is `STILL_ACTIVE == 259`, `CloseHandle`, and handle reset. The canonical PE IAT cells for `CreateThread`, `SetThreadPriority`, and `GetExitCodeThread` are now relocation-backed; no private thread ABI or platform facade is required.
- `ProfileEditor168` is no longer opaque between `+0x0C` and `+0x153`. Exact read/shutdown plus the **15/15 normal destructor** prove texture handles `+0/+4`, source pointer `+0x08`, real `UiTileA4` members at **+0x0C and +0xB0**, then width/x/y/state/limit dwords at `+0x154..+0x164`, preserving total size 0x168. Reverse empty destruction naturally writes the shared IColor vptr to the two tile bases. Keep constructor visibility separate: target ctor `0x00428000` is 33 bytes while the shared header's natural constructor is 28, so do not globally alter `UiTileA4` just to force that tiny body. `update_selection @ 0x00427C60` is semantically closed at **330/333** with only reset-condition/x-y scheduling residuals, and render `0x00427DB0` remains a broader x87/TU-lifetime stop.
- Story-progress storage now has a safer physical overlay. `ScoreData::record_story_event @ 0x0042E750` proves `flags +0x04`, fifteen event-column bytes `+0x08`, fifteen primary-availability bytes `+0x1C`, fifteen secondary-availability bytes `+0x30`, and checked `vector<int>` index lists `+0x5C/+0x6C`. In particular **+0x3D/+0x3E are secondary availability cells 13/14**, not independent dirty flags: the exact event wrapper sets both before refresh/persist. The recovered refresh semantics are retained in source, but `refresh_story_progress @ 0x0042E3F0` stays pending at **876/860 with a 245-byte exact prefix**; target clears the secondary vector before the primary and omits standalone VC8's two always-true checked-iterator owner self-validations. Rewriting the range as `begin + size()` worsens to 924 bytes, so do not manufacture iterator aliases to remove those checks.
- Two compact exact roots close useful cross-family contracts. `PlayerProfileBlockOwner::assign_profile_block @ 0x00409D60` copies a real 0x34 block at +4, treats byte 0 as a signed source id, and on an out-of-range id zeroes the whole block then publishes sentinel `-2`. `CScenarioData::load_story_page @ 0x004583E0` reuses the exact 0x524 scenario layout: clear text map `+0x32C`, reset runtime state, hide/reset result resource `+0x390`, then load the script at `+0x35C` with option zero.
- Preserve the bounded stops from this search instead of turning them into code-generation games. Besides ProfileEditor ctor/update/render and Score refresh, `ProfileRenderList::clear @ 0x0040BB40` has closed `vector<unsigned> +0x04` / `list<polymorphic*> +0x14` semantics but natural `erase(begin,end)` is 245/310 and `clear()` is 233/310 because the shipped TU retains a heavier checked erase/aligned frame. `TitleDesignResource::bind_object @ 0x0040CC50` is 122/121 with a target-only aligned EBP frame. `SpellDataOwner::finalize_loaded_spell_data @ 0x00432200` retained source is 226/260, and `CsvReader::load` reaches private optimized helpers. None is permission for fake aligned locals, manual iterators, register/volatile forcing, invented ABIs, assembly, or copied bytes.

- The ProfileCharacterSelect / story-transition continuation adds **4 authored canonical-exact roots / 1,163 bytes**, moving the tracked checkpoint to **1,163 exact / 194,205 authored bytes / 401 units; 1,049 excluded; 1,798 review-pending**. A complete sequential cold replay reaches all **401/401 units** with zero compare failures; all configured objects/provenance are rebuilt, and the final Story-ending unit is independently rechecked 162/162 exact. The new roots are `CProfileCharacterSelect::update @ 0x004485B0` (372), constructor `0x004487E0` (571), `ScenarioTransitionView::consume_transition_code @ 0x0045A360` (58), and `BattlePhaseBlock::load_story_ending_page @ 0x0045A3A0` (162). All four focused units rebuild from deleted objects and compare zero-difference under pinned VC8; the existing 602-byte `BattleRoundTransition` caller also remains exact after correcting the shared transition return ABI.
- `CProfileCharacterSelect` closes a reusable **0x288 physical owner** across exact constructor/update plus the already-owned destructor. Constructor matching depends on expressing its +0x13C design member as a genuine polymorphic source type with four virtual slots before `load`: the target loads vslot +0x10 before publishing the player member, while a semantically equivalent manual/shared facade schedules the player store first. This is source type visibility, not permission to hand-load a vtable. The update reuses the same player/character/design/cursor/GuideOverlay layout and truthfully allocates the existing 0x6B0 `CProfileDeckEdit` type on confirmation.
- Two story-transition ABI/source rules are independently exact-backed. `consume_transition_code` owns a native checked `std::list<int> @ +0x5A4`, returns `front()` as a **full `int` in EAX**, then erases `begin()`; the exact caller stores only AL, but declaring the callee as `signed char` adds an EBX lifetime and grows 58 bytes to 60. `load_story_ending_page` must keep the ScoreData record call as one expression whose second argument is `selected_column_590` and whose first argument is `resolve_scenario_event_name(String28(character_name, 0, ~0u))`: VC8 right-to-left argument evaluation pushes the column before opening the 0x1C by-value string argument area, exactly matching the shipped stack lifetime. Naming the resolver result first preserves semantics but changes the prologue schedule.
- Preserve the bounded Profile/UI probes from this pass. `CProfileCharacterSelect::refresh_character_preview @ 0x004484F0` is **178/178** but standalone VC8 always loads/tests the character before clearing the released texture handle; literal-zero, named-character and real-class receiver forms all canonicalize to the same order. `CMenuConfig` ctor `0x00441520` reaches an exact **394-byte prefix** after recovering a genuine polymorphic design resource, explicit cursor branches/order, and one real work dword reused by keys/texture handle; the first remaining residual is checked-tree receiver ownership. `CProfileDeckEdit` selected-card refresh `0x00448B40` reaches **440/439 with a 95-byte exact prefix** and then retains an optional-resource-handle CSE across a virtual sprite call; pointer-vs-dword typing and a real height local do not change it. `MenuCursorState::update @ 0x0041FA20` lowers `% 6` to a magic multiply while target retains signed `idiv`; `BgmService::fade @ 0x00403940` forwards into target-private `EAX=stream/EDI=duration` code at `0x00402B70`. Do not close any of these with register/volatile forcing, fake dependencies, handwritten vtables, or invented standard ABIs.

- The replay/profile/network-constructor continuation adds **13 authored canonical-exact roots / 1,630 bytes**, moving the tracked checkpoint to **1,159 exact / 193,042 authored bytes / 397 units; 1,049 excluded; 1,802 review-pending**. A complete cold replay passes **397/397 units** with rc=0. New roots are `mt19937_seed_u32 @ 0x00406670` (61), the complete `ConnectConfigIni` file/read family `0x0040F0B0/0x0040F100/0x0040F120` (77/22/35), `BattleInputGate::queue_control_word @ 0x0042B270` (125), replay save `0x00428D90` (385), `ProfileMenuBaseData` ctor/dtor `0x0042D260/0x0042D320` (192/205), Score BGM-path publication `0x0042DEB0` (86), CNetworkServer/CNetworkClient ctors `0x0043CB30/0x0043CCA0` (108/108), and their factories `0x0043CE00/0x0043CE80` (113/113). All accepted bodies are ordinary pinned-VC8 C++ with strict relocation replay; no inline/native assembly, copied bytes, register/volatile forcing, fake liveness, padding-for-codegen, manual vptr writes, or fabricated private ABI is used.
- Three source-lifetime rules from this wave are broadly reusable. `mt19937_seed_u32` must keep a real **signed `int` loop counter**: VC8 strength-reduces the indexed recurrence to the target pointer induction while retaining the signed `jl`; writing the source as a pointer loop changes only the latch to unsigned `jb`. `BattleInputGate::queue_control_word` independently proves a **32-bit incoming argument slot** whose low 16 bits are normalized in place before a checked `deque<short>::push_back`; exact callers may retain a narrower call-site facade when changing the shared declaration would add non-target narrowing code. Replay save needs two real byte locals (`month`, `slot_count`) plus direct bool arithmetic `2 * (input_b != 0) + (input_a != 0)` so VC8 naturally reproduces the target DL/AL/CL publication and two `setne` instructions.
- `ProfileMenuBaseData` now has a complete exact-backed **0x33C lifetime**. It owns the texture cell at +0, a real polymorphic 0x94 sprite at +0x04, VC8 strings at +0x98/+0xB4, `CInputManagerEx +0xD0`, owner pointer +0x138, and twenty native checked `deque<unsigned short>` values at +0x1AC. In construction the input-owner pointer is published only **after** the EH-vector construction of all twenty deques; `CInputManagerEx` clears its +0x64 bool before the +0x60 injected-mask word. Reverse destruction is deque array -> input manager -> strings -> sprite/IColor lifetime. These are member/TU visibility facts, not permission to hand-code vtables or vector helpers.
- The network factory/constructor quartet closes the positive half of the current multiple-inheritance model. Exact `CNetworkBase @ 0x0044DB10` occupies 0x6DC; a real 8-byte `INetworkUDPEventProc` secondary base begins at +0x6DC. Ordinary C++ therefore makes CNetworkClient extent **0x6E4** and CNetworkServer extent **0x6EC** (the Server owns an additional eight-byte tail), publishes primary/secondary RTTI vfptrs naturally, and passes the secondary-base pointer to the +0x3B4 cleanup binder. Plain `new` then reproduces both factory allocation sizes, constructor-failure EH cleanup, and `g_network_session` publication exactly.
- Compact service roots add useful control-flow and ownership evidence. `ConnectConfigIni` owns a real 260-byte path buffer: `has_file` uses `GetCurrentDirectoryA`, two safe concatenations and a shared false tail; adjacent `read_uint @ 0x0040F100` and `read_string @ 0x0040F120` prove that the same receiver buffer is the INI filename passed to `GetPrivateProfileIntA` / `GetPrivateProfileStringA`, with `-1` and the canonical empty string as defaults. `ScoreMusicLookup::publish_bgm_path` is the authored wrapper around already-exact case-insensitive CRC and the already-origin-excluded generated `set<unsigned>::insert` body; generated container machinery remains outside the authored numerator even when an exact authored wrapper calls it.
- Preserve the negative evidence from the same search rather than restarting coercion experiments. Replay header read `0x00429A30` proves a real local `std::string` plus concrete 0x0C `CFileReader` but truthful standalone VC8 still reaches only about 293/313 because of string-construction/frame visibility. SceneRenderCluster dtor `0x0041F1F0` matches through the early string/map/list teardown then diverges on native-STL destructor scheduling; Design-resource render `0x0040BC80/0x0040BE20` retains target-only aligned EBP frames; `CEffectSprite` ctor `0x00421FF0` is 79/79 with only derived-vfptr/EH-state publication order swapped; `bind_object_alt @ 0x00420B90` bottoms out around 120/118 on owner/head/node lifetime allocation; quantized trig `0x00406360/0x004063D0` and RenderModeManager line/rect `0x00401180/0x004012A0` expose distinct stack/x87 TU profiles. Network UDP send/acquire/release additionally reaches target-private internal receiver ABIs (`0x00417660` / `0x00405200`). Shared animation frame application `0x00436760` is now bounded too: the truthful common 0x1C frame prefix plus real scalar texture-rectangle locals reaches **335/335** with an exact first 120 bytes, but mode-2 frame/payload alias lifetime still allocates the persistent frame receiver differently from the shipped TU; branch-local pointer caching shortens the body and perturbs the already-exact prefix. Do not fabricate standard C++ signatures, force registers, or add fake liveness to close these islands.

- The profile-text/default-profile/event-record continuation adds **4 authored canonical-exact functions / 878 bytes**. `insert_profile_input_character @ 0x0040EA30` and `delete_profile_input_character @ 0x0040EC60` close 300/300 and 293/293 inside the existing physical 0xA14 ProfileMenuController TU. Both use a real 1024-byte suffix buffer, bounded CRT copy calls and the Shift-JIS lead ranges `0x81..0x9F` / `0xE0..0xFE`; changing the controller unit to truthful `/GS` leaves all eight older exact members unchanged. The decisive source shape is a nested unsigned-byte classifier (`<0x81` non-lead, `<0xA0` lead, `<0xE0` non-lead, `<0xFF` lead) plus direct `1024-cursor` / `1023-cursor` expressions. Naming and reusing an available-length temporary moves the target `strlen`/cursor schedule and breaks equality; this is ordinary source dataflow, not register control.
- `create_default_profile_for_list @ 0x0043C1A0` is 213/213 from one real 0x33C automatic ProfileMenuBaseData owner. Ordinary construction, two `if (!load(fixed_path)) { initialize_defaults(); save(fixed_path); }` branches, automatic destruction and `/GS` generate the complete target EH/frame topology, including handler `0x006B8BFB`. `lookup_event_effect_record @ 0x0043C150` is 72/72 from checked `std::map<char,int>::find` on loader-zero object `0x006FBF14`; passing the address of the original one-byte argument slot is byte-significant. Existing exact caller probes retain their wider call-site declaration because changing it would add narrowing code; executable REL32 identity proves the callee while the target callee independently proves the one-byte lookup key.
- Four adjacent controller roots remain bounded stops. Candidate-window `0x0040E780` has exact 111-byte call topology but a standalone 0x30 frame versus target 0x34. Composition commit `0x0040E8B0` has a 295-byte exact prefix and complete semantics, but standalone VC8 emits 375/383 by common-tail-merging the final DBCS terminator. Cursor movement `0x0040EBC0` receives its private delta in EAX, and dispatcher `0x0040EDE0` uses private `ECX=lParam`, `EDX=wParam`, `ESI=hWnd`, stack `Msg`. Preserve these as TU/LTCG/private-entry evidence; do not use gotos, fake liveness or invented standard ABIs.
- The network ACK/Server-secondary continuation adds **3 authored canonical-exact functions / 1,142 bytes**, moving the checkpoint to **1,091 exact / 179,691 authored bytes / 357 units; 1,049 excluded; 1,870 review-pending**. Current primary vtables own `CNetworkClient_send_ack_info_if_ready @ 0x0044F0F0` (269) and `CNetworkServer_send_ack_info_if_ready @ 0x0044FE30` (341); the Server secondary vtable owns `CNetworkServerSecondary_dispatch_packet @ 0x0044FB40` (532).
- The two ACK_INFO senders prove a reusable local-packet ABI rule. The real 0x40 ACK_INFO packet has a non-polymorphic four-byte result base, a derived vfptr, two checked `deque<short>` members, peer bytes and tail state; its inline construction/destruction naturally gives the shared folded EH handler `0x006BA668`. Calling `serialize()` through the concrete local lets VC8 devirtualize and makes both methods three bytes short. A truthful slot-0 serialize-only polymorphic view keeps the target vfptr load/indirect call and closes **269/269 + 341/341** without manual vtable loads. The Client copies only deque/peer slot 1 after gate `+0x3A5 & +0x3A6`; the Server publishes side scalar state, side0 deque/peer and shared tail fields after gate `+0x3A6`.
- `CNetworkServerSecondary_dispatch_packet @ 0x0044FB40` mirrors the exact Client secondary ownership rule: preserve the eight-byte secondary `this` and repeatedly form the primary owner at `this-0x6DC`. Context-positive type 11 forwards a 0x10 replay request to the primary owner; context zero dispatches packet cases in physical source order `1,2,3,8,4,5,7`. INPUT/ACK_INFO native checked-deque lifetimes generate the target two EH states. The reviewed callable is 532 bytes; a 32-byte switch table follows the RET in the same COMDAT and is not authored-function progress.
- `CNetworkClient` owner replay dispatcher `0x0044E460` remains pending despite a natural **566/566** body, correct ACK_INFO/REPLAY lifetimes and matching EH/frame topology. Exhaustive target-backed scalar declaration/statement-order variants cannot reproduce the shipped `byte210 -> EAX, value254 -> EDX, byte211 -> CL, sequence -> EAX` register schedule. Treat it as allocator/TU residual; do not close it with volatile/register forcing, fake aliases, assembly or copied bytes.

- The network primary/secondary state continuation adds **4 authored canonical-exact functions / 1,075 bytes**, moving the checkpoint to **1,088 exact / 178,549 authored bytes / 355 units; 1,049 excluded; 1,873 review-pending**. Current primary vtables own `CNetworkClient_sync_peer_state @ 0x0044E8F0` (166), `CNetworkClient_publish_input_value @ 0x0044ED10` (152), and `CNetworkServer_sync_peer_state @ 0x0044F730` (175); the Client secondary vtable owns `CNetworkClientSecondary_dispatch_packet @ 0x0044EE80` (582). All four are fresh pinned-VC8 C++ and canonical zero-difference.
- Reuse the packet-call boundary proved by the earlier Server sender: a member packet's virtual serializer takes **only the output buffer**; `0x50`/option/result-pointer arguments belong to the outer `send_client`/`send_server` member. Giving those arguments to the serializer makes VC8 load the virtual slot too early. The peer-state pair closes when the final failure gate is source-visible (`!available` on Client, `available < adaptive_delay` on Server), and Client input publication is a **void** primary slot whose final critical-section leave is a tail call.
- `CNetworkClientSecondary_dispatch_packet @ 0x0044EE80` is a useful multiple-inheritance source reference. Do **not** bind `primary = this - 0x6DC` as a long-lived local: the shipped body preserves ESI as the 8-byte secondary interface and repeatedly forms the primary owner through negative offsets. That one ownership choice changes the first draft from a 564-byte callable into the target **582/582** body. Its local packet types are SCENE_ACK 0x0C, INPUT 0x24, ACK_INFO 0x40, TERMINATE 0x08 and REPLAY 0x28; native checked `deque<short>` lifetimes naturally generate three EH states. VC8 also emits a 40-byte switch table after the reviewed RET in the same COMDAT; it is not authored-function byte progress.
- Two neighboring Client roots remain explicit non-acceptance evidence. `0x0044EAD0` naturally reaches **573/573** and a 459-byte exact prefix; the first residual is only the commutative load order of `behind_count_088 + ahead_count_084`, and ordinary temporary/two-step accumulation spellings canonicalize to the same fresh order. `0x0044F200` becomes **399/399** only after restoring a real four-byte scoped critical-section guard: target FuncInfo has one unwind state whose cleanup leaves the lock. The remaining mismatch is a register permutation while publishing the 0x40 snapshot record. Keep both pending rather than forcing aliases, volatile/register state, assembly or fake liveness.

- The scene factory/update continuation closes **7 authored functions / 1,621 bytes**. `create_scene_for_id_41e320 @ 0x0041E320` is 980/980 from a real typed-new switch, not manual operator-new/placement construction. Its source case order is **0,1,6,2,3,16,5,20,8,10,13,9,11,14,12,15**; this exact order is independently encoded by the target new-expression EH states 0..15. The 21-entry jump table starts at target `0x0041E6F4`, one byte after the 980-byte callable boundary, and the fresh COFF section is exactly 0x428 bytes = 0x3D4 callable + 0x54 table.
- Scene allocation extent is now reusable class evidence: CLogo 0x198, Opening 0xA0, CLoading/SV/CL 0xBC, CTitle 0x28C, CSelect 0x2ED8, CSelectSV/CL 0x2EDC, CSelectScenario 0x3EC, CBattle/Watch 0x08, CBattleSV/CL 0x0C, Ending 0x98 and CLoadingWatch 0xA4. Four ordinary polymorphic derived constructors at `0x0041E160/0x0041E1C0/0x0041E180/0x0041E1E0` are 18/18: call CSelect/CLoading base then publish RTTI-named vtables `0x006C04AC/0x006C0504/0x006C04CC/0x006C0524`. CSelect derived classes add one dword; CLoading derived classes add no physical tail.
- `Ending::update @ 0x0041E7C0` is 352/352 with phase/cluster/fade bytes +0x94..+0x97 and the current battle-phase ending owner. Keep the direct source expression `g_scene_mode == 22 ? 20 : 2`: fresh VC8 lowers it to target `sub eax,22; neg; sbb`; writing a named `scene_mode-22` delta lowers to `add eax,-22` instead. Old `.analysis` objects can therefore be stale even when their source file now looks right; always fresh-rebuild before acceptance. `CLoadingWatch::render @ 0x004269D0` is 217/217 with phase +0x9C, sprite +0x08, exact cosine helper and current `__ftol2_sse @ 0x0068A1A0`.
- `CBattleSV/CBattleCL` update recovery adds a useful negative rule. Vtables `0x006C04E8/0x006C0540` own update slots `0x00426180/0x00426380`; their session/peer/renderer control flow is semantically closed. A sparse `switch (scene_phase())` is required to reproduce target `sub eax,1` rather than `cmp eax,1`, but truthful standalone VC8 currently reaches 252/247 and 259/267 because identical return epilogues are merged differently. Preserve this as a linker/backend tail-merging stop; do not introduce goto-only layout coercion, volatile/register state or fake calls.

- The scene-constructor continuation closes **9 authored canonical-exact functions / 1,062 bytes** in two ordinary `/GS` TUs. `SceneRenderCluster_ctor @ 0x0041F120` is 196/196 with a real `CScript @ +0x18`, checked `map<int,void*> @ +0x4C`, `list<0x7C record> @ +0x58`, checked `map<int,20-byte value> @ +0x64`, and `std::string @ +0x70`. The same physical ownership island closes Ending/CLoading/CLogo/Opening/CLoadingWatch ctors (106/177/168/130/165) and the CLoading/CLogo/Opening update slots (23/45/52). Their already-exact destructors and current RTTI/vtables independently own every class/address before byte comparison.
- **Tail padding is application storage when current ownership proves it.** The cluster constructor's ordinary C++ type rounds to 0x94 bytes even though its meaningful fields end after seven tail bytes at offset 0x92; Ending embeds only the meaningful **0x93-byte** storage at +0x04 and publishes its next byte at +0x97, which is the constructor type's nominal tail-padding byte. This is a physical-layout fact, not permission to add padding for codegen. Opening starts the same 0x93 storage at +0x08 but retains an independent +0x9B byte before its +0x9C active flag. Keep constructor views and owner-storage views distinct when target member boundaries require it.
- Destructor visibility can be the missing constructor byte sequence. Opening performs throwing cluster `initialize`/`load_script` calls after the cluster ctor. With only a ctor declaration, standalone VC8 emits a 125-byte body and no post-construction cleanup state; adding the target-proved `~SceneRenderClusterOwner93()` declaration naturally inserts `mov byte ptr [EH-state],1` and yields the shipped **130/130** body. Ending has no throwing work after cluster construction, so the same destructor declaration does not bloat its 106-byte ctor. Prefer truthful special-member visibility/TU boundaries over `noinline` or fake EH locals.
- Small boolean source spelling remains observable. Opening update returns 2 when combined-menu input word +8 equals 1 and 1 otherwise. `2 - (value != 1)` lets VC8 choose `setne/sub` and grows the method; the equivalent ordinary condition `(value - 1) ? 1 : 2` naturally emits the shipped `sub eax,1; neg eax; sbb eax,eax; add eax,2` sequence and closes 52/52. CLoading update likewise uses the real 16-bit timer at +0xB8 and `(request != 0) + 5`; CLogo update preserves the otherwise-unused combined-input getter call before its `state_194 >= 180` result.
- The exact cluster TU is also generated-origin evidence. SHA-pinned full-inventory replay classifies `list<SceneListRecord124>::_Buynode @ 0x0041EB30` (29) and `map<int,SceneMapValue20>::_Buynode @ 0x0041EB90` (55) compiler-generated with 86.2%/92.7% non-relocation coverage and unique current hits. Its `map<int,void*>::_Buynode @ 0x00420A40` fingerprint is also unique but was already independently excluded by the exact effect-manager lifetime manifest, so the new rule deliberately does not overlap or double-count it.

- The scene/network lifetime wave closes **18 authored canonical-exact functions / 1,019 bytes**. Current RTTI plus exact render/update consumers establish five scene layouts without manual vtables: Ending owns a 0x93 render cluster at +0x04; Opening owns the same cluster at +0x08; CLoading is `{title handle +0x04, UiSprite94 +0x08, 8-byte gap, checked deque<unsigned char> +0xA4}`; CLogo is `{title handle +0x04, CSpriteEx +0x08, CSpriteEx +0xF0}`; CLoadingWatch is `{title handle +0x04, UiSprite94 +0x08, timer/state tail}`. Ordinary virtual destructors naturally publish the class vfptr, release owned resources, run reverse member destruction and restore `IScene`; all five normal/scalar pairs plus IScene normal/scalar are zero-difference.
- CNetworkServer/CNetworkClient lifetime provides a reusable multiple-inheritance reference. Exact network-session users independently place fields through +0x6D8, while current RTTI CompleteObjectLocators put each derived secondary polymorphic base at **+0x6DC**. A normal C++ primary base of size 0x6DC plus a four-byte secondary interface makes VC8 naturally publish both derived vfptrs; Server passes null to cleanup member +0x3B4, Client passes the secondary-base pointer, then both call the pending-but-identity-proven CNetworkBase dtor. Primary/secondary target vtables are Server 0x006C16EC/0x006C16CC and Client 0x006C1754/0x006C1734. CInputManager likewise demonstrates that an empty virtual base destructor is observable as a 7-byte vfptr publication and a 31-byte scalar wrapper.

- The CNetworkBase recovery closes the previously identity-only base lifetime. The full current object is **0x6DC** and ordinary reverse member destruction is canonical 259/259: CriticalSectionWrapper at +0x48/+0xDC/+0x25C; checked deque<short> at +0xA0 and `[2]` at +0xB4; two 0x7C endpoint records at +0xF8; two 0x20 queue records at +0x214; UiSprite94[2] at +0x27C; the independently observed 0x314 polymorphic winsock/session member at +0x3B4; and a final deque<short> at +0x6C8. **The 0x7C endpoint uses CInputManager composition, not inheritance.** Inheritance emits a derived-vptr publication and a 25-byte endpoint dtor; composition naturally gives target 19 bytes (`deque::_Tidy` then exact CInputManager dtor). The 0x20 record similarly emits the target 8-byte deque tail. These implicit helper bodies are layout witnesses, not authored numerator.
- CNetworkBase vslot 1 `reset_runtime @ 0x0043CA60` is 175/175 when each 0x7C endpoint is held through one short-lived element pointer across its `queue.clear()` and `input.reset_counters()` calls. Recomputing both member addresses from `this` gives 177 bytes; the pointer is therefore source ownership/dataflow evidence rather than register forcing. CNetworkServer primary vtable 0x006C16EC independently owns state import/export at slots 2/3 and ACK-record consumption at slot 8. The exchanged state object is a real **0x4C** record with native deque<short> at +0x10/+0x30; import is 162/162 and export 284/284. Export source must be `if (pending) { ...; return true; } return false;`: the positive path is target fallthrough, while spelling the negative early return changes the branch/tail layout. The ACK record is 0x24 with checked deque<short> at +0x10, making `empty()/at(0)` naturally reproduce the 99-byte vslot body.
- The network primary/secondary protocol wave adds **15 authored canonical-exact functions / 1,031 bytes**, moving the checkpoint to **1,084 exact / 177,474 authored bytes / 353 units; 1,049 excluded; 1,877 review-pending**. Six leaf wire methods close packet type 1/2/5/7/8 serialization and the type-1 payload parser (`0x0044D7A0/0x0044D7C0/0x0044E720/0x0044E7C0/0x0044E850/0x0044E870`, 106 bytes total). Client primary-vtable roots add byte-valued send readiness `0x0044E730` (30), send-if-needed `0x0044E750` (100), CDPP_DELAY send `0x0044E9E0` (79), and the exact-ShortDeque8 delay advance `0x0044EA80` (79). Client/Server secondary slot 4 routes `0x0044E880/0x0044F600` add 93/289; Server primary adds generic packet send `0x0044F4E0` (53), send-state flush `0x0044F520` (37), and state-or-CDPP_TERMINATE send `0x0044F830` (165). All four durable TUs fresh-compare exact with ordinary pinned VC8 C++.
- Reuse the protocol ABI/source rules rather than rediscovering them. `CNetworkClient::send_state_ready @ 0x0044E730` returns **unsigned char**, not C++ `bool`; `bool` makes VC8 materialize through a temporary register instead of target `setne al`. In `send_if_needed`, +0x3A7 is captured as a raw byte across unlock; spelling it as `pending != 0` adds `cmp/setne`. `CDPP_DELAY` is a 12-byte packet and `CDPP_TERMINATE` an 8-byte packet, both using the already-proved non-polymorphic-data-base-before-derived-vfptr construction. Keep a source-visible packet pointer when invoking the serializer: calling the concrete local directly lets VC8 devirtualize and shortens the body by one byte.
- Network send argument evaluation is a caller-backed narrow ABI fact, not manual stack choreography. `CNetworkServer::send_packet @ 0x0044F4E0` is exactly `send_server(channel, buffer, packet->serialize(buffer), option, &packet->result)`: right-to-left evaluation pre-pushes the outer `result` and `option`, executes the **one-buffer** virtual serializer, then pushes size/buffer/channel for the five-argument send member. The same pattern closes `CNetworkServer::send_state_or_terminate @ 0x0044F830`; do not incorrectly give the inner serializer the outer send arguments. Secondary slot-4 routing continues to use the exact factory/RTTI owner relation `this - 0x6DC`. Server routing requires an **unsigned** direction (`direction > 0` gives target `jbe`) and a signed one-byte name length whose source first checks `(unsigned char)len < 0x20` and then `len > 0`, naturally yielding `jae`, `jle`, and `movsx`. `CNetworkClient::advance_delay_queue @ 0x0044EA80` reuses the exact `ShortDeque8` front/advance logic at owner +0x68 instead of hand-modeling a second queue type.
- The CNetworkClient/CNetworkServer continuation wave adds **10 authored canonical-exact functions / 1,027 bytes**, moving the checkpoint to **1,069 exact / 176,443 authored bytes / 349 units; 1,049 excluded; 1,892 review-pending**. CNetworkServer contributes `reset_runtime @ 0x0043CBD0` (57), `publish_snapshot @ 0x0044F390` (324), `send_request_info @ 0x0044F7E0` (65), `maintain_endpoints @ 0x0044F8E0` (78), and `import_aux_state @ 0x0044FF90` (69). CNetworkClient contributes secondary-interface `configure/set_option_bits/handle_request @ 0x0044E7E0/0x0044E840/0x0044EA30` (91/13/68), request-info send `0x0044E9A0` (63), and replay-sequence consume `0x0044EDB0` (199). The exact secondary callbacks prove the secondary interface is **8 bytes** (vfptr + state dword) at owner +0x6DC, so the complete derived object is 0x6E4; updating the shared lifetime view from an empty four-byte interface to this real extent keeps all four previously exact derived lifetime bodies exact.
- Two packet/lifetime rules from the same network wave are reusable. `CDPP_REQ_INFO` is an 8-byte polymorphic packet whose payload dword lives in a **non-polymorphic four-byte base**; normal C++ therefore clears +4 before publishing the derived vfptr, exactly matching both Server and Client serializers. `CDPP_REQ_REPLAY` uses the same base-before-vfptr construction for its 16-byte temporary in the secondary request handler. Client replay state uses a 0x24 record with checked `deque<short>` at +0x10; for each accepted sequence step the source consumes two entries in reverse index order and appends the first to client +0x1DC, then the second to +0x160, under the nested +0xDC critical section. Server snapshot publication must retain `if (pending) { ...; return true; } return false;` so the pending-positive path is fallthrough and the false epilogue remains at the target tail.
- Network ACK packet emission `0x0044FE30` is a bounded truthful stop. A polymorphic 0x3C packet with two native deque<short> members, the target field-copy order, virtual three-argument serializer, and the +0x3B4 winsock send member reproduces the packet/EH topology but emits 316 versus target 341. Target has an additional function-level `/GS` cookie and eight extra frame bytes, so the unresolved fact is a buffer-shaped packet/member type visible to VC8. Do not invent a dummy character array, force a cookie, or hand-code the prologue; recover the real packet type.
- Two lifetime probes remain explicit stop conditions. AttackObject source truthfully frees state +0x194 and defines the known AnimationObject cleanup, but standalone VC8 emits a separate/tail-called base destructor while target inlines that base teardown into the 78-byte AttackObject body; do not use forceinline or duplicate manual vptr cleanup to coerce it. A truthful native checked-container model for the older generic `CHandleManager<T>` layout (`deque + vectors + list + CriticalSectionWrapper`) emits a 137-byte destructor versus target 98 because that template/TU used a different container/debug-lifetime configuration; scalar 30-byte wrappers alone are not sufficient promotion evidence.

- Exact application TUs can serve directly as strong compiler-origin witnesses. `EventEffectBackgroundRuntime.cpp` is canonical 325/325 and its native checked `deque<BackgroundRenderEntry>` instantiations regenerate three inventory-unique VC8 COMDATs: `_Copy_backward_opt @ 0x0042FE10` (202), `_Copy_opt @ 0x0042FEE0` (199), and `erase @ 0x004304F0` (472). `EventEffectStateRuntime.cpp` is independently canonical exact and regenerates map find `0x004698E0` (114), tree `_Erase @ 0x004371C0` (80), deque `_Growmap @ 0x00449A80` (343), and deque `push_back @ 0x00449D70` (121). Two SHA-pinned manifests replay all seven relocation-masked bodies against the complete 4,010-candidate inventory; all seven hit only their declared address. This removes **1,531 bytes** from origin review without treating semantic IDA shape alone as generated proof.
- `CsvReader` has a real non-throwing cleanup lifetime: `~CsvReader() throw() { clear_rows(); }`. Current `PlayerSlotCharacterDeckLoad @ 0x00431E80` proves the distinction. The normal path directly calls `clear_rows @ 0x0040FDB0`, while its fourth EH unwind funclet calls destructor thunk `0x0042E980`, whose complete body is a five-byte jump to the same cleanup routine. Adding this truthful inline destructor leaves exact Story script runtime `0x0045A2E0/0x0045A4E0` unchanged. Do not model local CsvReader objects as trivial merely because many caller bodies inline their normal cleanup.
- Deck loader `0x00431E80` is now a sharply bounded LTCG/stack-coloring stop. Current target semantics require three string lifetimes—character name, `"data/csv/" + name` temporary, and final `+ "/deck.csv"` path—then a physical 0x30 CsvReader, deque clear, twenty `next_int`/`push_back<unsigned short>` iterations, and CsvReader cleanup. A natural `std::string` + explicit 0x30 reader model reaches **472/463**, with the target 0x8C frame, output in EBP, four unwind funclets, and correct cleanup identities. Its remaining difference is stack coloring: standalone VC8 places the three string objects in the opposite lifetime-slot order from the shipped target. Preserve this evidence instead of adding fake locals/scopes or register forcing.

- The profile-input / EventEffect background continuation closes **9 authored canonical-exact functions / 749 bytes** while replayably excluding one generated 132-byte value-copy COMDAT. The profile controller exact set is `0x0040E720/0x0040E7F0/0x0040E840/0x0040EB60/0x0040EB70/0x0040EB90/0x0040ED90/0x0040EDC0` (91/77/112/11/26/39/46/22). Current ProfileMenu callers already own `set_mode` and `select_profile`; the callee TU independently fixes a real **0xA14** owner: HIMC +0, state +4, primary text +0x08, work text +0x408, IME composition +0x808, attributes +0x908, maximum length +0xA08, reset/primary/secondary/mode bytes +0xA0C..+0xA0F, and cursor +0xA10. Keep the existing narrow caller facade when that is all a TU proves; physical-owner and call-site views need not be globally conflated.
- Import visibility is byte-significant in the controller TU. Current target `0x0040E840` calls `ImmGetCompositionStringA` by direct REL32 to the executable import thunk at **0x0066B830**, while candidate-window target `0x0040E780` similarly calls `ImmGetContext @ 0x0066B824` and `ImmSetCandidateWindow @ 0x0066B81E`; by contrast `GetWindowRect` remains an indirect IAT call. Declaring the IMM32 routines `__declspec(dllimport)` adds indirect calls and changes persistent-register lifetime. Ordinary `extern "C" __stdcall` declarations recover the shipped linkage naturally; `refresh_profile_ime_composition` then becomes 112/112.
- `EventEffectStateView::update_background_entries_469b70 @ 0x00469B70` is **325/325** from native checked `std::deque<BackgroundRenderEntry>`. The source must take `begin()`, increment the iterator once, then visit only older layers: nonzero alpha decrements by 15 and advances, zero alpha uses `entry = deque.erase(entry)` with the real checked hidden-return iterator. After the loop, emitter subobject +0x48 invokes vslot +0x14. This independently explains why the first background entry has special rendering ownership elsewhere; do not replace the checked deque with raw map/node arithmetic.
- Exact EventEffect storage also supplies positive generated-origin evidence. The application uses `map<signed char, vector<unsigned char>>`; a tracked SHA-pinned VC8 probe regenerates the 132-byte `vector<unsigned char>` copy constructor, whose relocation-masked fingerprint is inventory-unique at **0x00469CC0** across all 4,010 spans. Adjacent `0x00469E10/0x00469FA0` look tree-generated semantically, but standalone `_Buynode/_Insert` bodies are 156/492 versus target 166/509 and therefore remain review rather than being overclassified.
- Preserve the remaining IME/background stop conditions. Candidate-window `0x0040E780` has the correct 111-byte body shape once IMM32 linkage is fixed but target keeps four extra stack-frame bytes (0x34 versus fresh 0x30). Composition commit `0x0040E8B0` remains a target-retained DBCS-tail/TU merge stop, and cursor/dispatcher roots `0x0040EBC0/0x0040EDE0` have private entry registers rather than standard source ABIs. Background `set_transition @ 0x004654F0` reaches the reviewed 407-byte callable length with only two dead stack-color slots swapped; `step_transition @ 0x004656A0` truthful sparse-switch forms remain 831/825 on one entry zero-register; `initialize_common @ 0x004664A0` and manager update `0x00466F40` remain frame/register/TU lifetime blockers.

- The scenario-flow/setup-helper wave closes **7 authored canonical-exact functions / 1,011 bytes**. `CSelectScenario::update @ 0x00424690` is 247/247 and `on_scene_enter @ 0x00424790` is 271/271 from the current 0x3EC layout using real `std::string @ +0x10` and checked `std::vector<int> @ +0x2C`. The same wave closes `get_stage_for_character/get_bgm_for_character @ 0x0043C0B0/0x0043C100` (72/72 each), `prepare_scenario_match @ 0x0043AE90` (119), `PlayerSlotRecord::select_deck @ 0x004313B0` (59), and `start_async_engine_scene_load @ 0x0043B840` (171).
- Small source abstraction can change ABI-visible lifetime. The two 72-byte character lookup wrappers share the same checked map algorithm and `lower_bound @ 0x0045C320`, but the original wrappers must each pass **their own `character_key` parameter stack slot** as the key pointer. Factoring that code into a helper makes VC8 first copy the key to a local and grows each body to 80 bytes. Likewise, `PlayerSlotRecord::select_deck` is exact when `source` is published at +0x1A8 and ordinary tail-call source selects +0x140 or +0x174; VC8 naturally overwrites the dead source argument slot with the selected profile-block pointer before jumping to `0x00409D60`. Do not replace these recovered source lifetimes with hand-written stack manipulation.
- Scenario preparation establishes a contiguous runtime owner at `0x006FCA4C`: deck index +0x04, flags +0x08/+0x09/+0x0A, 0x14-byte deck storage +0x0C, current-deck pointer +0x20, field +0x24, flags +0x28..+0x2A, later state +0x40/+0x44/+0x48..+0x4A. Exactness requires clearing flags +0x08/+0x09/+0x29 and publishing +0x2A=2 **before** exact-backed `PlayerSlotRecord::copy_deck @ 0x00431E50`; moving those stores after the call changes bytes. Async scene loading reuses the exact battle/network event RAII source shape `{HANDLE handle; unsigned wait_ms=0x10}`. With current `CreateEventA/WaitForSingleObject/CloseHandle` IATs, thread-control `0x006FBDC8`, `is_running @ 0x004146B0`, `start @ 0x00414600`, request `0x006FBD48`, and stdcall worker `0x0043B580`, ordinary `/GS` C++ is 171/171.
- Nearby UI roots remain deliberate non-acceptances. `CSelectScenario::update_selection @ 0x004248A0` reaches truthful **414/414** when transition/preview publications stay branch-local, but standalone VC8 orders the positive/negative blocks differently; an ordinary early-return spelling grows to 437, so do not introduce gotos merely to force layout. `CSelectScenario::render @ 0x00424A40` remains 764/756 because VC8 hoists the `color_174` receiver across the seven-row loop. `CMenuResult::update_active @ 0x004472A0` has its result-list evaluation order recovered via short-lived selected-character locals, but standalone VC8 still tail-merges a target-retained 17-byte true epilogue; split/nested/bool source forms do not produce the shipped block placement. Keep these pending rather than adding fake liveness, volatile/register state, or assembly.

- The Arcade setup/transition continuation closes **2 authored canonical-exact vtable roots / 1,073 bytes**. Current `CBattleManagerArcade` vtable `0x006C154C` owns `prepare_battle_setup @ 0x00473520` in the same slot where base vtable `0x006C1504` owns exact `CBattleManager::prepare_battle_setup @ 0x00472110`, and owns `transition_slot_34 @ 0x00473250` over exact base `0x004728C0`. The derived setup override is 842/842 from ordinary `/GS` C++ using a real checked `std::deque<int>` candidate pool and `std::list<int> @ +0x5B4`; the derived transition override is 231/231 from an ordinary sparse `switch`.
- Arcade source lifetime is byte-significant without register coercion. The 842-byte setup loop must create the pushed deque value **inside** the `character != player_character` branch; keeping the loop variable itself addressable across the loop moves its stack lifetime. `get_score_data()` is deliberately re-read at each target branch rather than cached. Current block/EH order proves the source case/branch topology, and the special stage expression is the direct constant choice `selector_random_roll(2) ? 14 : 16`; VC8 naturally emits `test/sete/lea`. The final stage/BGM writes are `MatchSetup::stage_04/+0x04` and `bgm_id_05/+0x05`, while entry `setup+0x2D` is side-1 `fighter_option_05`; do not collapse those distinct physical owners.
- The 231-byte Arcade transition override is exact only when source remains a sparse `switch(phase)` with cases 4 and 6 rather than an if/else chain: VC8 then emits the shipped `phase-4 / phase-6 / default` subtraction chain and target block order. Phase 4 operates on **`fighter_10 @ +0x10`**, not `fighter_0c @ +0x0C`, testing `terminal_finalize_4EB` and `terminal_delay_4E8` before the out-of-line Arcade helper `0x00473110`; phase 6 reuses the independently exact 0x44 `CMenuEnd` allocation/constructor/install path, and all other phases delegate to exact base transition. Exact callers may pin the ABI/identity of `0x00473110` without promoting it: its semantics are reconstructed in the setup TU but natural VC8 still coalesces two adjacent cdecl stack cleanups, leaving the callable body about six bytes short.

- The effect handle-pool / fighter-factory wave closes **3 authored canonical-exact roots / 1,635 bytes**. `CHandleManagerEx<CSelectObject>::acquire @ 0x00422B70` and `CHandleManagerEx<InfoEffectObject>::acquire @ 0x0046F190` are both 434/434 from the same ordinary checked-vector/list pool algorithm already proven across the roster pools: critical-section RAII, free-slot reuse, 16-bit slot/generation publication, generation wrap, vector growth, checked list erase, placement reconstruction, and checked final lookup. Current exact `TObjectManagerBase` acquire/preallocate callers independently own both specializations. Target allocation extents fix `sizeof(CSelectObject)==0x170` and **`sizeof(InfoEffectObject)==0x178`**; the latter corrects the earlier truncated 0x170 probe and agrees with exact `InfoEffectManager::spawn` fields at +0x160/+0x164/+0x16C.
- `BattleSlotState::create_fighter_for_slot @ 0x004642F0` is 767/767 only after recovering two source facts rather than transcribing the decompiler. The fifteen branches are ordinary typed `new FighterType(setup)` expressions using target-proved allocation extents, not manual `operator new` plus placement new; VC8 then reuses the dead setup argument slot for new-expression cleanup exactly like the shipped body. The physical source case order is **0,1,2,5,3,4,6,7,8,9,11,10,12,13,14**. Current target block addresses and EH states encode that order independently, while the switch table still maps every character id correctly. All fifteen constructor targets are already canonical exact, and exact `Fighter::initialize_fighter_spell_resources @ 0x00463070` closes the common suffix.
- Effect-pool hierarchy recovery also establishes a reusable stop condition. Current RTTI and exact `CharacterObject` precedent place source-first non-polymorphic `CObjectBase` physically at `EffectObjectBase +0x158`, behind the primary `AnimationObject`; `sizeof(EffectObjectBase)==0x160`, WeatherEffectObject allocates 0x180, EffectObject 0x178, and InfoEffectObject 0x178. A visible non-throwing `AnimationObject()` contract is required for Weather/Effect pool bodies to reach the target 463-byte size without synthetic new-expression cleanup. Both 463-byte siblings and the 59-byte CSelect/Info object ctors then retain the same single receiver-hoist scheduling residue: target stores handle +0x15C before forming the next constructor receiver, whereas natural VC8 forms ECX first. In-class, out-of-class, initializer/body, destructor-visibility, and `__forceinline` truth tests do not move it. Keep these bodies pending; do not use register/volatile forcing or fake dependencies.

- The normal InfoManager / InfoLong render wave closes **3 authored functions / 1,386 bytes**: `CInfoManager::render_normal @ 0x0046E650` is 753/753 and `InfoLongRecord154::render/update @ 0x004325C0/0x004327C0` are 323/310. The normal render is a single large RTTI-owned root, not a wrapper: it owns both network/local profile overlays, upper/lower design placement, two exact InfoShort renders, two BattleInfo renders, two InfoLong renders, and the effect/render-mode phase sequence. Current `g_scene_renderer @ 0x006FBCA4` exposes Fighters at +0x0C/+0x10; Fighter byte +0x72C gates profile rendering. Network profile name starts at session +0x24 and polymorphic 0x94 sprites live at +0x27C/+0x310; local PlayerSlotRecord reuses the exact-backed polymorphic sprite +0x04 and `std::string +0xB4` whose size is physically at record +0xC8.
- Profile placement in `0x0046E650` is source-type evidence, not arithmetic cleanup. The target first converts unsigned name length to **float** (including the 2^32f correction), multiplies by double 7.0, rounds that product back to float, subtracts it from double 212.0, then rounds the final X to float. The truthful source is the nested conversion `float(212.0 - float(float(name_length) * 7.0))`; `float(name_length * 7.0)` converts through double and emits the wrong `dc` 2^32 correction, while named width/X locals keep extra stack lifetimes. A real polymorphic 0x94 profile-sprite static type is likewise required so VC8 forms the receiver before loading its vptr; the manual `UiSprite94` vtable facade was eight bytes short.
- `InfoLongRecord154` now has a cross-method physical layout. Two real polymorphic 0x94 sprites occupy +0x04 and +0x98, so animation state begins at +0x12C. The second sprite's scale fields are **sprite +0x88/+0x8C** with a four-byte tail to 0x94; update/render independently establish timer +0x12C, alphas +0x130/+0x131, mirror flag +0x132, x/y +0x134/+0x138, velocities +0x13C/+0x140, scale +0x144, active +0x14C and lifetime +0x150. Render is exact only when `primary=&primary_004` becomes live before the first color call and `secondary=&secondary_098` is formed only after that call; declaring both before the call shortens the body by four bytes, while direct members swap EBX/EDI. Update requires unsigned `alpha > 0` predicates (`jbe` after `test`) and explicit `next=timer+1; lifetime=lifetime_150; timer=next` order. These are ordinary source-lifetime facts, not register directives.

- The Info combo / short-record runtime wave closes **4 authored functions / 1,309 bytes** and removes four generated review candidates / 638 bytes. `InfoComboResource::render_combo @ 0x0046A660` is 413/413, `InfoShortRecord34::render @ 0x0046A800` is 156/156, `InfoShortRecord34::update @ 0x0046AC30` is 572/572, and normal `CInfoManager::update @ 0x0046E5A0` is 168/168. The decisive physical contract is a real checked `std::deque<InfoComboEntry>` at record +0x20 with a **12-byte by-value entry** `{float x, int timer, byte item_index}`. VC8 therefore selects deque block size one; target `map[index]` is the entry itself. Pointer-deque models and cached-entry loops are rejected because they emit different block/index topology.
- Source spelling is observable without being a register directive. The 156-byte render is exact with `player_index == 0 ? 0.0f - x : x + 520.0f`, preserving target `fld x` before the branch, `fldz/fsubrp` on the zero path, and one final store. The 572-byte update requires ordinary integer promotion of `fighter->flag_491 - active_mask` before `& (1 << i)`; narrowing back to `unsigned char` shortens the body. Its player-direction branch is physically `player_index == 0` fallthrough with -300 and nonzero +300. These are semantic expression/order facts, not permission for volatile/register forcing.
- The same exact TU proves generated ownership separately from authored consumers. SHA-pinned full-inventory fingerprints classify `std::deque<InfoComboEntry>::_Growmap @ 0x0046AA60` (341) and `push_back @ 0x0046ABC0` (101) compiler-generated. Expanding the existing SpellRecord map probe with `clear()` also classifies implicit `SpellRecordView::~SpellRecordView @ 0x0042CF20` (71) and checked-tree `_Erase @ 0x0042E370` (125). All four are accepted only because their relocation-masked fingerprints are inventory-unique across all 4,010 spans.
- Two adjacent authored leads remain pending rather than being forced. `InfoComboResource::load @ 0x0046A8C0` improves from a flat 451-byte spelling to **427/413** when the repeated number-owner replacement is expressed as a real inline template binder; that recovery also creates the target long-lived found-object lifetime, but standalone VC8 still splits receiver/number address across two registers. `CInfoManager` normal render `0x0046E650` remains a high-value 753-byte follow-up now that both 0x34 render children and combo render are exact; recover its profile/network branches from current callers rather than copying the decompiler.

- The InfoManager binding/lifetime continuation closes **9 authored functions / 924 bytes** across four ordinary VC8 TUs. `InfoShortRecord34::bind_46a620 @ 0x0046A620` fixes a physical 0x34 record prefix: combo +0x00, Fighter +0x04, duration +0x14 = 90, state +0x18 = 0, active byte +0x1C = 0. Keep the leaf definition in a separate TU from manager roots: exposing it to the 214/215-byte callers risks inlining, while declaration-only visibility reproduces the linked target.
- Normal and Story pair binding independently prove the same **real `Fighter *fighters[2]` local lifetime**. `CInfoManagerBase::bind_fighters @ 0x0046EF40` is 214/214: clear +0x494/+0x484, release tracked effects from +0x08, fetch renderer fighters +0x0C/+0x10, bind 0x34 records +0x16C, normal `BattleInfoRecordF8` records +0x498/+0x590, 0x154 records +0x1D4/+0x328, and publish Fighter+0x33C at +0x47C/+0x480. Story `0x00471F10` is 215/215 with the same local array but uses normal record +0x498 for fighter 0 and Story attach +0x590 for fighter 1, then publishes +0x494 = 30. The compiler-generated stack-relative pair walk is therefore source-lifetime evidence rather than a register trick.
- `CInfoManagerStory::refresh_roster_state @ 0x00471EC0` is 78/78 and cross-validates the second-side layout: renderer fighter[1] -> short record +0x1A0, published pointer +0x480, Story record +0x590, long record +0x328. Normal/Story indexed effect wrappers `0x0046F820/0x00472060` are 35/31 and truthfully forward to the pending `BattleInfoRecordF8::emit_group_effect @ 0x0046F9F0`; Story forwards only side zero. Exact callers can establish a pending callee ABI without pretending the callee itself is exact.
- RTTI-owned normal `CInfoManager` lifetime at `0x0046F7A0/0x0046F850/0x0046F920` is canonical 119/171/30. The exact `CInfoManagerBase` occupies 0x498 bytes and the derived normal class owns **two physical `BattleInfoRecordF8` objects** beginning at +0x498. Natural `/GS` C++ selects VC8 EH-vector construction/destruction with callbacks `0x0046F940/0x0046F970`, publishes current vtable `0x006C3850`, calls pending-but-identified normal initializer `0x0046EB90`, and tears down upper/lower/combo/effect resources before the record array and base. Because EH vector helpers take ctor/dtor callbacks as absolute function pointers, an exact callable body is not by itself enough relocation evidence; the callbacks need independently attested DIR32 identities.
- Four adjacent InfoManager bodies are deliberate stop conditions, now with reusable evidence. Normal initializer `0x0046EB90` is semantically complete at **230/228**: target retires the upper/lower design lifetimes after two record initializations and reuses EBP for the +0x48C pointer-walk cursor, while truthful standalone VC8 keeps the upper design CSE live and spills the cursor; direct member-expression spelling worsens to 234, so do not block CSE with volatile/fake aliasing. `InfoLongRecord154::bind @ 0x00432550` is **107/107** with only width/height output stack-slot coloring different; reversing declarations is codegen-neutral and a real two-element array grows to 113, so do not alias dead parameters to force slots. `BattleInfoRecordF8::emit_group_effect @ 0x0046F9F0` has closed x/y/direction/emitter semantics but natural typed source is 61 versus target 85 because target materializes an 8-byte x/y frame; do not add fake locals. `BattleInfoRecordF8::bind_fighter @ 0x0046FA90` reaches the full ownership/gauge semantics at 612 versus target 648; recovered `TGageValue<T*>` is a 16-byte polymorphic `{vptr,target,float start,float span}` object, but target keeps a 0x0C loop conversion frame that standalone VC8 coalesces. Preserve this type/lifetime evidence rather than inserting dummy stack state.

- The spell/common-resource + Story lifetime wave closes **20 authored functions / 2,433 bytes** while replayably excluding three generated short-set COMDATs. `SpellDataOwner::find_local_then_common_spell_record @ 0x00432D20` is 179/179 with the real checked `std::map<int,SpellRecordView>` contract for both the owner-local tree and loader-zero `g_common_spell_tree @ 0x006FA874`; natural `find()/end()` emits the target owner/end validations without manual node arithmetic. Indexed display callback `0x00432DE0` keeps name length, unclamped width and clamped width live simultaneously; collapsing them changes the target register schedule.
- Common spell resources are one target-backed ownership island. `clear_common_resources @ 0x00433990` (235) iterates and clears native checked `std::deque<unsigned> @ 0x006FA6C8`, releases standalone handle `0x006FA6DC`, then clears the shared spell map. `reload_common_resources @ 0x00434AC0` (332) loads `data/battle/cardbar.bmp`, configures the current 0x128 effect record, and calls the shared parser with `common` and `data/csv/common/spellcard.csv`. A SHA-pinned `/GS` `std::set<short>` probe separately classifies `0x00432F90/0x00433190/0x00433450` compiler-generated, so generated container code and authored consumers remain distinct claims.
- Story bootstrap closes a cross-checked manager lifetime chain. `CBattleManagerBase @ 0x0046B200/0x0046B2A0` owns three arrays of two 0x0C CollisionList elements at +0x2C/+0x44/+0x5C, naturally selecting VC8 EH vector ctor/dtor iterators. `CInfoManagerBase @ 0x0046F5D0/0x0046F6A0` constructs exact InfoEffectManager +0x04, design resources +0x98/+0xCC/+0x100, two 0x34 records +0x16C and two 0x154 records +0x1D4, then publishes exact-backed `g_info_manager @ 0x006FBCA8`; natural member order gives ctor/dtor/scalar 195/185/30.
- `CInfoManagerStory @ 0x00471FF0/0x00472080/0x004720F0` is an ordinary derived lifetime with one physical `BattleInfoRecordF8 @ +0x498`. Its independent ctor/dtor `0x0046F940/0x0046F970` are 35/118: five retained effect pointers at record +0xD8..+0xE8 are zero-constructed, and each non-null pointer decrements the referenced object's ownership count at +0x158 before being cleared. This is positive application ownership evidence, not a compiler-generated special-member guess.
- TU visibility remains positive evidence for `CSpriteEx::scale_xyz_pivots @ 0x00434C10`: an independent declaration-only TU emits three out-of-line axis-scale calls and is 80/80; placing the wrapper beside the definitions lets VC8 inline it to 72 bytes. Story loading helpers `0x00434CC0/0x00434CE0/0x00434D10/0x00434D40` likewise preserve out-of-line manager boundaries and current global identities, closing 257 bytes as one bootstrap island.

- The PlayerSlotRecord/profile text-config wave closes **4 authored canonical-exact functions / 742 bytes** and one reusable 0x33C record contract. `PlayerSlotRecord::set_profile_color @ 0x00431440` is 461/461, `reload_profile @ 0x00431DA0` is 167/167, indexed deck copy `0x00431E50` is 47/47, and `ScenarioEffectConfig128` default construction `0x004113E0` is 67/67. Exact state-two/state-three ProfileMenu callers plus current globals identify the same PlayerSlotRecord receiver. The physical prefix is texture handle `+0x00`, a real **polymorphic 0x94 sprite at +0x04**, `std::string +0x98/+0xB4`, and twenty checked `std::deque<unsigned short>` values beginning at `+0x1AC`. This refines the old opaque `ProfileMenuBaseData` prefix without globally exposing the sprite type to already-exact TUs.
- `PlayerSlotRecord::set_profile_color` is a positive virtual-dispatch typing reference. The text path itself is already target-backed: 0x194 owner ctor/configure/dtor `0x00411430/0x004115A0/0x004114C0`, exact Shift-JIS profile label `0x006C11DC`, the same 0x128 config defaults used by exact profile UI, and title-resource release/create-texture calls. A manual `UiSprite94::set_texture` ABI facade produced the correct 461-byte size and an exact 375-byte prefix but prepared arguments before reading the sprite vptr. Declaring the `+0x04` member as a genuine polymorphic class with set-texture at vslot `+0x14` makes ordinary C++ emit the target vptr-load-before-argument schedule and closes 461/461. When current control flow proves a real virtual call, prefer a real polymorphic source type over manual vtable arithmetic.
- Two smaller PlayerSlotRecord bodies add source-lifetime rules that generalize. `reload_profile` is exactly a real local `std::string` copy of member `+0x98` followed by `load_profile(c_str())`; this naturally gives the target `/GS` EH state and inline SSO cleanup. The 47-byte deck adapter is `if (output) *output = decks[index]; return &decks[index];`. Caching `&decks[index]` in a local shortens it to 35 bytes because VC8 reuses the address; repeating the member expression forces no register and simply reflects the target dataflow, preserving this/index across the generated checked-deque assignment and recomputing the member for the return.
- The nearby profile loaders are bounded TU/LTCG stop conditions. `PlayerSlotRecord::load_profile @ 0x00431AC0` is 723/728 with a 273-byte exact prefix when the path is constructed as `std::string file_name(path)`; default-construction followed by assign was 707/728 because EH state began too early. The remaining five bytes are one checked `deque::end()` upper-bound self-comparison that standalone VC8 eliminates. Named iterators, `rbegin()`, and prefix-`--end()` alter the whole allocator/frame and are rejected. Deck CSV loader `0x00431E80` is 487/463 with an exact 75-byte prefix and complete `character_key_to_name -> data/csv/<name>/deck.csv -> CsvReader -> clear -> 20 push_back` semantics; its extra bytes are temporary-string dead stores/EH visibility, and adding a truthful `throw()` destructor contract changes nothing. Do not recreate either residual with redundant comparisons, fake iterator lifetimes, volatile/register forcing, assembly, or padding.
- The package-reader / hit-scalar / outcome / checked-iterator recovery wave adds **5 authored canonical-exact functions / 1,873 bytes** without codegen coercion. `CPackageFileReader::seek @ 0x0041C180` is 124/124 only when each origin arm publishes `position = SetFilePointer(...) - entry_offset` and returns locally; a shared temporary/common return tail changes VC8 control-flow lowering. `AttackCandidate::calculate_forwarded_quantity @ 0x0045B4A0` and `calculate_base_hit_quantity @ 0x0045B600` are both 347/347 when source/related Fighter accesses remain direct member expressions, the first source*target factors stay in one expression, and the partial-range gate is spelled `maximum > current`. `CollisionContext::dispatch_family2_against_family1 @ 0x0046DB30` adds 525/525 from three real 8-byte checked end iterators `{owner,node}`: owner validation belongs to every loop-condition comparison, not just loop entry. This removes the retained fake padding arrays and naturally reproduces the target 0x28 frame and all 525 bytes.
- `CollisionContext::try_outcome_path_a @ 0x0046D3F0` is 530/530 after restoring three current-target dataflow details: alias-sensitive flag/amount paths reread `candidate->frame_1a4`, the terminal counter gate is `counter_482 <= 0` followed by the decreasing-counter test, and the `factor_4d0` conversion is a real signed-short `adjusted_amount` reused for both capped-counter publication and deferred quantity. The sibling `try_outcome_path_b @ 0x0046D610` remains deliberately non-exact at 530/542: direct frame reload spelling does not move its first residual, where target keeps frame flags in EDX and fresh VC8 chooses ECX. `Fighter::select_outcome_path_from_frame_flags @ 0x0045E120` improves from the retained 601-byte body to 610/620 after current globals/AL-width recovery, but changing the final positive/negative branch source order produces 609 bytes and does not solve the persistent EDI-versus-EBP zero-register lifetime. `apply_forwarded_quantity_to_pair @ 0x0046C5B0` remains 299/301; removing the historical `register` keyword is codegen-neutral. Treat all three as allocator/source-partition blockers until stronger evidence appears; do not use volatile/register forcing, fake liveness, copied bytes, assembly, or padding.
- The adjacent `dispatch_family1_object_clashes @ 0x0046DD40` is now a bounded checked-iterator/TU blocker rather than an unexplained 19-byte gap. Target 686 repeatedly performs owner self-validation in the second cross-list pass (`cmp owner,owner` before outer and inner end comparisons). The old retained 667-byte source also carried a `volatile context=this` scheduling hack; removing it is codegen-truthful and leaves the current natural body at 664 rather than preserving a forbidden coercion. Three checked-owner probes were rejected: explicit begin/end owner locals grow to 712 and spill the outer node, full iterator-vs-iterator end objects grow to 705, while a minimal `iterator.list != &family_1[n]` condition is proven equal and optimized away into a separate 657-byte CFG variant. Recover the original checked-STL/TU visibility that preserves those owner checks; do not reintroduce volatile state or fabricate owner liveness.
- The Fighter landing/runtime continuation closes ten authored bodies / **816 bytes** while also refining shared ABI/layout. `Fighter_resolve_stage_surface_landing_transition @ 0x00473DD0` is 442/442 from ordinary action-range semantics: exact stage-surface tests, real Fighter motion fields `y +0xF0`, velocity `+0xF4/+0xF8`, acceleration Y `+0x100`, angle `+0x12C`, action `+0x13C`, landing response `+0x18C/+0x190`, and virtual transitions to 71/704/9. The exact helper `is_y_at_or_below_stage_surface` remains an `int` ABI; this caller truthfully narrows the result to `unsigned char` because current target tests AL. Preserve the callee ABI and narrow only at a target-proved caller, rather than rewriting the shared return type.
- Translation-unit visibility closes two more shared wrappers without attributes. `Fighter_publish_battle_layout_scalar @ 0x004359D0` is an unused-this member whose target loads the exact-backed battle-layout aggregate at `0x006FBCC0` and calls `BattleBackgroundLayout::set_runtime_scalar @ 0x00427280` out of line. Defining the setter in the Fighter TU makes VC8 inline a 13-byte direct store; keeping the real setter in `BattleBackgroundLayout.cpp` and only its declaration visible produces target 21/21. Likewise `selector_random_range @ 0x004351A0` must live outside `OutcomeRandom.cpp`: same-TU visibility inlines `selector_random_roll` into MT19937, while a declaration-only TU naturally gives the target 26-byte `lower + roll(upper-lower)` wrapper. These are positive TU-boundary facts, not permission for `noinline`.
- Aggregate relocation addends remain one object identity. The scalar setter proves a real float at battle-layout **+0x40**, address `0x006FBD00`, inside the already exact-backed aggregate based at `0x006FBCC0`; the relocation ledger therefore extends the base symbol with addend `0x40` instead of inventing a second global. `Fighter_dispatch_indexed_event_member @ 0x004359F0` is the companion 14-byte caller-proven unused-this wrapper over exact `dispatch_indexed_event`, and `Fighter_has_crossed_stage_surface_while_descending @ 0x005F9310` is the 64-byte shared `y <= stage && vy < 0` predicate used by current core Fighter dispatchers.
- `CSpriteEx::set_uv_size @ 0x00407340` corrects the physical sprite layout: fields `+0x78/+0x7C` are **float texture width/height**, not integer facade words. The 95-byte exact body computes normalized `u/v`, combines them with UV origins `+0xE0/+0xE4`, and publishes all four 0x1C vertex UV pairs; all earlier exact transform TUs replay unchanged after the type correction. `AnimationObject_set_sprite_uv_size @ 0x00435520` is the natural 29-byte `this+4` wrapper and remains in a declaration-only TU so the sprite call stays out of line.
- Current render-callback recovery separates virtual semantics from source qualification. Registered callback `AnimationObject_render_group_callback @ 0x00493820` is 93/93 with render group `+0x33C`, auxiliary renderer `+0x154`, and secondary renderer `+0x338`. An ordinary unqualified `render_main()` emits a virtual call, but target directly calls exact base body `0x00435280`; the truthful C++ spelling is the **qualified** `AnimationRenderObjectView::render_main()`. Qualified member calls are therefore legitimate target-backed source recovery when current control flow proves direct base dispatch; do not replace them with manual vtable arithmetic. Its unused-this auxiliary finalizer `0x004350F0` is 22/22 from exact render mode 1/0 plus `g_battle_render_pass += 2`.
- The newest compiler-origin census removes **35 review candidates / 2,363 bytes** with replayable evidence. Four exact-object manifests account for 11 / 592 bytes. A tracked `vector<CSpriteEx>` witness using the real 0xE8 application type uniquely identifies 10 helpers / 689 bytes, including `_Allocate @ 0x00435C80`, range destruction `0x00435FC0`, and the `0x00465B50..0x00465F90` copy/erase/fill/size family. Two checked-map witnesses use target node-layout evidence: flags at `+0x20/+0x21` imply a 0x24-node `map<int,16-byte-value>` family (7 / 506), while flags at `+0x104/+0x105` imply a 0x108-node / 0xF8-pair `map<int,244-byte-value>` family (7 / 576). The value facades prove generated code shape/ownership only; they do not claim the game's semantic mapped type. Every anchor is accepted only after complete 4,010-span fingerprint replay.

- The CScript command-runtime template family is now current-target exact-backed as one coherent source model: **35 distinct authored functions / 2,803 bytes** in `cross-v106a-script-command-runtime`. Fourteen RTTI-owned `CCommand0`/`TCommand1..5` parse virtuals, fifteen RTTI-owned `CScript::CCommandFactory<T>::create` virtuals, and six distinct command/factory lifetime bodies all cold-compare canonical exact. Together with the already-exact fifteen `register_command` specializations, 721-byte scene-timeline registry, 65-byte `CScript` ctor, and 505-byte `CComCharacter::parse`, this closes the generic command registry/runtime ABI rather than a collection of clone guesses.
- Current command RTTI fixes the physical source layout: `CCommandBase` is a four-byte polymorphic base, every generic command publishes its registry id at `+0x04`, and typed payload begins at `+0x08`. Generic parse uses one ordinary `char delimiter[2] = ","` plus `strtok_s`; `int`/`bool`/byte fields derive from `atol`, float fields from `atof`, and string fields use the existing string-assign ABI with a real null-token-to-empty-string branch. VC8 reproduces all thirteen non-empty template parser sizes (48..200 bytes) plus the three-byte `CCommand0::parse` without inline assembly, copied bytes, register forcing, or fake liveness.
- The command-factory construction order is reusable source evidence. Target create virtuals are exactly the lowering of `Command *command = new Command; command->command_id_04 = command_id_04; return command;`, **not** `new Command(command_id)`. Default construction owns the vptr/string lifetime inside the allocation-success branch, while command-id publication is a separate post-construction assignment; consequently target null-allocation paths still contain the id store. That natural source shape reproduces every 41/56/66-byte factory body. Do not “fix” the apparently unsafe null path or move the id into a command constructor: both change the linked VC8 CFG.
- Command lifetime folding is also class evidence rather than a byte-clone shortcut. Trivial command specializations share the 31-byte `CCommandBase` scalar wrapper; one-string command layouts fold to a 64-byte string-destroying wrapper where compatible; `TCommand5<int,string,int,int,int>` has its own 64-byte wrapper because the string begins at `+0x0C`; `TCommand2<string,string>` has an independently exact 77-byte normal destructor plus 30-byte scalar wrapper. Current RTTI/vtables tie each physical body to the relevant command specializations before canonical comparison. Expanding `ScriptCommandRegistry.hpp` to this real runtime contract leaves every previously exact register-command/timeline/lifetime unit unchanged, so the shared header is safe for later Scenario interpreter work.
- The adjacent effect-animation sequence family is now fully exact-backed: eight shared methods / **856 bytes** at `0x00473950/0x004739D0/0x004739F0/0x00473A40/0x00473A70/0x00473B00/0x00473B80/0x00473C50`. Four current vtable data-xref owners (including the `0x006C0968/0x006C1458/0x006C14D0/0x006C3800` slot family) establish shared effect-object ownership rather than address-order inference. The decisive layout correction is a real checked `std::vector<EffectAnimationFrame28>` at **block +0x04**, with 0x1C-byte frames; target `add block,4` receiver formation, checked indexing, and `size()` lowering all appear automatically once the native container is modeled. Manual begin/end fields were three bytes short in apply/next/previous block and are rejected.
- Effect sequence source order is also target evidence. `select_sequence @ 0x00473B80` does **not** clear `time_counter_144` before checked-tree `lower_bound`; it publishes the selected block/index first, then clears time before walking `next_1c`. `reset_sequence @ 0x00473C50` clears `sequence_index_13e` before `time_counter_144`. With tree `+0x160`, current block `+0x168`, frame index/count `+0x140/+0x148`, and virtual set-frame slot `+0x1C`, ordinary VC8 reproduces all eight bodies exactly. Preserve these semantic lifetime/order facts instead of using raw vector fields or scheduling coercion.
- Four new fail-closed generated-origin manifests remove **11 review candidates / 2,142 bytes**: SpellRecord tree/copy COMDATs (4 / 935), `deque<YoumuObjectRecord>` helpers (4 / 615), Scenario event-parser `list<int>` range insert/assign (2 / 374), and BattleInputRecording `vector<FixedSlotEnvelope>::_Assign_n` (1 / 218). All are SHA-pinned VC8 rebuilds whose relocation-masked hit sets are checked against the complete 4,010-candidate inventory. A discovery scan of the new ScriptCommandRuntime object found no additional review-only standard-library hits; its matching fingerprints resolve only to the newly authored command bodies or already-excluded string/char-traits code, so nothing is double-counted.

- The canonical target is the original Japanese 1.06a executable described in
  `config/target.toml`; it is the payload named `app/th105.exe` in the official
  updater.
- The official payload and the selected game-directory `th105.exe` are
  byte-identical. The sibling `th105c.exe` is the older 1.06 binary and is not
  an acceptable substitute.
- The PE is x86, image base `0x00400000`, entry `0x0068B9D2`, with stripped
  relocations and Microsoft linker 8.0.
- Rich-header records point to compiler build 50727 and include 42 C++ LTCG
  inputs. This supports the VC8 family and warns against assuming independent
  object boundaries.
- The fresh IDA database exposes 4,001 auto-analysis function candidates. The tracked ledger contains 4,010 candidates because current target evidence recovered a missed independent CFileReader destructor at `0x0040CEB0`, source-level fighter phase entries at `0x00464630`/`0x00464780`, standalone `CMenuReplay` scalar wrapper `0x00446F40`, source helper `CMenuReplay::update_mode_state @ 0x00446590`, the independent `CFileWriter::~CFileWriter @ 0x00407BF0`, the independent reverse CSpriteEx quad-copy boundary `0x004073C0`, independent `FrameData::~FrameData @ 0x00421A70`, and the 284-byte Fighter counter-threshold body `0x0045B300`; IDA had attached or missed those boundaries during auto-analysis. The writer body is a complete 21-byte RET-terminated vtable-owned destructor surrounded by `INT3` padding. Auto-analysis ownership and unreviewed sizes remain provisional.
- The accepted 1.06a authored set contains 875 functions / 141,292 bytes in 269
  VC8 match units. Current-target IDA/call evidence, relocation reconciliation,
  source archaeology, and layout recovery establish hypotheses; only canonical
  VC8 zero-difference comparisons establish exactness.

- The battle-info / Story-manager core now contributes ten canonical-exact bodies / 2,126 bytes across four ordinary VC8 units. `BattleInfoRecordF8` is a real 0xF8 physical record with scalar header state through +0x20, ten groups of five `UiDesignObject*` pointers at +0x24..+0xD7, five owned-effect references at +0xD8, and three tail UI pointers at +0xEC/+0xF0/+0xF4. Normal and Story paths truthfully overlay +0x10/+0x14/+0x18/+0x1C; do not force one phase-specific name onto all uses. Exact consumers are initialize `0x0046FD20`, render `0x00470710`, Story initialize/attach `0x00471C10/0x00471BE0`, InfoManager runtime/bind/render `0x00471C90/0x00471D00/0x00471D10`, and three `CBattleManagerStory` phase virtuals `0x00471670/0x00471740/0x004717E0`.
- `BattleInfoRecord_initialize @ 0x0046FD20` provides two source-recovery lessons. Publishing the signed mode byte before the effect pointer preserves the target parameter/register lifetime. The `group_c4` binding formula is `i + 10*mode + 205`; target `lea`/add scheduling proves the multiply-by-ten even though the decompiler presented a multiply-by-five expression. When arithmetic feeds resource/object identity, audit the actual instruction chain before committing the decompiler algebra.
- `BattleInfoRecordF8::render_470710 @ 0x00470710` closes a high-value container identity: Fighter `+0x55C` is layout-compatible with native checked `std::deque<FighterSequenceSlot>` in this direct-index consumer. Branch-local out-of-line scale lookups preserve the target x87 lifetime; the final render must use the deque element directly as the sprite receiver, because first caching a `FighterSequenceSlot*` or sprite pointer moves the element lifetime to ECX and breaks the target EBX index-to-element transition. Do not overgeneralize this source shape: mechanically replacing the separate front-access logic in `BattleInfoRecordF8::update @ 0x0046FF30` with `deque[0]` shortens that pending body and is rejected.
- Current RTTI names vtable `0x006C1374` as `CBattleManagerStory`; constructor sites call base constructor `0x0046B200` before publishing that vptr. The recovered Story prefix fixes unsigned frame `+0x04`, Fighters `+0x0C/+0x10`, an InfoManager-like pointer `+0x8C`, and signed mode byte `+0x90`. Three vtable-owned phase methods are canonical exact together: threshold pipeline `0x00471670` (203), 180/300-frame pipeline `0x00471740` (157), and Story/async pipeline `0x004717E0` (309). The threshold switch intentionally has only modes 0/1/2; direct `g_battle_phase_block` rereads preserve ESI for the real 8-byte event RAII lifetime; positive success nesting preserves the shared return-2 node in the 157-byte method. These are semantic CFG/lifetime facts, not register directives.
- The exact Story trio now gives three canonical callers of `resolve_fighter_body_collision @ 0x0046CCA0` in the same RTTI-owned class island, alongside exact action/status/post-update phase calls. This strengthens caller/TU context for that 1,862-byte root but does not make it exact: the current standalone body remains blocked by float-stack/x87 lifetime. A real VC8 `/O2 /GL` + `/LTCG` experiment on the neighboring 323-byte Story runtime also retained its EAX/EDX scheduling residual, so LTCG is not a generic escape hatch for these allocator blockers.
- Core-root layout work on `Character::Character @ 0x00462AB0` recovers reusable Fighter/Character ownership even though the ctor remains deliberately non-exact. The best natural `/GS` source is 557/557 with only a nine-byte EH-state/store-order swap. Target/fresh evidence fixes `std::vector<int>* @ +0x130`, a 64×4 zero-construct handle array at `+0x164`, handle+sprite resource records at `+0x338/+0x3D0`, SpellDataOwner `+0x4F0`, sequence controller `+0x55C`, and deque-like owners `+0x65C/+0x68C/+0x710`. The four-byte handle constructor independently reproduces current `0x0040CF10` 9/9. Ordinary source/TU visibility, header-inline forms, `/Oi-`, `/Ob1`, `/Oy-`, `/Os`, `/O1`, and bounded `/GL` experiments do not move the final swap; do not add fake EH liveness to force it.
- `CProfileMenu` now has a cross-TU-proved physical string layout: its five 0x1C members at `+0x518/+0x534/+0x550/+0x56C/+0x588` are real VC8 `std::string` objects. Modeling them as such makes normal destructor `0x0044D340` canonical 422/422 while existing exact ctor, render, state-four/five/six, file-commit, and dispatcher TUs remain exact. Keep `MenuString28` as an ABI-compatible narrow view at helpers whose exact call contracts use that facade; do not confuse a call-site ABI view with the physical member type.
- C++ linkage is byte-significant under `/EHsc` even when the x86 stack ABI looks the same. `CProfileMenu::update_state_two/three @ 0x0044BEF0/0x0044BFE0` are 236/236 and 248/248 only when `install_menu_object @ 0x0043B0F0` retains its decorated C++ linkage. A false `extern "C"` declaration removes the target eight-byte new-expression cleanup-state reset before the install call. The same pair also requires `string_518.c_str()` to be evaluated before forming the `base_data_004` PlayerSlotRecord receiver. Preserve linkage and evaluation-order semantics rather than manufacturing volatile/register liveness.
- Current RTTI/vtable ownership now closes the shared `AnimationObject`/`AttackObject` sequence ABI. Fourteen ordinary-C++ bodies / 1,024 bytes are canonical exact: base action setters `0x00435200/0x00435220`; AttackObject setter/finalize overrides `0x0045BFE0/0x0045C000`; character-wave and forwarded-hit adapters `0x0045C030/0x0045C060`; and sequence frame/block/tree methods `0x0045C110/0x0045C180/0x0045C1A0/0x0045C1F0/0x0045C220/0x0045C2B0/0x0045C390/0x0045C460`. The narrow shared layout fixes action `+0x13C`, sequence/frame indices `+0x13E/+0x140`, timer `+0x144`, frame count `+0x148`, frame pointers `+0x150/+0x158`, current block `+0x15C`, checked action tree `+0x160`, 64-entry wave table `+0x164`, and related Fighter pointer `+0x170`; sequence frames are 0x88 bytes and block prev/next links are `+0x18/+0x1C`. `lower_bound @ 0x0045C320` remains review-pending rather than being counted as authored.
- Sequence recovery adds two compiler-shaping facts. A target-observed chained assignment of both frame pointers keeps the computed frame in EAX and naturally reproduces `apply_sequence_frame` 111/111; two independent assignments change the register/store schedule. The setter/finalize overrides require a separate declaration-only TU so the base action setters stay out of line. Keep this ordinary source partition instead of `noinline`, volatile/register tricks, or assembly.
- The current origin census has 1,002 target-backed exclusions: 417 VC8 runtime functions selected from SHA-pinned VC8 SP1 archives, 44 zlib 1.2.3 functions bounded by current-target provenance strings, 19 direct import thunks whose current names/opcodes are attested, 39 Ogg/Vorbis functions whose current candidate bytes are inventory-unique matches of relocation-free COMDATs, 154 additional Ogg/Vorbis functions whose non-relocation bytes uniquely match relocation-bearing COMDATs re-extracted from the SHA-pinned official Xiph Win32 SDK 1.0.1, and 329 compiler-generated checked/template functions freshly generated by the SHA-pinned VC8 SP1 compiler from tracked source/probe TUs. The generated/relocated anchors are fail-closed: every relocation must stay inside the candidate boundary, REL32 fields must belong to CALL/JMP/Jcc forms, at least 70% and 24 bytes of each candidate must remain exact non-relocation evidence, and any COMDAT tail must satisfy its manifest bound. A generated fingerprint must be inventory-unique unless the manifest declares an equivalence group; in that case its complete current-inventory hit set must equal exactly the addresses listed in that group. `config/function-origin-rules.toml` and `scripts/function-origins.py` replay all target/archive checks before materializing the census.

- The Menu overlay wave adds eight authored canonical-exact functions / 1,066 bytes across RTTI-owned `CMenuBattle`, `CMenuConfig`, `CMenuPractice`, and `CMenuConnect`. `CMenuBattle` closes a real 0x78 layout (`Menu`, `TitleDesignResource`, five UI pointers, `MenuCursorState`, checked `std::vector<int>`, owner pointer), but its constructor remains deliberately pending at 799/800 on stack coloring even after target-backed branch-local binding loops are restored. `CMenuConfig` proves natural reverse member teardown with `UiSprite94[2]` array destruction folded through exact `IColor::~IColor @ 0x0040AD50`. `CMenuPractice::render` requires checked-vector element **references** (`UiDesignObject *&`), not prematurely loaded pointer values, and its unresolved `+0x15C` layout gap is intentionally left unnamed. `CMenuConnect::update` is exact only with the target physical branch `state != 6`, pointer-walk item traversal, and the reviewed 195-byte RET boundary; its VC8 section has a 29-byte trailing switch table.


- Current AnimationObject render recovery closes seven more authored bodies / 1,325 bytes. RTTI-owned AnimationObject, AttackObject, CharacterObject and all fifteen CharacterObjectEx vtables share `render_main @ 0x00435280` and `render_color_mode @ 0x00435540`; natural C++ also closes battle-layout transform `0x004271B0`, checked render-list `back()` dispatch `0x00466AB0`, and sprite render-effect RGB/alpha/luma facades `0x006654D0/0x006655A0/0x00665650`. The shared render service is loader-zero pointer `0x006FD398`; `v4Arg0 @ 0x006C3290` plus stable vtable slots define the effect contract.
- Render source lifetime is byte-significant without being a register directive. `render_main` must keep mode 0 and mode 1 as separate source cases even though their statements are the same; grouping the labels lets VC8 merge a physical block that the target duplicates. `render_color_mode` must not cache `&sprite_004` before the switch: a long-lived receiver changes the entry register schedule. Repeating the member expression inside each case preserves ECX until dispatch and gives the exact 479-byte callable body; its 25-byte post-RET switch table/alignment tail remains compiler-owned.
- Checked-container choice can identify ownership. `BattleObjectManager::dispatch_render_sprite` is exactly the lowering of `std::list<RenderEntry*>::back()`: the target follows sentinel `+4`, performs both VC8 checked-iterator comparisons, loads the pointer payload at node +8, and tail-calls vslot +0x14. `front()` is a different topology and is rejected. Use such container-specific lowering as layout evidence rather than replacing it with raw node arithmetic.

## Repository decisions

- Adopt TH08's separation of mapping, source presence, exact matches, library
  classification, public CI, current handoff, and cold exact replay.
- Retain `functions.csv` plus `function-origins.csv` as a TH105-specific layer
  because static libraries, compiler code, thunks/funclets, and LTCG make a
  flat authored mapping unsafe.
- Retained 1.06 source is archaeology and a hypothesis corpus. It counts only
  after a current 1.06a mapping, semantic audit, reproducible VC8 unit, and
  canonical exact comparison justify the corresponding ledgers.
- Cross-version raw-byte identity is a prioritization signal, never match proof.
  The accepted 47-function wave came from raw-identical candidates, but every
  function was freshly compiled and zero-diff compared against 1.06a before
  promotion.
- Do not infer current REL32 callees solely by applying an address delta from
  1.06. A failed `load_spell_data` probe demonstrated that an independently
  moved callee can violate the surrounding delta; current IDA disassembly gave
  the correct 1.06a target `0x00434300`, after which the canonical comparison
  became exact.
- A normalized VC8 instruction/operand fingerprint of retained exact source is
  useful for *prioritizing* 1.06a candidates when raw bytes changed. Normalize
  relocation/address-sensitive operands only, then independently reconcile the
  candidate boundary and current-target callees before comparing. Template and
  clone families can have identical normalized signatures, so structural ties
  must remain unresolved until class/xref/relocation evidence disambiguates
  them.
- Current IDA vtable names can close DIR32 constructor blockers when paired
  with exact target bytes. The `AttackObject` and `CharacterObject` constructors
  were accepted only after their 1.06a vtable destinations (`0x006C2B3C` and
  `0x006C400C`) were recorded in the relocation allowlist and the fresh VC8
  comparison became zero-difference.
- A historical exact ledger boundary may itself be incomplete. The current
  `BattleController_dispatch_battle_state_frame` body is 535 bytes and fresh VC8
  matches all 535; the old 529-byte ledger stopped immediately before the final
  six-byte epilogue. Treat size deltas as boundary questions before assuming a
  source-level patch.
- Never use bytes that happen to occupy file padding beyond a PE section's raw
  initialized data as proof of a BSS/global variable's contents. Keep such
  DIR32 candidates blocked until address/storage identity can be represented
  truthfully by the comparator.
- A durable semantic mapping name and the COFF `symbol_base` used by the exact
  comparator may differ. Keep semantic names stable in the ledgers and use the
  actual compiled symbol for object extraction.
- `scripts/rank_retained_exact.py` is the durable migration path for historical
  exact source not represented by old match units. The survey found 152
  historical unconfigured `.cpp` exact hypotheses and could uniquely extract
  147 COFF symbols under the pinned VC8 profiles. Structural score and zero
  non-relocation differences remain ranking evidence, not identity or exactness.
  The `CFileReader::read` / `WriteFile` clone is the reference counterexample.
- The stage-surface array at `0x006FA898` is 0x500 floats / 0x1400 bytes. Current
  stage-geometry helpers legitimately relocate the same symbol with addend
  `0x13FC`, selecting element `0x4FF` at `0x006FBC94`; do not model that last
  element as a separate global.
- Target-specific resource/error strings are part of exact authored recovery. The
  DirectSound secondary-buffer path only became canonical after the retained
  localized error bytes were replaced with the original Japanese 1.06a Shift-JIS
  string; source-language data must follow the canonical executable, not a
  translated release.
- Current semantic globals should replace retained old-target magic absolute
  addresses when the 1.06a target proves identity. `update_common_fighter_state_window`
  now references the verified `g_battle_input_gate` instead of `0x006e4e28`, and
  `emit_effect_at_collision_center` uses the verified `g_effect_sink` instead of
  `0x006e6250`; both natural C++ recoveries compile exact under the current target.
- Preserve a shared callee's proven ABI when only one caller consumes a narrower
  result. `is_y_at_or_below_stage_surface` remains an exact `int`-returning helper;
  `update_common_fighter_state_window` reproduces the current `test al, al` by
  narrowing at the call site. Do not mutate a return type merely because x86 name
  decoration cannot expose that mistake.
- One current address can support multiple truthful narrow semantic views.
  `0x006FA21C` is independently used as the render-mode service by exact UI code
  and as the static phase service by exact battle code. Keep the views distinct in
  source while address-validating the shared object identity.
- DirectSoundGlobals begins at `0x00703858`, with primary/direct-sound/listener
  pointers at addends `0/4/8`. A comparator mismatch caught the incorrect model
  that treated the `direct_sound` field at `0x0070385C` as the object base.
  `shutdown_direct_sound` independently exercises all three addends in listener,
  primary, device order and now matches 82/82; this is positive evidence for the
  full three-field layout. The unrelated shutdown cluster at
  `0x007036BC..0x007036C4` remains separate until independently proved otherwise.
- Exact byte recovery does not prove authored ownership. The retained
  `create_collision_list_node` source has a zero-non-relocation-difference
  51-byte candidate at `0x00421F30`, but current xrefs show that helper is shared
  broadly across engine and roster container code. It remains origin-review
  pending. By contrast, `create_collision_list_sentinel` at `0x00435E50` is
  authored-accepted because exact `initialize_collision_list` stores its result
  as the sentinel and accepted roster units independently resolve the same
  helper; fresh VC8 is 26/26 exact.
- Nearby vtables must not be conflated by address proximity. `ResultList` uses
  the vtable at `0x006C2058`, while current `CMenuResult` destruction writes the
  distinct vtable at `0x006C2070`; its slot `+8` points to the accepted 43-byte
  `CMenuResult::update` at `0x00447560`.
- Current indexed event dispatch uses the 128-entry handle table at `0x006FCA98`,
  service object at `0x00702288`, and dispatch helper `0x00401D50`. The 28-byte
  `dispatch_indexed_event` wrapper and multiple accepted callers agree on those
  identities, so later event/audio work should reuse them rather than rediscover
  old-target globals.
- The current input-selection global map is now exact-backed: player records at
  `0x006FBF20` with stride `0x33C`, source bytes at `0x006FBD38`, selected-input
  pointers at `0x006FBD3C`, session option at `0x006FBD44`, network-session
  pointer at `0x006FBD5C`, player2 input storage at `0x006FBDD0`, and combined
  menu input at `0x006FCF80`. The combined counter accessor is the same object at
  addend `+0x38`, not a separate global. Replacing retained 1.06 magic absolutes
  with these semantic externs made ten accessors naturally compile exact without
  changing control flow.
- The battle phase wrappers at `0x0046AF90` and `0x0046AFA0` are a
  reference normalized-clone tie that current tail destinations resolve. Both
  load exact-backed fighter-phase context `0x006FBC9C`, but `0x0046AF90` jumps
  to LTCG entry `0x00464630`, whose five passes are the action/owned-object
  callback phase, while `0x0046AFA0` jumps to `0x00464780`, whose three passes
  are position, transient-status, and timer cleanup. Both wrappers are 11/11
  exact after semantic global migration.
- A source-level callee may survive in the linked target only as an IDA tail chunk. It is valid to map a REL32 relocation to that observed entry when body semantics establish identity. When stronger evidence also proves an independent source/compiler boundary—an exact wrapper tail-jump to the entry, a complete RET-terminated body, and a fresh VC8 section of the same extent—add a tracked candidate without mutating IDA. The fighter phase entries `0x00464630` (323 bytes) and `0x00464780` (187 bytes) are the reference cases.
- `run_post_update_callbacks_and_global_state @ 0x0046AFB0` is 51/51 exact after
  replacing stale 1.06 globals with exact-backed `g_info_manager` at
  `0x006FBCA8` and shared battle-setup state at `0x006FBCC0`; its current helper
  destinations are `0x00426BB0` and `0x00426DF0`.
- Low non-relocation mismatch counts are still only ranking evidence. Retained
  `CFileReader_dtor` initially ranked `0x00419FD0` with only three non-relocation
  mismatches, but current IDA shows that target is a `CBitmapData` destructor
  restoring its own vtable and freeing `this+0x1C`; it was rejected immediately.
- The actual plain `CFileReader` destructor is `0x0040CEB0`. A canonical-PE
  wildcard fingerprint masking only the vtable and CloseHandle IAT fields finds
  two exact 21-byte destructor shapes: `0x00407BF0` restores the vtable whose
  method slot is WriteFile, while `0x0040CEB0` restores `0x006C0F34`, whose
  subsequent slots are the accepted reader methods `0x0040CED0`, `0x00407C40`,
  `0x00407C50`, and `0x0040CF00`. This is the reference way to split tiny
  polymorphic destructor clones.
- The same current `CFileReader` vtable `0x006C0F34` closes two more class
  identities: its seek slot points directly to `0x00407C50`, a 25/25 exact
  SetFilePointer forwarder using the handle at `this+4`, and its first slot points
  to scalar-deleting destructor `0x0041B890`, which restores the reader vptr,
  CloseHandles the file, conditionally deletes on flag bit 0, and is 45/45 exact.
  The seek machine body may be folded/shared with a writer, but the reader's
  source-level ownership is independently established by this vtable edge.
- The neighboring `CFileWriter` class is now independently exact-backed rather than a masked clone hypothesis. Current RTTI/vtable `0x006C0F4C` owns normal destructor `0x00407BF0` (21/21), `write @ 0x00407C10` (35/35), the physically folded last-size/seek bodies at `0x00407C40/0x00407C50`, and scalar-deleting destructor `0x00407C70` (45/45). Count only the three distinct writer bodies: a second class owning an ICF/folded address does not create a second function candidate. `write` is identified by its actual KERNEL32 `WriteFile` IAT, not by normalized similarity to the reader forwarder.
- IDA currently chunks `0x0040CEB0` under an unrelated large function and the
  original 4,001-row inventory omitted it. Canonical target layout gives a hard
  boundary: `INT3` padding at `0x0040CEA5..0x0040CEAF`, the 21-byte RET-terminated
  body at `0x0040CEB0..0x0040CEC4`, more `INT3` padding through `0x0040CECF`,
  then accepted `CFileReader::read @ 0x0040CED0`. This first boundary correction
  raised the tracked inventory from 4,001 to 4,002. The later source-level phase
  entries at `0x00464630` and `0x00464780` raise the current corrected inventory
  to 4,004. Preserve target-backed boundary corrections in the ledger rather than
  mutating IDA to satisfy the build.
- `mt19937_next_u32 @ 0x004066B0` is 279/279 exact. Current IDA confirms the
  standard 624-word MT19937 twist loops, fallback seed 5489, and tempering masks.
  The historical 271-byte row was a stale boundary; fresh VC8 and current target
  both establish the full 279-byte extent.
- `CriticalSectionWrapper::enter/leave` are a relocation-normalized tie only.
  Current `0x0040AC30` calls the `EnterCriticalSection` IAT at `0x006C0108`, while
  `0x0040AC40` calls `LeaveCriticalSection` at `0x006C0104`; both perform the
  same `this+4` adjustment. The imported destination is therefore sufficient to
  disambiguate the two 11-byte authored wrappers, which both compile exact.
- `NetworkSessionState` shifted by `+0x10` in the recovered field window between
  retained 1.06 and current 1.06a: the guarded copy is now `+0x6C0 <- +0x6D8`.
  This is not inferred from one clone: current caller `0x00426180` obtains the
  object through exact `get_network_session @ 0x0043B090`, calls the helper, and
  independently inlines the same guarded field copy. Updating only the struct
  padding/field offsets makes `0x00426160` naturally 22/22 exact.
- Historical exact rows can truncate a final instruction. `CMenuResult::render`
  was recorded as 267 bytes but current/fresh VC8 is 270; `CMenuResult::~CMenuResult`
  was 253 but current/fresh is 256. Current render semantics are the optional
  stand, cursor, sixteen result rows, design, and GuideOverlay; current destructor
  restores `CMenuResult` vtable `0x006C2070`, clears fifteen `0x108`-stride result
  lists, and tears down design/guide/vector/base state. Accept the 270/256 current
  boundaries, not the stale historical sizes.
- The same boundary rule applies to relocation-free fighter reset loops.
  `reset_fighter_fields_6bc_728 @ 0x0046AFF0` is 130/130 and
  `reset_fighter_fields_6b4_728 @ 0x0046B080` is 222/222. IDA shows both iterate
  exactly two fighter pointers from the collision context and clear the retained
  field sets; the old body-set sizes 127/219 simply omitted the final three bytes.
- `consume_counter_484_steps @ 0x0045B260` is 149/149 exact. For each requested
  step while counter `+0x484` is positive, it subtracts 200 with zero clamp,
  mirrors to `+0x482`, clears `+0x486`, and calls the exact-backed InfoManager
  with the fighter source byte and `counter/200`. The historical 138-byte row
  already documented a 149-byte contiguous exact span; prefer that full span.
- `advance_menu_item_wave @ 0x004258F0` is 147/147 exact. Current IDA confirms
  twelve menu entries with 20-frame staggering, a 1080-period/720-flat gate, and
  `10 - cosine(angle) * 10` for the active wave. The retained 137-byte body row
  had already been matched over the full 147-byte contiguous span including
  internal loop-alignment padding.
- Tiny `mov eax, imm32; ret` getters are a structural dead end for ranking: the
  immediate is exactly the identity-bearing field that relocation normalization
  erases. Current caller semantics instead establish `get_game_mode @ 0x0043AC40`
  reading `0x006FBD4C`, `get_match_setup @ 0x0043AC50` returning `0x006FCA48`,
  `get_game_config @ 0x0043AF10` returning `0x006FC598`, and `get_score_data @
  0x0043AF20` returning `0x006FCC98`. The GameConfig identity is independently
  closed by current SelectScenario code reading `getter()+0x64` as
  `default_game_type`; the other three getters already have accepted semantic
  callers. Replacing retained 1.06 absolute literals with extern semantic globals
  makes all four six-byte functions naturally exact.
- The source-level fighter phase bodies previously used only as LTCG tail
  destinations are themselves canonical exact: `run_active_character_and_owned_object_callbacks
  @ 0x00464630` is 323/323 and `commit_fighter_physics_status_timers @ 0x00464780`
  is 187/187. Their exact 11-byte wrappers uniquely jump to these entries, current
  bodies preserve the five-pass and three-pass semantics respectively, and fresh
  VC8 section tails establish the complete boundaries. IDA tail-chunk ownership
  is therefore not a reason to exclude a source-level authored function when the
  target entry, bounded body, and canonical bytes all agree.
- Stale historical extents are now a recurring migration failure mode, not a one-off. In addition to MT19937 and CMenuResult render/dtor, `CSelectScenario::~CSelectScenario` grows from 271 to 277 bytes and `CMenuSelect::update_player_assignment` from 400 to 406; fresh VC8 section tails and current control flow agree exactly, and no source behavior change is required. Check full section tails before treating a size delta as codegen drift.
- External source-owned vtable anchors need not be redefined just to satisfy a probe object. `_title_color_vtable_anchor` is independently established at `0x006C0624`; MenuResult/SelectScenario use an address-validated external relocation view while the defining TU retains literal validation. This preserves one semantic anchor without weakening the comparator's fail-closed import rule.
- Historical `size` can disagree with historical exact evidence itself. Fighter resets (127/219 rows vs 130/222 exact spans), `consume_counter_484_steps` (138 vs 149), and `advance_menu_item_wave` (137 vs 147) all reproduce the larger current/fresh VC8 extents. Treat old size columns as hypotheses even when the old row said matching.
- ICF/shared code can still have target-backed authored ownership through vtables. The current `CFileReader` vtable `0x006C0F34` points to scalar deleting dtor `0x0041B890` and seek `0x00407C50` alongside accepted reader methods; that class chain closes the identity even if another class also reuses the same implementation address.
- Vtable edges beat normalized clone ranking for scalar deleting destructors. Current `CMenuResult` vtable `0x006C2070` points to `0x00447890`, `CProfileMenu` vtable `0x006C2484` points to `0x0044D6E0`, and `CEffectSprite` vtable `0x006C072C` points to `0x0041F6F0`. Fresh VC8 matches those wrappers 30/30, 30/30, and 31/31. Structural candidates such as `0x006B4DE0` and `0x006A08C0` were rejected despite similar shapes.
- Profile-menu file commits now provide a cross-checked string/file lifetime reference. `commit_state_four @ 0x0044C380` is 908/908, `commit_state_five @ 0x0044C710` is 324/324, and `commit_profile_change @ 0x0044C860` is 529/529. Current `0x00429970` initializes its hidden return exactly as `MenuString28` (capacity 15, size zero, inline buffer), correcting an earlier temporary-type hypothesis. Current `0x00408C40` initializes the same 0x1C layout, but destructor visibility is caller-TU evidence: the file-commit TU needs the inline SSO teardown while exact state six retains an out-of-line temporary-dtor view. Do not globally rewrite one caller view into the other. The copy retry limit tests the full destination path length after `.dat`; the rename helper performs two independent `string_56c.c_str()` evaluations before `_mbschr`, and caching that pointer changes VC8 alias/lifetime codegen.
'
- The current game/config accessor globals are now exact-backed rather than retained magic absolutes: `g_game_mode @ 0x006FBD4C`, `g_match_setup @ 0x006FCA48`, `g_game_config @ 0x006FC598`, and `g_score_data @ 0x006FCC98`. The four six-byte getters compile exact from semantic externs and are independently consumed by accepted battle/UI paths.

- Template-family acceptance can use current vtable ownership as a batch proof when every specialization is independently target-backed. The fifteen roster `CharacterObjectManager<Fighter,FighterObject>` constructors are each 123/123 exact: current code enters the exact class-specific `TObjectManagerBase` constructor, writes the shared interface vtable plus the specialization vtable, stores the owner at `+0x64`, then calls the specialization's preallocate helper with 256. The same primary vtables' first entries point one-for-one to fifteen 30-byte scalar-deleting destructors, and the common VC8 template TU reproduces all thirty functions exactly. Do not promote a template clone merely because bytes tie; require the class-specific vtable edge.
- The same `CharacterObjectManager<Fighter,FighterObject>` family now closes its complete normal-destruction chain. Each already-exact 30-byte derived scalar wrapper directly identifies an 81-byte normal dtor; all fifteen normal dtors push one linker-folded EH handler `0x006BBE48`, but class identity comes from the scalar-wrapper edge plus the exact class-specific secondary `TObjectManagerBase` dtor. Source shape is byte-significant: a user-defined empty derived destructor emits 91 bytes because VC8 republishes both derived vptrs, while an implicit virtual destructor emits the target 81-byte body. Making the trivial `ICharacterObjectManager::~ICharacterObjectManager() {}` visible in the existing ctor TU leaves all fifteen 123-byte ctors exact and makes the implicit dtors exact. The shared base dtor/scalar are themselves 7/7 and 31/31 exact at `0x00491B90/0x00491BA0`. Prefer implicit special-member recovery when target/vtable evidence supports it; do not hand-write vptr stores to imitate compiler lifetime code.
- The narrow-character-constructor pattern generalizes beyond Alice. Twelve standard roster fighters compile to the same 126-byte `/GS` derived lifetime from ordinary `Character(init_arg)` plus class-vptr publication, `new 0x68` ObjectManager construction, and publication at Fighter `+0x658`; Iku is the same pattern plus the independently observed `-6.0f` store at `+0x138`, giving 138 bytes exactly. Every specialization is accepted only after mapping its own current RTTI vtable and its already-exact class-specific ObjectManager constructor. The thirteen newly tracked ctor EH symbols all fold to target handler `0x006BBCE3`; folded EH is only shared-shape evidence. Youmu is a deliberate exception: `0x0053B330` owns a non-trivial `+0x7D0` member lifetime and zeroes `+0x7D4..+0x7E0`, so do not shoehorn it into the standard template before identifying that member.
- Header-only lifecycle declarations can be converted into exact authored source with a deliberately narrow C++ lifetime view when current target evidence fixes the touched base/layout. `Alice_construct @ 0x004FAC40` is the reference case: current IDA proves the Character base ctor `0x00462AB0`, Alice vtable `0x006C4BDC`, `-6.0f` store at `+0x138`, allocation of a 0x68 Alice object manager via `0x004FAA40`, and publication at `+0x658`. A natural derived `Alice : Character` constructor with only those proven fields causes VC8 `/GS`/EH/new-cleanup codegen to match all 138 bytes. This is the preferred seed for the larger lifecycle/event roots; do not transcribe decompiler pseudocode into source.
- Sparse switch reconstruction must validate compiler-owned table topology as well as the function body. `Alice_handle_event_bridge @ 0x0050ED00` is the reference case: current IDA establishes 22 event labels, but the linked switch has only 19 unique destinations and a 121-byte index table at `0x0050F180`. Natural VC8 source becomes 1075/1075 exact only after preserving the observed byte return type, using real virtual slots `+0x08/+0x0C`, duplicating the semantically identical case-1/case-11 finalize code so VC8 performs its own backward cross-jump, nesting the case-2 timer condition, and leaving unknown events to the post-switch `return 1` fallback rather than writing a source `default:` label. The same COMDAT contains 198 bytes of trailing jump/index metadata; comparator `ledger_size` correctly compares the 1075-byte body while same-section DIR32 relocations preserve local table offsets. An explicit `case 10` experiment reproduced the body but created a wrong 20-slot table, so body equality alone is not enough evidence for a switch source shape.
- `PatRecord88_construct @ 0x00460F90` and `PatRecord88_destroy @ 0x0045FFD0` establish the current nested PAT lifetime layout: a 0x1C `FrameData` base, checked `vector<PatRaw16>` members at `+0x58/+0x68`, checked `vector<PatRaw16*>` at `+0x78`, guarded pointee/`+0x54` ownership, reverse member teardown, and base destruction. Natural `/GS` C++ is 300/300 exact for construction and 515/515 exact for destruction. The constructor store sequence matters semantically: current code clears base bytes `+0x04..+0x17` with a 0x14-byte memset, writes `tag_14 = 0`, then clears `+0x18`; widening the memset to 0x18 reorders the ownership-pointer store and fails canonical comparison. The same SHA-pinned nested-vector probe gives generated fingerprints for `vector<PatRaw16>::erase @ 0x0045FBD0`, assignment `0x00460AD0`, two 42-byte value-vector destructor clones (`0x0040C240/0x00435F90`), and two 90-byte pointer-vector erase clones (`0x0040DF70/0x00464B40`). The clone pairs are excluded only because the validator proves that each fingerprint hit set equals its complete manifest equivalence group; they are not excluded from STL-looking decompilation alone.
- Equivalent source shaping is acceptable when it exposes a semantic condition already proved by current target control flow rather than manufacturing a register use. `test_aabb_against_descriptor_shape @ 0x0046B7B0` is the reference: the accumulated broad-phase value is rejected exactly when bit 31 is clear. Writing that as `(static_cast<unsigned>(overlap) & 0x80000000u) == 0` keeps the same condition but makes VC8 emit the target separate AND / bit-31 TEST / signed-branch sequence; the complete function is 248/248 exact. This is distinct from forbidden register coercion or inert expressions.
- Accurate C++ layout can itself establish compiler ownership. `vector<PatRaw16>::operator= @ 0x00460AD0` is a 317-byte, inventory-unique generated COMDAT with 289 exact non-relocation bytes. `PatRecord88` copy construction at `0x00460C90` becomes an inventory-unique 311-byte implicit VC8 copy constructor only after the real layout is modeled: `FrameData` has a declared out-of-line copy ctor; +0x1C..+0x38 and +0x44/+0x46/+0x48/+0x49 are typed scalar members; +0x3A/+0x3B and +0x4A/+0x4B are natural alignment gaps rather than explicit arrays. Modeling those gaps as copyable members or letting the base copy inline changes the emitted body, so historical “copy helper” names are not authored evidence. Both current functions are now replayably `compiler_generated`.
- Character event bridges are now a target-backed recovery family rather than header-only declarations. Fourteen are canonical exact: Alice `0x0050ED00` (1075), Reimu `0x004A3220` (1061), Marisa `0x004CA980` (865), Sakuya `0x004E9B40` (843), Patchouli `0x0052FDB0` (904), Youmu `0x00544BF0` (969), Remilia `0x0055D380` (1043), Yuyuko `0x0057AB90` (986), Suika `0x005BF7D0` (1203), Udonge `0x005E1C50` (959), Komachi `0x00601D20` (1118), Aya `0x006203B0` (1177), Iku `0x0063CCB0` (1553), and Tenshi `0x00659420` (1412). Each current body was independently reconstructed around its character-specific event constants, access widths, effect/action ids, helper calls, and virtual slots before canonical comparison; historical same-size evidence was only a location seed. Yukari remains the only roster event bridge not exact.
- Unused-`this` member identity can be proved by receiver behavior across multiple current callers even when the callee body never reads ECX. `0x00434E10` has eight current call sites; sampled battle/character callers consistently put a `+0x130` event subobject in ECX, sometimes preserving it in EDI across adjacent member calls. Modeling it as `EventSubobject130::set_global_pair(float,float)` makes Iku cases 138/139 naturally reproduce the target EDI lifetime. `0x004359D0` behaves analogously as a fighter member in Tenshi: case 133 reaches it with ECX still holding `this`, while case 134 must reload ECX after intervening shared-state calls. One member declaration reproduces both sites; do not encode two contradictory ABI views merely because IDA drops an unused receiver.
- Exact-caller graph mining is a productive current-target discovery route after retained-source ranking saturates. Fourteen canonical-exact event bridges independently pointed at the same review-pending `SharedEventState` helper quartet: `0x00427290/0x004272B0/0x004272D0/0x004272F0`. Consolidating their repeated local declarations into one tracked narrow state view proves floats at +0/+4/+8, mirrors at +0x14/+0x18/+0x1C, targets at +0x20/+0x24/+0x28, and enable bytes at +0x2C..+0x2E. Natural VC8 then reproduces the four current bodies 17/17, 17/17, 17/17, and 33/33, while all fourteen caller units remain exact. Use exact-caller convergence to prioritize helpers, but still require an independent canonical comparison before promotion.
- Translation-unit visibility can itself be current-target evidence. `set_shared_second_target @ 0x00476AD0` forwards `(char,float)` to `SharedEventState::set_target_24_enabled_2d @ 0x004272B0` with an unused receiver. When the setter definition is visible in the same probe TU, VC8 `/O2` inlines it and emits direct DIR32 accesses to `g_shared_event_state+0x24/+0x2D`, which does not match the target. Putting the truthful wrapper in its own TU leaves only the declaration visible and naturally emits the target global receiver load plus out-of-line call, giving 26/26 exact. Treat this as positive TU-boundary evidence, not permission to disable optimization or force noinline.
- `EventSubobject130::set_global_pair @ 0x00434E10` closes another unused-receiver helper through exact-backed global identity. Current callers preserve the Fighter `+0x130` member receiver, but the 34-byte body intentionally ignores ECX: it captures exact-backed battle object manager `0x006FBCA0` as the return value, stores the first float at manager `+0x10C`, reloads the same shared pointer for the second float at `+0x110`, and returns the originally captured manager. Expressing that real return-value lifetime makes VC8 reproduce 34/34 without register forcing.
- Source-level temporary lifetime can be part of exact VC8 stack-frame recovery without becoming padding. Iku has two semantically distinct three-float spawn parameter arrays, one used in case 8 and one in case 134. Declaring both before the switch keeps them simultaneously live and naturally produces the target `sub esp,0x18`; block-local arrays let VC8 reuse one 0x0C slot and fail exactness. This is a real lifetime distinction, not a fake local.
- Yukari event case 156 is a current compiler-shaping blocker, not a license for dead code. Natural source reproduces every preceding case and reaches the final condition with the correct `state==794 && sequence!=1` semantics, but standalone VC8 removes the target's redundant `cmp ax,ax; jne` before the default return. Adding a tautology solely to recreate that dead test would violate the repository's no-inert-code rule, so `Yukari_handle_event_bridge @ 0x005983B0` remains review-pending.
- Event-bridge source shape is not globally uniform even when the Fighter-facing ABI is shared. Sakuya/Patchouli/Youmu/Udonge use direct shared-state cases without the dynamic +0x730 countdown; Reimu/Alice/Remilia/Komachi/Suika/Aya use the countdown plus current second-target member helper `0x00476AD0`; Yuyuko is naturally expressible with a result-and-break switch even though VC8 folds the machine code into direct returns. Promote opaque bytes into typed fields only when current access width requires it: Suika proves a signed short sequence/state value at `+0x13E`, a word at `+0x734`, and a float at `+0x740`; Aya independently proves the signed-short `+0x13E` gate. Do not impose one character's typed extension on another character just because the offsets are nearby.
- When target code shares a long event-case suffix by jumping one case back into another, do not mechanically encode that jump in source. Sakuya/Marisa cases 9 and 50 are the reference: writing the truthful full motion suffix independently in both cases lets VC8 naturally tail-merge the duplicate suffix and emits the target `push alternate_action; jmp common_tail`. An explicit source `goto common_tail` produced the wrong basic-block order and failed exactness. Prefer duplicated semantic source when both branches really perform the same work, and let the compiler own tail merging.
- An unused C++ receiver can make IDA infer a free/stdcall-looking prototype even when the source-level ABI is a member function. The spell CSV parsers are the reference case: wrappers `0x00434300` and `0x00434A10`, plus independent current caller `0x00434AC0`, all place the owning `SpellDataOwner*` in ECX immediately before parser calls `0x00433B30/0x004343A0`. Declaring/defining those parser bodies as `SpellDataOwner` members (their bodies need not read `this`) makes both wrappers naturally grow by exactly two bytes and compare 148/148 and 161/161. Repeated receiver-setting call sites plus canonical wrapper bytes outweigh a decompiler prototype that discarded an unused ECX.
- Raw exactness can identify compiler-generated code rather than authored code. `0x0046EA60` was initially a 105/105 zero-relocation candidate for retained `UiDesignTree::find`, but current initialization fills its receiver tree with `data/bgm/st%02d.ogg` records, disproving that authored identity. A tracked pinned-VC8 `std::map<unsigned, void*>::find` probe reproduces the 105-byte COMDAT raw-exact and inventory-uniquely, so the address is now `compiler_generated` instead of an authored match.
- Broadly shared list-node allocation is compiler-generated even when retained authored source can reproduce the same bytes. A tracked pinned-VC8 `std::list<void*>::_Buynode` probe emits a 51-byte COMDAT with only the `operator new` REL32 field variable; masking that field produces exactly two hits across all 4,004 tracked candidates, `0x004129E0` and `0x00421F30`. Both have wide cross-subsystem/container caller sets. The manifest declares those two addresses as one equivalence group, so origin replay fails unless the complete inventory hit set is exactly that pair. This closes the long-standing `create_collision_list_node @ 0x00421F30` review without falsely counting a generic STL node helper as authored.
- Authored callers do not make a shared teardown helper authored. Fourteen exact `TObjectManagerBase` destructors call `0x00435EB0`, which retained semantics could tempt us to name `CollisionList::~CollisionList`; current bytes instead match the SHA-pinned VC8 `std::list<void*>::_Tidy` COMDAT all 72/72. The real generated `std::list<void*>::~list` emitted by the same probe is only a five-byte tail jump into `_Tidy`. Masking `_Tidy`'s two operator-delete REL32 fields leaves 64/72 exact bytes and produces exactly two current-inventory hits, `0x004023E0` and `0x00435EB0`; `0x004023E0` is independently used as a list-like member teardown inside `CHandleManager<CWaveBuffer*>`. The manifest therefore excludes the pair only as one complete equivalence group. Always test a reusable STL-looking callee against generated COMDATs before promoting a semantic alias from authored callers.
- Six current `SpellRecordView` tree helpers are compiler-generated rather than authored parser methods: `_Lrotate @ 0x0042C9E0`, `_Max @ 0x0042CA30`, `_Rrotate @ 0x0042CA70`, `const_iterator::_Dec @ 0x004329B0`, `find @ 0x00432C40`, and `insert @ 0x00433890`. Tracked `scripts/probes/spell_tree.cpp` under the SHA-pinned VC8 SP1 compiler regenerates the six `std::_Tree` COMDATs (615 bytes total); `config/vc8-generated-spell-tree-origin-anchors.toml` requires boundary/form checks, minimum exact non-relocation coverage, and a unique full-inventory relocation-masked fingerprint for every anchor. Keep these out of the authored denominator even though their current callers sit inside the authored spell parser/lookup cluster.
- Profile UI infrastructure is now one exact-backed family rather than scattered globals. `Menu::initialize_profile_menu @ 0x0043FBF0` proves a real `MenuCursorState` aggregate at `0x006FCFFC`: item_count/page_size/input_counter/selection/window_start at +0/+4/+8/+0xC/+0x10. Source statement order is byte-significant: `ProfileNameList::item_count()` must stay live while input/cursor/window fields are initialized, then item_count is published; writing it immediately shortens the body from the target 77 to 75 bytes. The same TU makes `hide_profile_message @ 0x0043F8D0` (48), `draw_profile_overlay @ 0x0043F900` (97), `render_profile_menu_footer @ 0x0043F970` (83), `render_menu_list @ 0x0043FA10` (215), `Menu::render_profile_player_slot @ 0x0043FAF0` (103), and `Menu::render_profile_tiles @ 0x0043FB60` (142) canonical exact. In `render_menu_list`, the unsigned row index must convert to `float` before participating in double-precision spacing math; this naturally selects the target single-precision `2^32` correction rather than the wrong qword conversion path. `render_profile_player_slot` is a caller-proven unused-this Menu member, and `render_profile_tiles` reuses the existing shared count-byte identity at `0x006FA88E` rather than inventing a duplicate global.
- Common profile messaging/selection is owned by `Menu`, not separate Replay/Profile facades. `set_texture @ 0x00406A60`, `show_profile_result @ 0x0043F810`, `show_profile_message @ 0x0043F640`, and `select_profile @ 0x00440410` are exact 39/93/160/224. A four-byte class value return from `TitleResourceManager::create_text_texture` reproduces the target hidden-sret call naturally; an explicit output local grows the result wrapper by two bytes. The message cursor is `0x006FD018` while the profile-list cursor is `0x006FCFFC`. The selection helper is exact only when the first `MenuString28::assign` return is preserved as a real reference into the second assign. `update_profile_message` remains 294/298 on final-return tail merging.
- `CProfileMenu::commit_state_one @ 0x0044C110` is 621/621 exact under `/GS`. A real local `ProfileMenuBaseData` provides the target ctor/dtor/EH lifetime; current `0x00432050` is its initialization member and `0x004317A0` is the save-to-profile member (current body is a CreateFileA/WriteFile pipeline). The same `MenuString28` hidden-return/destructor-visibility contract recovered for the copy/delete/rename helpers also reproduces success/failure suffix messages here. Keep this member in the existing ProfileMenu file-commit TU; do not flatten the local object into a byte buffer or hand-code cleanup.
- `ProfileMenuBaseData` is no longer an opaque 0x33C blob. Current `initialize_defaults @ 0x00432050`, `save_to_profile @ 0x004317A0`, and exact normalization establish `std::string @ +0x98/+0xB4`, flag `+0x13C`, two 0x34 serialized blocks at `+0x140/+0x174`, flag `+0x1A8`, and twenty checked `std::deque<unsigned short>` objects starting at `+0x1AC`. `normalize_profile_name @ 0x00431690` is 145/145 exact only when `.dat` removal uses the **source** `string_098.size()` before multibyte truncation of the copied `string_0b4`; using the destination size is semantically equivalent at that point but changes target dataflow. `initialize_defaults` remains 334/334-or-335 under all twenty legal memset/field-order topologies tested, with only compiler scheduling left; `save_to_profile` naturally reproduces its `/GS`/EH, local `CFileWriter`, four header writes, and twenty-deck serialization but remains 754/784. Keep both pending rather than manufacture dependencies or register lifetimes.
- Structural clone ties need class/caller evidence, not old address order. The
  367-byte Yukari mirrored-command gate at `0x0058C170` was accepted only after
  its sole current fighter dispatcher, nearby exact Yukari anchors, and all five
  current REL32 destinations established the specialization; it then compared
  367/367 exact.
- Source-owned vtable anchor data must migrate with the current class identity.
  The title/UI color anchor now points at current `IColor` scalar-deleting
  destructor `0x0040AE80`; that semantic correction makes the `/GS` CTitle
  destructor compare 161/161 exact.
- Per-character wave-cache loading is now current-target exact-backed. `load_indexed_character_wave_resources @ 0x0045F680` loops exactly 64 slots, derives the character directory from Fighter `record_key_330` through `0x0043BF80`, formats the source-owned `data/se/%s/%03d.wav` literal at `0x006C2B20`, calls the verified wave-service object `0x00702288` via `0x00401AF0`, and publishes each returned handle through Fighter `+0x164`. Retained VC8 `/GS` source had zero non-relocation differences; only current relocation identities were reconciled before the 132/132 canonical match. Reuse these service/string identities for later fighter resource-loading recovery rather than applying old-version address deltas.
- UI selection tracking is now an exact-backed shared contract rather than duplicated extern declarations. `set_ui_selection_state_tracking @ 0x0043FFB0` is 88/88 exact: enabled mode snapshots the low byte of `g_ui_selection_state @ 0x006FD290` into `g_ui_selection_snapshot @ 0x006FD046` and clears `g_ui_selection_fade @ 0x006FD047`; disabled mode clears the snapshot, sets fade to `0xFF`, and, when state is nonzero, performs VC8 checked `std::list<UiSelectionMenu*>::back()` through `g_ui_selection_menu_stack @ 0x006FD288` before tail-dispatching vslot `+0x04`. The companion `has_ui_selection_state_changed @ 0x0043B6E0` is 24/24 exact from the truthful unsigned `state - snapshot > 0` predicate. Keep the state as a dword and the snapshot as a byte: widening the snapshot would erase the target `movzx` and change the subtraction contract.
- Loader-zero `.bss` globals are valid canonical relocation identities even when no initialized raw bytes exist in the PE. The four UI-selection globals above live in the image virtual tail; their initial zero state is supplied by the loader. For a checked STL object, preserve relocation addends: the list object is based at `0x006FD288`, while target code reads its sentinel pointer at base `+4` (`0x006FD28C`). Record the base symbol plus addend instead of inventing a second absolute global for the sentinel.
- `character_key_to_name @ 0x0043BF80` closes the identity behind those resource/spell callers without inventing a second name table. Exact-backed `g_scenario_event_name_map @ 0x006FCF70` is already a checked `std::map<int,String28>` used by 209/209 exact `resolve_scenario_event_name`; the forward accessor is simply `find(character_key)->second.c_str()`. VC8 naturally emits the target checked-iterator owner/end validation plus the 16-byte `String28` SSO threshold, giving 78/78 exact. The same tracked compile emits `std::map<int,String28>::find @ 0x006A4D60` as a 105-byte raw-exact body with exactly one current-inventory hit, which independently classifies that callee compiler-generated. One shared container contract can therefore close both an authored accessor and its generated specialization; keep those ownership claims separate.
- `try_dispatch_flagged_gate_actions_220_224 @ 0x00493B40` shows when an explicit source control-flow edge is legitimate. Current 1.06a disassembly independently proves three success tails: action 222 has a private consume-and-return path, action 220 has a second counter/return path, and actions 223/224 reuse the latter. The retained source was already semantically correct and differed only in four internal short-branch displacement bytes. Preserving the observed positive-path success node in ordinary C++ makes VC8 emit all 258 target bytes exactly. This is not a license for arbitrary gotos: the edge must already exist in target control flow and must not introduce dead work, padding, fabricated locals, or register coercion.
- `process_fighter_transient_status` at `0x0045E3D0` demonstrates acceptable
  natural source shaping: an equivalent C++ conditional expression for the
  32-byte normalization loop restored the target VC8 schedule and all 301 bytes
  compare exactly. By contrast, shared-tail/LTCG register-allocation blockers
  remain blocked rather than being forced with assembly or register tricks.

- `PatGroup` at `0x00461520/0x00461590` is now a concrete C++ lifetime anchor, not a raw 0x20-byte view. Its first 0x10 bytes are VC8 checked `std::vector<PatRecord88>` storage; both authored ctor and dtor explicitly call `records.clear()`, and natural `/GS` source compares 106/106 and 128/128 exact. Four adjacent template helpers are now replayably `compiler_generated`: `vector::_Tidy @ 0x0045FC30`, `vector::erase @ 0x00461320`, the 249-byte vector copy constructor at `0x00461420`, and `std::_Construct<PatGroup> @ 0x00461940`. The first two come directly from tracked `PatGroup.cpp`; the latter two come from tracked `scripts/probes/pat_vector_generated.cpp`. The copy-constructor COMDAT is 250 bytes only because of one terminal `0xCC` alignment byte, while the target body is 249 bytes followed by target `0xCC` padding; the origin validator permits only that manifest-bounded tail and forbids relocations in it.
- All fifteen current roster `TObjectManagerBase<ClassObject,CharacterObject>` normal destructors are now 100/100 exact from one natural template lifetime, and all fifteen vtable-owned scalar deleting wrappers are 30/30 exact. Their identity is not address-order inference: each current body restores its class-specific template vtable, calls shared `release_all_tracked_objects`, destroys the same non-throwing `CollisionList`, then calls the class-specific `CHandleManagerEx<ClassObject>` destructor. The previous Yuyuko exception was stale: vtable `0x006C560C` owns scalar wrapper `0x0056DB80`, which directly calls normal dtor `0x0056DB10`; that dtor independently reaches shared release `0x0056DA90`, generated list teardown `0x00435EB0`, and exact Yuyuko handle-manager dtor `0x0056D2A0`, and is canonical 100/100.

- `CriticalSectionWrapper` is now recovered as the target's polymorphic `CCriticalSection` lifetime rather than a trivial struct with a synthetic leading state dword. The compiler-owned vptr occupies `+0`, `Win32CriticalSection24` begins at `+4`, and `sizeof==0x1c`; natural VC8 gives ctor `0x0040ABC0` 23/23, dtor `0x0040AC10` 17/17, scalar deleting wrapper `0x0040ABE0` 41/41, while existing enter/leave remain 11/11. Prefer a real polymorphic member lifetime when target ctor/dtor/vtable evidence closes the layout; do not preserve a fake data field merely because it once made offsets convenient.
- The `CHandleManagerEx<T>` lifetime family now covers twenty class-disambiguated specializations / 60 exact functions / 8,460 bytes from one natural template TU. The original sixteen roster/Info specializations are joined by `CSelectObject`, `WeatherEffectObject`, `EffectObject`, and `SystemEffectObject`; every specialization has an independently attested current vtable whose first slot owns its 30-byte scalar deleting wrapper, and the enclosing manager-base ctor/dtor edges close the class identity. Each ctor is 130 bytes and each normal dtor 263. Yuyuko remains a useful warning: its parent dtor edge was absent, so its identity was recovered from vtable `0x006C55BC` and the independent 263-byte dtor/30-byte wrapper references rather than address order. Batch template code only after every specialization has its own target-backed class edge or vtable proof.
- Two source-order details are byte-significant in `CHandleManagerEx<T>`. `generation_counter_30 = 0` belongs in the constructor body because current code performs the store only after the `CCriticalSection` member ctor returns; an initializer changes scheduling. In the destructor, read `ObjectType *object = slots_04[index]` once before the null check and virtual delete. Writing `if (slots[index]) delete slots[index]` makes VC8 emit a second checked `operator[]` path, growing the body from the target 263 bytes to 294; the local is target-proved dataflow, not artificial padding.
- Linker-folded EH handlers are not class identity. All sixteen `CHandleManagerEx<T>` ctors push the same target handler `0x006BC551`, and all sixteen dtors push `0x006BBB3C`. That proves shared EH shape only. Class identity remains the specialization-specific vtable and its independently recovered owner/caller edge.
- The neighboring fifteen-member roster pool `acquire` family remains deliberately review-pending. Current `/GS` `RosterObjectPools.cpp` emits 514-byte COMDATs while every target candidate is 519 bytes. The first real divergence is in the reuse branch: target keeps the packed handle-output pointer live in EBX across checked-list erase, forcing the iterator node through a stack slot; fresh standalone VC8 reloads the handle argument and keeps the iterator node in EBX, saving five bytes. Named iterator locals, `pop_back()`, struct references, branch-local handle views, and a typed 4-byte handle-pointer ABI all still emit 514. This is now a well-localized lifetime/allocation blocker; do not manufacture EBX liveness with volatile, dummy uses, register variables, inline assembly, or padding.
- Use one writable reconstruction session. Claims remain header-only.
- Use IDA Pro exclusively after exact attestation. There is no Ghidra fallback.
- Keep IDA client scripts independent of an interactive Python environment:
  they pin `mcp==1.26.0` and self-bootstrap it through `uv --no-project` when
  the selected interpreter does not provide it. This covers systemd/GPT-web
  login shells without modifying global Python packages.

- A generated template-helper fingerprint does not necessarily prove the full source container schema. The pinned VC8 witness for `std::map<unsigned,void*>::find @ 0x0046EA60` correctly proves compiler ownership of the key-only `find` COMDAT, but current initializer `0x00438F50` and exact authored `publish_transition_effect_at_46e130 @ 0x0046EB00` prove the live container at `0x006FBD2C` is actually `std::map<unsigned,unsigned short>`: path hashes map to 16-bit effect/BGM ids and the authored caller consumes `iterator->second`. Use generated fingerprints for ownership/code shape; recover mapped/value types from current data flow and layout evidence.
- Translation-unit visibility can be positive target evidence even when both functions are independently exact. `InfoManagerResourceView::initialize_story_info_resources_471e50 @ 0x00471E50` calls exact base loader `0x0046E960`. Defining both in one probe TU makes VC8 inline the 33-byte loader; keeping the Story override in its own TU with only the declaration visible preserves the linked call and gives exact 97/97. Do not substitute `noinline` when a truthful TU boundary explains the target.
- The InfoManager resource wave cross-validates a narrow current layout: effect manager `+0x04`, design resources `+0x98/+0xCC`, combo resource `+0x100`, two 0x34 records at `+0x16C`, two 0x154 records at `+0x1D4`, bound inputs `+0x47C`, runtime tick `+0x484`, scalar `+0x488`, bound design entries `+0x48C/+0x490`, resource state `+0x494`, and two 0xF8 records at `+0x498`. Exact load/release/Story methods exercise the resource and 0xF8 portions; current ctor/dtor/runtime methods independently establish the remaining offsets.
- `TObjectManagerBase<InfoEffectObject,EffectObjectBase>` is another current-target exact specialization of the existing natural manager-base template: ctor `0x0046EE40` is 103/103, dtor `0x0046F020` is 100/100, and the vtable-owned scalar deleting wrapper `0x0046F4E0` is 30/30. Current RTTI for `InfoEffectObject` independently places `CObjectBase` at `+0x158`, `AnimationObjectBase` at `+0x04`, and `Environment` at `+0x130`; current RTTI plus the pool/spawn consumers reproduce `sizeof(EffectObjectBase)==0x160` and correct `sizeof(InfoEffectObject)==0x178`; the earlier 0x170 probe omitted the target-proved +0x160/+0x164/+0x16C tail. The 59-byte object constructor remains blocked by the shared receiver-hoist scheduling difference and is not promoted.
- Same-size or near-same-size semantic completion is still not exactness. `0x0046E5A0`, `0x0046EB90`, `0x0046EF40`, `0x0046F370`, and `0x0046F3D0` all have current semantics/layout closed far enough to reproduce their core control flow, and some fresh objects have the exact target length, but standalone VC8 selects different callee-saved registers or one spill. Leave them review-pending rather than adding volatile/register coercion, fake locals, assembly, or manual vtable evaluation.

- The effect/UI manager template wave adds 39 authored canonical-exact functions / 4,541 bytes. Four new `CHandleManagerEx<T>` specializations contribute 12 functions, four `TObjectManagerBase<T,...>` specializations contribute 12, the CSelect/Weather/Effect spawn family contributes 3, and four full `CEffectManager<T>` lifetimes contribute 12. Current RTTI names exactly four `CEffectManager` instantiations (`CSelectObject`, `WeatherEffectObject`, `EffectObject`, `InfoEffectObject`); class-specific vtables and enclosing manager edges disambiguate every accepted address before canonical comparison.
- `CEffectManager<T>` is a current-target multiple-inheritance layout, not a flat facade: `IEffectManager` is primary at `+0`, `TObjectManagerBase<T,EffectObjectBase>` is secondary at `+4`, then target-backed containers occupy `+0x64` (16-byte checked vector), `+0x74` (20-byte owning deque), and `+0x88` (12-byte checked tree), for `sizeof==0x94`. Natural VC8 emits exact 169-byte ctors, 194-byte dtors, and 30-byte scalar deleting wrappers for all four current instantiations.
- Translation-unit visibility is byte-significant inside this MI lifetime. Leaving the trivial `IEffectManager::~IEffectManager()` only declared makes standalone VC8 emit a 203-byte `CEffectManager<T>` dtor with an out-of-line base-dtor call. Making the truthful empty base destructor visible lets VC8 inline the target final `IEffectManager` vptr restore and produces the exact 194-byte body, nine bytes shorter without attributes or fake work.
- A derived-to-secondary-base pointer conversion can inject source semantics absent from the target. In `CEffectManager<T>::spawn_effect`, storing `TObjectManagerBase<T,...> *base = this` makes VC8 preserve null during the conversion (`test/jz`) even though a member-function `this` is already non-null. Calling the qualified base member directly, `TObjectManagerBase<T,...>::acquire_and_link_object()`, naturally emits the target `lea ecx,[this+4]` and keeps all three spawn members 115/115 exact. Prefer a truthful qualified base call over volatile/register tricks or manual pointer arithmetic.
- The shared `TObjectManagerBase` tracked-list wave closes **38 authored functions / 4,009 bytes** across nineteen specializations: fifteen roster object managers and the CSelect/Weather/Effect/InfoEffect manager bases. Every `acquire_and_link_object` is 94/94 and every `preallocate_object_pool` is 117/117. The missing source abstraction was not a register hint: pool acquire plus handle-token publication is one inlinable object-acquire operation, followed by a real checked `std::list<BaseObject *>::push_back`. Expressing the list ownership at that level makes ordinary VC8 `/O2` naturally choose the target persistent `this`/list lifetimes (`EDI` for the 94-byte body and `EBP`/`EBX` for the 117-byte loop). A normal TU-local `static` helper is enough; `__forceinline`, volatile state, register variables, fake dependencies, and assembly are unnecessary.
- The roster family publishes its handle at object `+0x334` and appends `CharacterObject *`; the four effect/UI specializations publish at `+0x15C` and append `EffectObjectBase *`. Current class-specific ObjectManager constructors/CEffectManager callers and class-specific handle-manager acquire edges disambiguate every specialization before canonical comparison. Sakuya also corrects a historical ABI overclaim: its exact constructor calls `0x004DF1B0` and immediately overwrites EAX, so `preallocate_object_pool` is a `void` member; the old pointer return was incidental residue. The shared release helpers `0x0056DA90`, `0x0046EEB0`, and SystemEffect-specific `0x00436E60` remain separate pending problems; do not infer that solving list append also solves list teardown.


- The SystemEffect/sprite wave closes a real inheritance/layout chain rather than a padding view. Current `CSpriteBase` copy construction at `0x004396A0` copies `+0x08..+0x7F` as 0x78 bytes after the `IColor` prefix, proving `sizeof(CSpriteBase)==0x80`; the four color fields at `+0x18/+0x34/+0x50/+0x6C` are the color dwords of four 0x1C vertices `{x,y,z,rhw,color,u,v}`. Current `CSpriteEx` copy construction at `0x00450640` then proves `sizeof(CSpriteEx)==0xE8`, with four 12-byte source xyz points at `+0x80` and four working xyz points at `+0xB0`. `CEffectSprite` begins its own observed fields at `+0xE8` and remains 0x12C. This layout makes the IColor dtor/scalar `0x0040AD50/0x0040AE80`, color virtuals `0x0040AC50/0x0040AC70/0x0040AC90`, and CSpriteEx transforms `0x004073A0/0x004073E0/0x004074E0/0x00407580` canonical exact from ordinary C++.
- Packed color multiplication at `0x0040AC90` is a source-type lesson: each RGBA component is an `unsigned char`, but ordinary C++ integer promotion makes `byte * byte / 255` operate as signed `int`, naturally producing VC8's `0x80808081` signed divide-by-255 lowering. Casting operands to `unsigned int` emits a different shorter division sequence and is wrong. The target also writes the packed result to the four vertex colors in order 3->0; preserve observed store order when it is semantically neutral rather than inventing register constraints.
- `SystemEffectObjectBase/SystemEffectObject` is independently fixed by current RTTI and allocation. The base has `CObjectBase +0x04`, `AnimationObjectBase +0x0C`, resource handle `+0x138`, and size 0x13C; the derived object adds mode/timer at `+0x13C/+0x140` for size 0x144. Derived vtable `0x006C2860` owns `update_state @ 0x00660A00` and `reset_state @ 0x00660B00`, while base vtable `0x006C278C` owns `render_effect @ 0x00473CF0` and the shared scalar lifetime. The alpha state machine operates on the high byte of `CEffectSprite::color_10c`, explaining the observed `this+0x11B` byte accesses.
- Two natural source-shape details are byte-significant for SystemEffect. Making the truthful empty `IColor` and `CEffectSprite` destructors visible lets VC8 inline the trivial sprite lifetime and shrinks `SystemEffectObjectBase::~` from 116 to the exact 107 bytes without attributes. For `reset_state`, spelling cases 0, 1 and 2 separately with the same real stores lets VC8 tail-merge to the target 35-byte arithmetic dispatch; grouping the labels emits a 24-byte range check instead. For `update_state`, source switch order 2->1->0 makes VC8 retain the target case-2 shared `alpha += 3` tail and gives 248/248; order 0->1->2 keeps the opposite copy and emits 244 bytes.
- CSpriteEx transform source order can carry real x87 topology. `reset_transform @ 0x004073A0` is exactly a 0x30 aggregate copy and naturally lowers to twelve `rep movsd` dwords. `translate @ 0x004073E0` becomes 189/189 only when the twelve ordinary assignments preserve the target-observed operand order (`arg + field` versus `field + arg`) instead of normalizing every addition through one loop; the x/y axis scale loops remain direct 91/91 matches. This is source-level floating-point evaluation order, not dummy work or register forcing.
- The adjacent CSpriteEx transform surface is now substantially closed. Current padding proves a missed independent `commit_transform @ 0x004073C0` boundary; reverse `memcpy(working -> source, 0x30)` is 24/24. Origin-scale overloads `scale_x(float) @ 0x004074A0`, `scale_y(float) @ 0x00407540`, and `scale_z(float) @ 0x004075E0` are each 61/61, while `scale_z(float,pivot) @ 0x00407620` is 91/91. For the one-argument scales, ordinary C++ multiplication is mathematically commutative but VC8 x87 evaluation order is byte-visible: points 0/3 require `scale * field`, points 1/2 require `field * scale`. Preserve the observed expression order rather than introducing volatile state or register coercion.
- The same investigation closes a typed background-render pipeline instead of isolated helper shapes. `BattleBackgroundLayout::place_sprite @ 0x00427200` is 128/128 on the exact-backed global object at `0x006FBCC0`; current callers and target math fix scale `+0x14`, edge fields `+0x18/+0x1C/+0x30/+0x34`, and the 420/640/320 placement constants before exact CSpriteEx translate/X/Y origin-scale calls. `EventEffectStateView` at exact-backed receiver `0x006FBCAC` contains checked `std::deque<BackgroundRenderEntry> @ +0x28` where each entry is exactly eight bytes `{alpha, pad[3], CSpriteEx*}`, plus a polymorphic emitter at `+0x48`. Natural checked-deque iteration, render-state setup, three 3.0 origin scales, -100 Y translation, the exact placement helper, virtual color update, CSpriteEx final render, and null-entry full-screen fallback reproduce `render_background_effects @ 0x004696E0` all 511/511. The same +0x48 ownership makes `EventEffectStateView::emit_effect @ 0x004695B0` 49/49.
- Three tempting neighbors are deliberate non-acceptances from this wave. `CSpriteEx` three-axis pivot rotation `0x00407680` has closed 1095-byte semantics but fresh source is 1089 bytes: target preserves each angle on x87 with `fld angle; fld st; fldz; fucompp`, while ordinary `if(angle)`, `angle != 0`, `0 != angle`, float-zero, and local-zero spellings settle into different 2-byte-shorter compare topologies at all three axes. `EventEffectStateView::dispatch_emitter_state @ 0x004695F0` is target 11 versus fresh 10 solely because target loads the +0x48 vptr before adjusting ECX and fresh VC8 adjusts ECX first. `RenderModeManager::set_blend_mode @ 0x004047E0` is a 256-byte old/new-mode transition-key jump table with no source-backed reconstruction yet. Do not close any of these with inline assembly, volatile/dummy liveness, manual vtable loads, or copied bytes.
- Small forwarding/lifetime bodies remain useful only when ownership is independently established. `RenderModeManager::set_sampler_state @ 0x00404730` is 32/32 from the already-proven D3D9 SetSamplerState vslot and current background-render callsites. `SharedEventStateForwarder` now has all three isolated 26-byte wrappers for targets +0x20/+0x24/+0x28 exact; keeping the setter definitions out of that TU is real source visibility evidence because otherwise VC8 inlines them. Five 30-byte scalar-deleting wrappers (`CSelectScenario`, `CTitle`, `CFileList`, `CMenuSelect`, `PatRecord88`) are accepted only because each current class vtable owns the wrapper and its same-TU normal destructor is already canonical exact; raw wrapper shape by itself is not authored identity.
- Several adjacent SystemEffect/sprite methods remain intentionally pending despite closed semantics. System manager `spawn @ 0x00458340` has the target 125-byte length once the texture API's out-token and returned loaded handle are modeled separately, but fresh VC8 uses a 12-byte frame instead of target 8 because it does not coalesce the dead token/path slot. `TObjectManagerBase<SystemEffectObject,...>::acquire_and_link @ 0x00457110` is 91 bytes with only a persistent-this register permutation, and `CHandleManagerEx<SystemEffectObject>::acquire @ 0x00456B70` is now narrowed to **498/499 with the same 147-instruction topology** after recovering a real `SystemEffectObjectBase()` initialization of `resource_handle_138 = 0`; target/fresh still differ only in the EBP/EDI allocation role and one-byte addressing length. `CSpriteEx::finalize_render @ 0x00407AD0` is semantically closed through resource binding plus D3D9 `SetFVF(324)`/`DrawPrimitiveUP(TRIANGLESTRIP,2,...,28)`, but target keeps a single-precision 0.5 literal on the x87 stack while fresh VC8 pools a double. Keep all of these review-pending; do not use parameter-slot aliasing, volatile constants, manual vtable arithmetic, or register coercion.
- `SystemEffectManager<SystemEffectObject>` itself is a zero-extra-state facade, not a missing authored lifecycle body. Current RTTI locates its vtable at `0x006C288C`; `CScenarioData` construction calls the exact `TObjectManagerBase<SystemEffectObject,SystemEffectObjectBase>` ctor on the embedded subobject and only overwrites the vptr, while the manager vtable's first slot ICFs to the exact base scalar wrapper `0x004583C0`. Do not manufacture a standalone derived ctor/dtor merely because the RTTI type exists.

- Current RTTI `CDesignBase` and the retained `TitleDesignResource` facade refer to the same 0x34 UI design-resource object. Exact ctor/dtor/scalar at `0x004217F0/0x00421890/0x00421930` fix the lifetime layout as an IColor-like 0x08 primary base, a target-observed 0x0C `{begin,end,capacity}` handle owner at +0x08, checked `std::list<UiDesignObject*>` at +0x14, checked `std::map<unsigned,UiDesignObject*>` at +0x20, and an 8-byte checked list iterator at +0x2C. The existing `Title.hpp` facade intentionally remains opaque so already-exact UI TUs do not acquire unrelated STL/TU visibility; `DesignBaseLifetime.hpp` is the durable lifetime/layout contract.
- The 0x0C CDesignBase handle owner must not be replaced with the probe profile's checked `std::vector<unsigned>`, which is 0x10 bytes. A genuine three-pointer owner with real allocation teardown reproduces the target while preserving the 0x34 object size. The normal destructor is an implicit virtual special member: writing an explicit empty derived destructor makes VC8 republish the CDesignBase vptr at entry, which the target does not do. This repeats the broader rule that implicit lifetime source shape can be byte-significant and is preferable to manual vptr stores.
- `CMenuContinue` and `CMenuEnd` are independently RTTI/vtable-owned Menu-derived lifetimes whose **visible member prefix** is closed through 0x40: state byte +0x04, aligned CDesignBase +0x08, owner pointer +0x3C. Continue ctor/dtor/scalar are exact 160/114/30 and End is 157/120/30. New current allocation evidence corrects the earlier full-size claim for `CMenuEnd`: every observed scalar `new` site before ctor `0x00445100` allocates **0x44** bytes, while ctor/dtor/update only expose members through +0x3C. Do not invent a `reserved_40` member merely to force `new`; recover the real trailing ownership first. Current End callers also reinforce an unused-this member ABI for the setup/message wrappers.
- Current RTTI names the shared file-list base `CFileList`; `CMenuReplay` member boundaries close the base at **0xB4**, while `ResultList` adds its character id at derived +0xB4 and is therefore 0xB8. Canonical exact ctor/dtor `0x00439F40/0x00439FC0` fix the base layout as checked `std::deque<std::string>` +0x04, `std::deque<unsigned char>` directory flags +0x18, `std::deque<unsigned>` handles +0x2C, `std::deque<FileListTile>` +0x40, three `std::string` values +0x54/+0x70/+0x8C, and tail state +0xA8/+0xAC/+0xB0. Exact `item_count/find_row/contains/trim_item_text/row_at/render_item/row_is_directory/enter_directory/format_item/shutdown/populate` at `0x004395E0/0x00439750/0x00439810/0x00439910/0x004399A0/0x00439A20/0x00439A60/0x00439A80/0x00439B20/0x0043A0A0/0x0043A150` independently exercise those members. `populate` is 1088/1088: its directory-mode path string must be default-constructed and then assigned, and that branch-local string must destruct before the common virtual `finalize()` tail. Direct construction shortens the body; calling `finalize()` inside the branch moves destruction after the virtual call.
- Binary `CProfileList : CFileList` remains a 0xB4 derivative: ctor `0x00431730` is 103/103 and slot-1 populate override `0x004313F0` is 29/29. The ctor uses target `profile/*.dat` through an out-of-line String28/std::string assign ABI; exposing the full inline VC8 string implementation in that caller TU expands the constructor to 240 bytes, so TU visibility is source evidence. The inherited row deque being empty triggers default-profile creation followed by a tail-call to base populate.
- Correcting the retained ResultList facade to a virtual base destructor makes implicit `ResultList::~ResultList @ 0x00447260` 55/55 exact without changing its 128-byte ctor or the exact CMenuResult render/update/destructor users. The binary ResultList vtable first slot is its scalar-deleting wrapper; the normal derived destructor must not publish a fake derived vptr at entry.
- CFileList checked-container spellings are byte evidence. `shutdown @ 0x0043A0A0` is 166/166 only with `handles_02c[i]`; `.at(i)` adds the `_Xran` path and grows to 177. `row_is_directory @ 0x00439A60` proves +0x18 is a one-byte deque rather than `deque<int>`, and `enter_directory @ 0x00439A80` naturally combines the checked flag and row deques with the temporary `"/" + row` string. The multibyte trim helper uses ordinary checked `std::string::operator[]` plus `erase`; current callers load loader-zero `0x00702918` as an unused receiver before byte-width helper `0x0040D1D0`.
- `CFileList::format_item @ 0x00439B20` is now canonical exact 233/233. The apparent prior relocation blocker was a source-semantics mistake: the local row construction uses the copy/assign helper at `0x004020D0`, while publication into the output string is `append(value, 0, -1)` and therefore truthfully reaches the distinct append helper at `0x00408B40`. Do not collapse those helpers or remap one COFF symbol to two targets. Cached `rank_retained_exact.py` `coff_size` values remain ranking hints only; acceptance uses the actual object and canonical comparator.
- Replay UI closes a second derivative. `CReplayList` is 0xB4 with ctor `0x0042A0A0` (159) and replay-specific formatter `0x0042A150` (501) exact. The ctor configures `Replay/*.rep`, `*.rep`, `Replay`, width 96 and directory mode through caller-visible out-of-line string assignment. The formatter copies a checked row, uses the one-byte directory flag for green `<color 40FF40>` rows, otherwise formats `directory/name`, reads the replay header through `0x0043B060 -> 0x00429A30`, compares against current code `0x6A` from `0x0043AA40`, and marks incompatible rows `<color 404040>`. `wsprintfA` must be declared `dllimport`; a plain extern emits a 5-byte REL32 call, while the target and exact source use the 6-byte IAT indirect call.
- `CMenuReplay` is a natural 0x280 Menu-derived lifetime with **signed** state bytes at +0x04/+0x05, `CReplayList +0x08`, `TitleDesignResource +0xBC`, three UI pointers, two `MenuCursorState` values, `GuideOverlay[2]`, and checked `std::list<int>` selection history +0x274. The signed identity is direct target evidence: the update/delete dispatchers use `movsx` before state switches. Normal dtor/scalar/scene-mode virtual `0x00446D20/0x00446F40/0x00446F60` remain 187/30/11 exact after refining the list from the old `void *` facade to `int`; its `_Tidy` call still folds to current `0x00435EB0`.
- The ReplayMenu state/render wave adds exact `update_mode_state @ 0x00446590` (186), `render @ 0x00446650` (282), and `update_delete_state @ 0x00446A60` (555). Mode is a current source-level boundary missed by initial IDA analysis: `CMenuReplay::update` restores the receiver/registers then tail-jumps to it, and the helper owns its own prologue/RET tails. Render must call `render_cursor` directly inside each state branch and convert the unsigned row index to `float` before double-precision spacing math. Delete is a signed-byte switch over browse/confirmation/result substates; one long-lived input-record pointer, checked `list<int>`, FileList navigation, Profile-message ABIs, and dllimport `wsprintfA/DeleteFileA` naturally reproduce all 555 bytes. Returning from the root recolors `items_f0[0]` and `[2]`, not `[1]` and `[2]`.
- Several adjacent Replay/FileList bodies are deliberate non-acceptances. `CMenuReplay` ctor is 350/350 with one receiver-scheduling difference; `refresh_file_list` is semantics-closed at 231 vs target 229 on EBX/zero scheduling; the 511-byte browse helper reaches the target input prefix once all reads use the same input-record pointer but remains 559 bytes because standalone VC8 duplicates `/GS` epilogues that target tail-merges. `CFileList::go_parent_directory @ 0x004398B0` is semantics-closed and 91/91 from `char slash='/'` plus `find_last_of(&slash,npos,1)`, but VC8 colors that real char into the high byte of the four-byte stack slot while target uses the low byte. Do not use char arrays/unions, goto-only CFG shaping, volatile, or register forcing to close these compiler choices.
- `CFileList::finalize @ 0x0043A590` remains pending, but its local **0x194-byte text-render owner is fully target-backed rather than opaque storage**. Current ctor/dtor/configure/parser/texture consumers close `HDC/HFONT/previous GDI object +0/+4/+8`, the same 0x128 text config at +0x0C, pen x/y and ascent +0x134/+0x138/+0x13C, pixel buffers +0x140/+0x144, height/width/pitch +0x148/+0x14C/+0x150, color table pointers +0x154/+0x158, checked allocation `list<void*> +0x15C`, packed color/flags +0x168/+0x16C/+0x16D, `std::string +0x170`, and ruby pen coordinates +0x18C/+0x190. Natural page formatting plus out-of-line handle/string visibility compiles to 1080 bytes versus target 1106; changing tile facade/template spellings does not remove the remaining 26-byte EH/iterator lifetime difference. Keep it pending rather than adding opaque padding or synthetic dependencies.
- The three CDesignBase color-forwarding virtuals `0x0040BFE0/0x0040C040/0x0040C0A0` are a deliberate non-acceptance. Current bodies traverse the recovered checked list and dispatch UiDesignObject vslots +4/+8/+0x0C, but natural `/O2` VC8 source emits 74-byte register-only bodies while each target body is 81 bytes with `push ebp; mov ebp,esp; and esp,-8`. Treat this as a TU/compiler stack-alignment blocker; do not add aligned dummy locals, volatile state, attributes, assembly, or other artificial work solely to force the target frame.


- The platform/input bootstrap island adds five current-target authored exact functions / 625 bytes from ordinary C++/Win32 source: `split_path_components @ 0x0040D0B0` (168), `read_clipboard_ansi @ 0x0040D160` (97), `ModulePathTextView::extra_bytes @ 0x0040D1D0` (34), `initialize_direct_input @ 0x0040D360` (138), and `initialize_keyboard_device @ 0x0040D560` (188). The path helper's sole resource-loader caller uses its directory output to rebuild asset paths; exact source must keep the non-null filename branch before the directory-only tail so VC8 emits the target far `je`. The clipboard helper's UI/network caller immediately parses the imported text as `IP:port`. The Shift-JIS helper's final condition is the ordered `value < 0xFF`, which naturally emits target `setb`; `value != 0xFF` emits `setne` and is not exact.
- DirectInput recovery demonstrates a reusable narrow-ABI technique: model only the observed COM vtable slots with typed `__stdcall` function pointers, then call them as ordinary C++. Current application bootstrap `0x00407DA0` calls DirectInput creation and keyboard setup consecutively; natural vslots reproduce `CoCreateInstance`, `IDirectInput8::Initialize +0x1C`, `CreateDevice +0x0C`, keyboard `SetDataFormat +0x2C`, `SetCooperativeLevel +0x34`, and `Acquire +0x1C` exactly. All GUIDs, data-format bytes, IAT cells, and Shift-JIS error strings are attested from the current PE before allowlisting; never fill external constants from memory when target bytes are available.
- Three adjacent platform bodies are explicit stop conditions. Module-directory initializer `0x0040D020` is 137/137 from truthful GetModuleFileName/strrchr/strcpy_s semantics, but standalone VC8 schedules the application-context global store before outgoing arguments while target schedules both context stores after argument setup. Device enumeration `0x0040D620` reaches an 80-byte local descriptor but calls `0x0040DE50` through an unresolved unused-ECX register ABI; do not invent a prototype merely to force codegen. `0x0040D200/0x0040D2A0` are referenced only from CRT initializer/terminator tables and remain origin-review rather than authored claims.
- DirectInput runtime is now exact-backed beyond keyboard bootstrap. `poll_direct_input_gamepads @ 0x0040D6D0` is 312/312, `DirectInputBackend::get_gamepad_button_count @ 0x0040D940` is 71/71, and `get_gamepad_state @ 0x0040D990` is 88/88. Together they fix one `DirectInputBackend @ 0x00702A28` aggregate with the DirectInput guard at +0x04, checked `vector<void*>` device storage at +0x10, 256-byte keyboard state at +0x20, gamepad count +0x120, `vector<RawGamepadState>` +0x124 with 80-byte elements, and `vector<int>` button counts +0x134. The poll path uses ordinary COM vslots `Poll/Acquire/GetDeviceState` and standard checked `operator[]`; no private helper ABI is needed for the accepted bodies.
- The same source/layout gives a replayable compiler-origin lane instead of inflating the authored denominator. `scripts/probes/direct_input_vectors_generated.cpp` regenerates fifteen current checked-vector COMDAT candidates / 1,455 bytes under the SHA-pinned VC8 SP1 compiler. Unique anchors include `vector<void*>::operator[] @ 0x0040DCC0`, `back @ 0x006A4DD0`, iterator dereference `0x006A29C0`, and `_Insert_n @ 0x00445F10`; explicit complete-hit equivalence groups cover the two RawGamepadState-vector `size()` clones, two `_Umove`, two `_Ufill`, three pointer-vector `insert`, and two `push_back` clones. `config/vc8-generated-direct-input-vector-origin-anchors.toml` fails closed if any masked fingerprint gains or loses a current-inventory hit.
- Exact authored TUs are themselves a high-value compiler-origin corpus. A discovery scan across the 199 configured fresh objects found 123 standard-library COMDAT fingerprints whose complete relocation-masked hit sets contained only review candidates. Discovery results were **not** applied directly. Fifteen source-specific `config/vc8-generated-*-std-origin-anchors.toml` manifests now recompile the actual already-exact TUs under their recorded `/GS` settings and replay 216 current candidates / 27,039 bytes. The largest families are `FixedSlotEnvelope.cpp` (63 candidates / 8,358 bytes), `EffectManagerLifetime.cpp` (36 / 5,217), `CFileList.cpp` (28 / 3,452), and `BattleInputSlotConfig.cpp` (38 / 4,614). Every fingerprint still requires at least 24 exact non-relocation bytes, at least 70% non-relocation coverage, supported DIR32/REL32 forms, bounded terminal `0xCC` only where declared, and an inventory-unique hit or an exact complete equivalence group. This raises replayable compiler-generated classification from 38 to 254 without treating `std`-looking decompilation as evidence.
- The same scan is also a guard against false authored promotion. The tempting 57-byte `SelectIntVector16::clear` shape has a complete three-address fingerprint match to a generated checked-vector `clear` COMDAT; application-side naming in a retained facade is therefore insufficient. Scanning non-`std` symbols in exact objects after the new census did not reveal another medium/large review candidate with a unique application identity. Use exact-object orphan mining for prioritization, but require current vtable/caller/RTTI ownership plus a canonical comparison before authored promotion.
- Origin discovery and origin proof must remain separate. A review-only orphan scan is useful for finding new candidates, but its apparent hit set is not sufficient because an already-classified target span may carry the same generated fingerprint. Before extending any generated-origin manifest, replay the relocation-masked fingerprint against the complete current `functions.csv` inventory. The follow-up `BattleInputSlotConfig.cpp` census used all 4,010 spans and proved one inventory-unique 1,818-byte checked `deque<short>::_Insert`, one inventory-unique 43-byte `unchecked_copy`, and one complete eight-address 43-byte `_Unchecked_move_backward` equivalence group. Those ten physical functions / 2,205 bytes are accepted only because the full-inventory hit sets are exactly 1/1/8 and `function-origins.py --check` replays the same fail-closed manifests.
- DirectInput enumeration remains a useful LTCG boundary rather than an invitation to fake calling conventions. Natural `initialize_gamepad_devices @ 0x0040D620` has the exact 176-byte target section and the same API/control-flow skeleton, but target keeps an extra checked-container lifetime slot and calls `vector<RawGamepadState>::_Assign_n @ 0x0040DE50` through a linker-private ABI with EDX/stack arguments while standalone VC8 emits ordinary thiscall. EnumDevices callback `0x0040D9F0` similarly reaches generated pointer-vector helpers whose receivers were LTCG-private; axis callback `0x0040DB40` becomes 161 bytes from truthful `gamepad_devices.back()` versus target 166 because the target realigns an EBP frame to 8 bytes. Mouse init `0x0040D810` is additionally blocked by error helper `0x0040D540`, whose message arrives in EAX. Leave all pending: no register variables, fake aligned locals, inline assembly, or invented prototypes.
- The lifetime side of the wave closes four current-vtable-owned application bodies and one missed boundary. `ResultList` scalar deleting wrapper `0x00447510` is 76/76 from vtable `0x006C2058`; base `Menu` scalar wrapper `0x00440750` is 41/41 from vtable `0x006C196C`; `AnimationObject` ctor `0x00422040` is 41/41 and publishes current vtable `0x006C0914`; `FrameData::~FrameData @ 0x00421A70` is a newly tracked 21-byte RET-terminated body between INT3 pads and its vtable-owned scalar wrapper `0x00421A90` is 47/47. PatRecord88's existing exact lifetime independently reaches the FrameData base destructor, so the new boundary is not inferred from raw similarity alone.
- Two nearby lifetime/render leads deliberately remain unaccepted. Recompiling ordinary `CEffectSprite` construction under `/GS` reaches the target 79-byte length, but target publishes the derived vptr before the EH-state store while fresh VC8 schedules those stores in the opposite order; do not force the swap. Current `ResultList::populate @ 0x004478B0` is 1,133 bytes, while the present tracked source/header visibility compiles to 1,003 bytes (an older retained TU once emitted 1,127), showing that resolver/deque visibility still needs reconstruction rather than decompiler transcription.

- Large-body negative evidence from the 27,039-byte exact-TU origin-census work should prevent repeated optimizer-coercion attempts. `resolve_fighter_body_collision @ 0x0046CCA0` has the target edge/AABB semantics and a 0x40 frame, but fresh ordinary source is 1853 vs target 1862; swapping real declaration order does not change stack coloring, while swapping the independent motion computations changes the target-observed evaluation order and is wrong. `try_dispatch_front_word_actions_690_696 @ 0x00494450` is target/fresh 950 with only five localized instruction-shape differences; nested/negated/split signed-range spellings all canonicalize to the same `lea(state-150); cmp 7` form, and moving `saved_window` earlier makes EBX live too early. `resolve_group_b_object_clash @ 0x0046CA80` is semantics-closed but current source is 557 vs target 542 on compiler tail merging; target-observed slot-first rewrites make VC8 worse (571). `try_group_a_vs_group_b_interaction @ 0x0046C930` is 164/164 with only `movzx edx,word [frame+0x2A]` versus fresh `mov dx,...`; unsigned casts, real locals, and a 16-bit bitfield probe still emit the latter. Treat these as TU/LTCG/allocation blockers until stronger source-partition/type evidence appears; do not use gotos, volatile, dummy locals, redundant arithmetic, or assembly.


- The Fighter continuity wave closes two substantial current bodies / 754 bytes in one existing TU. `update_fighter_counter_thresholds @ 0x0045B300` is a newly tracked 284-byte source boundary: `0x0045B260` ends at `0x0045B2F2`, eleven `INT3` bytes separate the next prologue, the helper ends at `0x0045B41B`, and `0x0045B420` begins only after more `INT3` padding. Natural source is 284/284 exact only when its threshold-notification service is the already exact-backed `g_info_manager @ 0x006FBCA8`; the earlier `g_battle_scene_renderer @ 0x006FBCA4` facade was a plausible but wrong owner. `decrement_fighter_timers_and_cleanup @ 0x0045E500` is 470/470 exact and independently tail-calls the new helper twice.
- `decrement_fighter_timers_and_cleanup` adds two reusable source-shape lessons. Current callers prove `0x004359F0` is an unused-this Fighter member even though the decompiler drops ECX and shows only a tail-forward to exact `dispatch_indexed_event @ 0x0043B1A0`; declaring it as a member naturally restores target `mov ecx,esi` before the call. The final threshold logic should be written as the truthful condition `counter_558 >= 500 && state_55a < 5 && state_72c != 2`, followed by the real prepare/dispatch/reset/update-and-return path, then one shared `state_55a >= 5` zeroing check. The retained nested form redundantly tested `state_55a >= 5` twice and could not reproduce the target shared-zero/early-return CFG. This is semantic CFG recovery, not permission to add source gotos. The two adjacent short clears are also statement-order-sensitive: target-compatible ordinary source writes +0x4A4 before +0x4A2.
- Nearby Fighter/sequence candidates are explicit stop conditions from the same investigation. `advance_fighter_sequence_55c @ 0x0045D040` is target/fresh 198 bytes with the same checked sequence logic, and making the truthful empty `CSprite::~CSprite` visible plus using a qualified destructor call naturally exposes the target `IColor` vptr teardown; standalone VC8 then hoists a zero in ECX across later deque updates and shrinks to 187, while target keeps immediate zero stores. `consume_spell_sequence_entry @ 0x0045D230` is 346/346 with the first 204 bytes exact, then target retains the spell `value_1e` in AX across argument setup while fresh stores it immediately. `adjust_capped_counter_558 @ 0x0045CED0` is 126/126 with only a branch-merge EAX/EDX permutation; short/int adjusted-value locals change the prologue/body and are worse. `save_battle_setup_slot @ 0x0042B9B0` proves the 16-byte `slots_f4` storage has checked-vector `size()` lowering, but exposing that vector type changes persistent-this allocation and yields 301 versus target 305. `reset_battle_round @ 0x00471180` is 290/290 after current global migration but differs on checked-list owner/pointer lifetime. Keep all pending; do not use volatile, fake locals, manual vtable loads, register variables, source-only gotos, or inline assembly to erase these compiler/TU differences.

- The CBattleManager/setup wave adds six authored canonical-exact functions / 1,085 bytes without lowering the source-evidence bar. Current base vtable `0x006C1504` owns `prepare_battle_setup @ 0x00472110` (276) and `post_pipeline_slot_30 @ 0x00472370` (277). The derived override caller at `0x00473520` calls the base setup slot and ignores EAX, proving a `void(MatchSetup*)` ABI rather than the decompiler's incidental byte return. A real `char path[260]` in a `/GS` TU, current input/setup/audio/pair services, and the exact round globals reproduce 276/276. The +0x30 virtual is ordinary nested Fighter terminal-pending/delay/finalize state plus this+0x5B0 and vslot +0x34 transitions, exact 277/277.
- FighterPhaseContext is now a reusable current layout rather than loose call-site facades. `begin_slot @ 0x004650B0` is 69/69 with unsigned slot bound, three handle dwords at +0x28, three active bytes at +0x34, checked `list<int>` storage at +0x48, event +0x24, and exact list push helper `0x004229A0`. `initialize_slot @ 0x00465130` is 88/88 with ABI `(unsigned slot, MatchSetup::Side*, bool asynchronous)`; the async path builds an 8-byte `{slot,side}` request, queues it via `0x00466DB0`, and signals event +0x20. These two definitions must remain in separate TUs: making `begin_slot` visible in the initialize TU causes VC8 to inline it, while the target preserves the out-of-line call. This is source partition evidence, not a reason to add `__declspec(noinline)`.
- `BattleObjectManager::reset_battle_render_state_and_signal @ 0x00466DF0` is 332/332 from a narrow offset view plus **real checked `std::list<int>`** members: pending +0x28 and active +0x34, event +0x24. Exactness depends on source lifetime that the target itself exposes: evaluate pending emptiness immediately after the two -0.5f stores, perform the common geometry/state reset while that condition remains live, then create the active-list reference only inside the transfer branch. The +0xF8 state is an unsigned byte assigned `0xFF`; a signed `-1` lets VC8 reuse AL and changes the target immediate store. Natural `front/push_back/pop_front` lowers through current `_Buynode/_Incsize/delete` helpers exactly. `reset_fighter_round_counters @ 0x0045F610` adds 43/43 and confirms an ECX-only fastcall Fighter ABI.
- Nearby stop conditions remain useful. `load_battle_setup_slot @ 0x0042BAF0` is target/fresh 332 but shares the save-slot checked-vector visibility/persistent-this blocker; exposing the vector fixes `size()` lowering but changes allocation elsewhere. `pre_reset_slot_40 @ 0x00472230` similarly depends on a 0x288 `CMenuPractice` extent while +0x15C remains unresolved. `FighterPhaseContext::reset_slot_state @ 0x00465040` reaches an unknown active-slot callee `0x0064AF70`. Do not add padding, fake prototypes, register coercion, or manual vtable calls to close these.

- The CBattleManager dispatcher/transition wave closes two vtable-owned authored bodies / 969 bytes. `dispatch_battle_state_frame_472b20 @ 0x00472B20` is 572/572 from ordinary `/GS` virtual C++: UI-selection early return, network/non-network menu handling, target-observed `new(0x288)` Practice versus `new(0x78)` Battle menu extents, session-option synchronized-input gating, frame increment, and seven phase virtuals at vslots `+0x10..+0x28`. VC8 places a 28-byte local jump table after the reviewed 572-byte RET boundary in the same 600-byte COMDAT; never count the table as authored function bytes. `transition_slot_34 @ 0x004728C0` is 397/397 and closes vslot `+0x34`: phases 1..6 reuse exact fighter counter reset and post-sequence effect services, reload exact-backed `g_info_manager @ 0x006FBCA8` at each effect call, and clear `frame_counter_04` in the common tail.
- Current `CBattleManager::transition_slot_34` corrects two retained assumptions. Its phase-1 placement literal is **400.0f at `0x006C3A5C`**, not the 480.0f used by the older BattleController transition path. Phase 6 only observes `operator new(0x44)`, external `CMenuEnd` ctor `0x00445100`, and menu installation; source may therefore use an opaque observed 0x44 allocation extent without naming the unresolved trailing four bytes. This does not upgrade the known CMenuEnd member layout from 0x40; allocation extent and recovered member extent remain separate evidence.
- `BattleInputGate::poll_synchronized_input @ 0x004712B0` remains deliberately pending despite truthful RAII semantics. `/GS` with `CriticalSectionScope` closes the EH/cookie, lock `+0x124`, input `+0x104`, and published/consumed `+0x140/+0x141` layout. The base source is 127 vs target 129; a real `bool equal` local reaches 129/129, but target saves callback AL in BL before materializing equality in CL while fresh VC8 assigns the merge in the opposite order. Do not use register forcing or dummy liveness for those two bytes.


- The Fighter resource TU now closes three canonical-exact bodies / 664 bytes under one `/GS` object: existing `load_indexed_character_wave_resources @ 0x0045F680` (132), new `Fighter::initialize_fighter_spell_resources @ 0x00463070` (333), and `FighterCutinResource::load_for_character @ 0x004651E0` (199). Current target strings fix the face/back/cutin paths; current `g_title_resource_manager`, `wsprintfA`, and -1.0f literal identities are independently attested. The two new bodies add 532 authored bytes without creating a fragmentary unit.
- `SpellDataOwner::load_spell_data`'s middle parameter is a **signed char story-mode value**, not a full `int`. This is cross-checked in both directions: the existing 71-byte selector body is unchanged when narrowed to `signed char`, and exact caller `0x00463070` then naturally emits target `sete al; push eax` instead of the false full-int zero extension. The selector only forwards the value to loaders already taking signed-char story mode. A `bool` declaration changes the selector to 75 bytes and is rejected.
- Post-call subobject lifetime is byte-significant in both recovered resource bodies. Keep the virtual configure expression directly on `face->sprite_04` / `sprite_04`; only after configure completes, introduce the real `FighterCutinSpriteWork *` used by the later mirror-scale writes. VC8 then reads the virtual slot through the member expression but starts the long-lived EDI receiver at the target point. Declaring that pointer before configure hoists the receiver and shortens the call sequence.
- `FighterCutinResource::load_for_character` is also a positive anti-coercion reference. Older retained source used `volatile` solely around `state_9c = -1`; removing `volatile` and restoring the real post-configure sprite lifetime makes ordinary VC8 schedule the truthful store between `pop edi` and `pop esi`, yielding 199/199. Do not preserve a historical volatile/register hack merely because it once looked useful for matching.
- Large UI follow-ups from the same investigation remain deliberate non-acceptances. `CSelectScenario` ctor is target 862 while truthful current source remains 856 after recovering more accurate SSO/checked-iterator lifetimes; its remaining differences are EH/evaluation scheduling. `on_scene_enter` is 261 vs 271 on member-input reload CSE, `update_selection` is 406 vs 414 after recovering the real `one=1` lifetime and `vertical > 0` branch but VC8 tail-merges the two float-rounding arms, and `update` remains 268 vs 247 around menu-allocation null tails. `CMenuResult::update_active` reaches 620 vs target 619 under the correct `/GS` profile but differs on final-return/CProfileMenu allocation tail merging; ctor is 493 vs 502 on scene-mode constant CSE; `refresh_selected_character` is 347 vs 342 on zero-register allocation. `/O1` and `/O2 /Os` truth tests both produce an unrelated 320-byte EBP-frame body, so do not change the project optimization profile to chase that helper.

- The roster relationship lifetime is a real polymorphic intermediate layer, not just the flat tail used by constructor probes. Fifteen current 406-byte destructors write RTTI-owned `CharacterObjectEx<Fighter,FighterObject>` vtables distinct from the final `FighterObject` vtables, and fifteen unique 30-byte scalar wrappers call those normal destructors. A destructor-only template view reproduces all thirty bodies / 6,540 bytes exactly while leaving the already-exact pool-constructor TU untouched. This is a concrete example where one binary class needs different source visibility in different recovered TUs: making `CharacterObjectEx` visible to the pool TU changes constructor lowering and is therefore rejected.
- Checked-deque source lifetime is byte-significant in those destructors. The parent search must keep `end()` ephemeral inside the loop condition; saving it as a local grows the 406-byte body to 410. The child walk must retain the target-observed `if (!child_refs.empty())` gate; removing it shrinks to 393. Replacing the loop with `std::find` grows to 440. Prefer these target-observed C++ lifetime/CFG distinctions over volatile/register/assembly shaping. `CharacterObject::~CharacterObject @ 0x00492ED0` is separately semantics-closed at target length 145 only when `AttackObject::~AttackObject` is non-throwing, but standalone VC8 still folds the target array-delete thunk into scalar delete; leave it pending until TU/link visibility explains that symbol naturally.

## Unknown

- Exact VC8 service pack and compiler/linker flags used for every original
  object, despite the current probe profile reproducing the accepted wave.
- Original translation-unit partition and which classes/functions underwent
  LTCG transformation.
- Accepted boundaries and authored/library origins for the remaining 2,389
  provisional candidates.
- The complete authored denominator needed to measure the 95% function and byte
  goals honestly.
- Which additional retained 1.06 source implementations remain semantically
  identical or naturally reconstructable in 1.06a.

Add reusable facts here only after they survive a target-backed bounded
investigation. Detailed compiler- and comparator-shaping patterns belong in
`docs/BUILD_MATCHING.md` once supported by reproducible 1.06a examples.

## Current battle globals

`config/known-globals.csv` now carries the current-target semantic identities
that have survived exact authored recovery: the battle input gate, match id,
stage-surface array, round/transition bytes, pair state, fighter phase context,
object/setup/effect/info managers, battle phase block, scenario-event map, and
title resource manager. These mappings come from current IDA semantics plus
accepted zero-difference functions; they are not cross-version delta guesses.

- The 237-byte roster object-manager spawn clone family is now class-disambiguated by current allocator-chain vtable writes, not address order, and all fifteen current classes are canonical exact. Fourteen share `RosterObjectSpawns.cpp`; Sakuya remains in `SakuyaObjectSpawn.cpp`, but removing retained-only `owner_field_160` and `child_refs` locals restores the same direct owner-field/original-parent/related-object dataflow and closes `0x004DF2C0` at 237/237. The old Sakuya source first diverged at function offset `0x0D` because those locals caused the parent argument to be loaded before the returned object was fixed in ESI. Treat clone-family source identity and class identity separately: current Sakuya allocator/vtable evidence was required before reusing the common source shape.

- The roster `ObjectPool::acquire` family is now canonical exact for all fifteen characters: 15 functions × 519 bytes = 7,785 authored bytes from one `/GS` macro TU. The decisive reused-slot source lifetime is `unsigned generation = handle->generation;` immediately after the checked free-list erase and before `generations_14[slot] = generation`. The target zero-extends the 16-bit handle generation into a 32-bit value before checked-vector bounds and preserves that value across the bounds path. Keeping the direct member expression lets VC8 delay the load and emits 514 bytes; narrowing the local to `unsigned short` changes the lifetime again and emits 522 bytes. The semantic 32-bit local makes every specialization 519/519 without `volatile`, register variables, inline assembly, fake dependencies, or copied bytes.
- Treat the shared code shape and class identity independently in this family. All fifteen bodies use the same folded EH handler and the same checked-container helpers (`vector<Object*>::push_back @ 0x0064AF70`, `vector<Object*>::at @ 0x0056D450`, `vector<unsigned>::push_back @ 0x0040DCF0`, `list<unsigned>::erase @ 0x004026A0`), but each function writes its own RTTI-owned `FighterObject` vtable twice. Those current vptr writes, not clone order or the shared EH address, disambiguate Reimu through Tenshi before canonical comparison.

- The 103-byte `TObjectManagerBase` ctor structural family contains twenty current
  clones. Current template-vtable writes separate five effect/UI instantiations
  from fifteen roster-character specializations; all fifteen roster ctors are
  canonical exact under the retained `/GS` profile.

### Aggregate DIR32 addends preserve object identity

`Menu::render_cursor @ 0x0043F9D0` is the reference for a shared aggregate whose
base and interior fields all appear as absolute relocations. Current IDA shows
twelve callers, loads the shared cursor sprite base `0x006E6958` as the render
receiver, writes scale fields at `+0x88/+0x8C`, and calls the current sprite
render body `0x00406BE0`. Natural C++ expressing `scale_x = scale * (1/512)`,
`scale_y = 1.0f`, then `render(x,y)` reproduces all 55 bytes under pinned VC8.

Keep one semantic aggregate symbol. The comparator can map
`COFF_SYMBOL+0x88` and `COFF_SYMBOL+0x8C` to a separately attested addend
allowlist while mapping bare `COFF_SYMBOL` to the object base. Do not invent
standalone globals for fields merely because their absolute addresses appear in
the target. For initialized literals, retain literal-byte validation; for an
aggregate field whose object section bytes are not independently represented by
the probe, address validation plus target-backed field identity is the truthful
model. This is relocation evidence only: canonical zero-difference comparison
still decides exactness.

### Loader-zero input facade and stripped-target ABI aliases

`is_menu_initial_press @ 0x0043ABD0` and `set_menu_input_state @ 0x0043ABF0`
are thin authored facades over the shared input-state receiver at `0x006FC618`.
Current target call flow proves the first forwards four stack byte/bool slots to
canonical-exact `is_initial_press @ 0x0040A660`; the second forwards index/state
to canonical-exact `set_input_state_table_entry @ 0x0040A6B0`. Ordinary C++
with `bool` parameters naturally keeps the incoming four-byte stack slots intact
and reproduces 31/31 plus 21/21 without normalization tricks.

Do not trust a decompiler/global-value display as PE initialization evidence. IDA
can present `0x006FC618` with a nonzero inferred value, while the canonical PE
places the address beyond `.data` raw-backed bytes inside its virtual extent; the
loader therefore supplies zero bytes initially. Relocation evidence must follow
the mapped executable, not the inferred display value. Also keep stripped-target
identity separate from source decoration: existing exact callers may compile
under ABI-compatible retained declarations with different mangled names. Do not
rewrite those callers merely to make one source spelling globally uniform when
the target address, forwarded stack slots, and exact member ABI already establish
the wrapper contract.

### Nested call arguments can recover the target evaluation schedule

`EventSubobject130::trigger_global_effect @ 0x00434E90` is 74/74 exact and is
called from all fourteen canonical-exact roster event bridges through the proven
Fighter `+0x130` subobject. Current target semantics publish the event id, resolve
an effect record through `0x0043C150`, emit it through exact-backed
`g_info_manager @ 0x006FBCA8` with `320.0f`, `42.0f`, and flags `1,1`, then
forward the same id to the shared state controller at `0x006FBCAC`.

A semantically equivalent temporary `effect_id = lookup(value)` made VC8 perform
the lookup before preparing the remaining call arguments and therefore produced
the wrong schedule. Keeping the real dependency in the call expression,
`emit(lookup(value), 320.0f, 42.0f, 1, 1)`, lets VC8 naturally preload and spill
the right-to-left independent arguments before evaluating the nested first
argument, exactly matching the target. This is a source-lifetime correction, not
register shaping: only use it when current call semantics independently prove the
nested expression and the temporary has no source-level lifetime of its own.

### Exact callers can name nearby facade wrappers without guesswork

`BattleAudioSetupView::set_stage_music_433860 @ 0x00434DE0` is 40/40 exact. The
name and ownership come from canonical-exact `battle_initialize_or_transition_46fe80
@ 0x00470880`, not from the stripped helper itself. Current disassembly then proves
the wrapper forwards `(stage, value, true)` through battle-object manager
`0x006FBCA0 -> 0x00467850` and mirrors `stage` through the shared event-effect
state `0x006FBCAC -> 0x00469D50`. A narrow standalone TU reproduces the target
without new relocations because both global identities were already established.

When a review helper is stripped but an exact caller already carries a stable
semantic declaration for that exact target address, treat the caller declaration
as high-value naming evidence. Still verify the callee body and arguments in
current IDA before promoting; the caller name is a hypothesis anchor, not a
substitute for target semantics or canonical comparison.

### Same-signature state forwarding can naturally become a tail jump

`EventEffectStateView::set_event_id @ 0x00469DF0` is 29/29 exact. When the id
changes it stores the new `+0` value and returns
`publish_stage_state(g_battle_transition_mode)`. Pinned VC8 naturally zero-extends
the shared byte, overwrites the incoming integer argument slot, and emits a `jmp`
to `publish_stage_state @ 0x00469D50`; unchanged ids return directly. No explicit
tail-call syntax, stack manipulation, or register forcing is needed.

This is a useful signal for short forwarding methods: when target code rewrites a
current stack argument and jumps to another same-receiver/same-width member, first
try a normal `return other_member(new_argument);`. Only accept the interpretation
when current state layout, the source of the replacement argument, and the tail
destination are independently established. Shared narrow contracts should live in
a header so separately reconstructed callers do not drift into incompatible ODR
views.
### Polymorphic subobject forwarders

`InfoEffectEmitterView::emit_effect @ 0x0046E9D0` is a compact reference for
recovering a facade that forwards into an embedded polymorphic subobject. Exact
`EventSubobject130::trigger_global_effect @ 0x00434E90` first fixes the member
identity. Current target code then independently proves `this+4` as the subobject
receiver, vslot `+0x0C` as the virtual emit operation, the five incoming arguments,
and one trailing zero argument supplied by the facade. Express those facts as a
narrow C++ subobject with a virtual method; pinned VC8 naturally emits the exact
49-byte stack schedule and `retn 0x14`.

Do not flatten such a call into a free function or manually reproduce the vtable
load just because the decompiler exposes registers. The embedded-subobject
ownership and virtual slot are semantic evidence. When the contract is promoted
to a shared header, replay every accepted TU that previously carried a local
forward declaration; here `EventSubobject130` remains 34/34 and 74/74 exact after
the consolidation.

The same shared subobject contract now backs `PostSequenceTransitionView::publish_transition_effect_46e040 @ 0x0046EA10`. Nine current battle callers converge on that helper, and exact `dispatch_round_phase_34` independently establishes the owner role. Natural `emitter_04.emit(effect_id, 320.0f, 240.0f, 1, 0, 0)` reproduces 47/47, including the target x87 materialization order. Reuse the shared emitter ABI when the owner offset and virtual slot agree; do not proliferate TU-local facades for the same subobject.
### Caller-backed unused-this return ABI

Do not infer a return contract from EAX residue in a callee that never explicitly
constructs a result. `EventSubobject130::trigger_secondary_event_effect @
0x00434E40` is the reference. IDA initially decompiles it as an `int`-returning
stdcall-like function because the no-op path leaves the incoming id in EAX and
the effect path leaves the virtual emitter result there. Fifteen current callers
in `0x00473F90` instead prove the real source ABI: each sets `ECX` to
`Fighter+0x130`, pushes one id, calls the helper, and immediately returns without
consuming EAX. Modeling it as an unused-receiver `void EventSubobject130` member
lets natural VC8 reproduce the target `jz` directly to the final `retn 4` and all
67 bytes. An `int` source forces an extra constant return or different block
layout.

The adjacent `PairEmbeddedModeView::select_pair_mode_434780 @ 0x00435CE0` uses
the same principle at larger scale. Its member identity comes from exact
`initialize_battle_fighter_pair_46a490`; the body may leave `this` unused while
calling other unused-this members. Preserve caller-proved class ABI even when
the body does not dereference ECX. A decompiler's free/stdcall presentation is
not stronger than repeated current call-site evidence plus canonical codegen.
### Promote repeated subobject offsets into shared layouts

When many independently exact class views agree on the same embedded subobject,
replace the corresponding unknown range in the shared layout instead of keeping
the fact duplicated in per-function archaeology. Fourteen exact roster event
bridges place `EventSubobject130` at Fighter `+0x130`. `Fighter::
advance_secondary_event_effect_cycle @ 0x00473F90` provides independent
cross-roster caller evidence: callers pass Fighter `this`, and each switch case
adds exactly `0x130` before calling the exact secondary-event helper. Splitting
`Fighter::unknown_105` around a real `EventSubobject130` therefore refines known
layout without changing size or inventing padding. Replay every unit that sees
the shared header after such a promotion.

The same function is a dense-switch counterpart to the sparse event-bridge
examples. Natural cases 0..14 compile to an exact 244-byte executable body and
a 15-entry/60-byte jump table immediately after it in the same COMDAT. Keep the
accepted function boundary at the target's RET-terminated 244 bytes while still
auditing the compiler table shape; the comparator's same-section DIR32 handling
resolves local case labels without treating the trailing table as authored
function bytes.

### Vtable-owned class islands can batch exact virtual methods

Current constructor writes can turn a stripped cluster into a bounded class recovery lane. `sub_438CC0` publishes the `CBattleManager` vtable at `0x006C1504`; derived `sub_438D80` calls that constructor and then publishes the `CBattleManagerArcade` vtable at `0x006C154C`. The two tables share nine slots now reproduced by `BattleManagerVirtuals.cpp`: `0x00471630` (58), `0x00471920` (6), `0x00472490` (23), `0x004724B0` (44), `0x00472D80` (60), `0x00472DC0` (66), `0x00472E10` (133), `0x00472EA0` (111), and `0x00472F10` (102), for 603 exact authored bytes. Vtable ownership is the identity evidence; canonical comparison still proves each body.

The class layout is intentionally narrow. Current bodies establish unsigned `frame_counter_04`, `pending_08`, two Fighter pointers at `+0x0C/+0x10`, `phase_88`, and a polymorphic phase object at `+0x8C`. Existing exact Fighter work already proves `terminal_pending_4e9`, `unknown_4ec`, and `state_72c`. An unsigned declaration for `frame_counter_04` is semantic evidence, not code shaping: target `0x00472D80` uses `jbe` for the `<=60` gate, while a signed hypothesis compiles to `jle`.

Unused-this ambiguity can also occur at linked helper edges inside a class island. Call sites before `0x0046AF90` and `0x0046AFA0` preserve or reload `ECX = CBattleManager*` even though the 11-byte target wrappers themselves do not consume ECX and can independently be modeled as free wrappers. `BattleManagerVirtuals.cpp` therefore uses explicit caller-backed member *views* for those edges without changing the existing callee ownership/signature ledgers. One target address may admit multiple narrow ABI views when the body leaves a register semantically unused; do not rewrite an already accepted callee solely to satisfy one caller.

Prefer direct semantic-global field access when it matches the source lifetime. `CBattleManager::run_info_phase_pipeline_472f10` initially used a local `InfoManagerPhaseCounter494View*`; VC8 cached `counter_494` in a register and produced a four-byte-longer body. Repeating the truthful `g_info_manager` view at the two field accesses lets VC8 naturally emit target memory `cmp [eax+0x494],30` / `add [eax+0x494],1`, making all 102 bytes exact. This is not permission to duplicate arbitrary expressions: the global identity and field are independently target-backed.

The adjacent `CBattleManager` timer method `0x00472A70` is a deliberate non-acceptance example. Current semantics prove phase 2, pair-mode globals, a 16-bit timer, thresholds 499/999, exact event-effect helpers, and a common tail at `0x0046B160`. Multiple natural source shapes either reload the timer, spill it, or allocate a different 16-bit register; the target keeps one value in AX across the branch. Stop there. Do not use `register`, volatile, fake locals, assembly, or tautological control flow to force the standalone compiler.

### Cross-xrefs can correct a plausible but wrong subsystem name

Do not freeze a global's ownership from the first exact caller that exposes it. During `CBattleManagerArcade` transition recovery, `0x006FCF7C` and service object `0x007026E0` first appeared inside a scene-transition path and could plausibly have been named as generic scene-transition state. Current `0x0043CF00/0x0043CF80` are the stronger ownership evidence: both are BGM load/play bodies, replace the handle at `0x006FCF7C`, and call sibling members on `0x007026E0`. The durable names are therefore `g_bgm_handle` and `g_bgm_service`. Revisit provisional semantic names whenever broader xrefs expose a more specific owner before committing them.

The resulting natural audio facade is exact without implementation tricks: `stop_bgm @ 0x0043B120` is 17/17, `fade_bgm @ 0x0043B140` is 40/40, and `set_bgm_volume @ 0x0043B170` is 47/47. The volume wrapper's second argument is converted through canonical double `100.0` before the service call. Keep this audio contract separate from screen-fade state even though both are orchestrated by the same scene transition.

Screen-fade state lives independently in loader-zero globals `0x006FBD50` (value), `0x006FBD54` (signed delta), and `0x006FBD58` (color); current frame update `0x0043B4A0` integrates and clamps the value. `start_scene_fade_out @ 0x0043B240`, `start_scene_fade_in @ 0x0043B280`, and `is_scene_fade_in_progress @ 0x0043B2C0` reproduce 58/58, 58/58, and 24/24. Async request getter `0x0043AB90` is 6/6 from loader-zero `0x006FBD48`, whose ownership is independently established by loader thread `0x0043B580` and request publisher `0x0043B840`.

### Derived overrides may compose exact base and facade contracts

`CBattleManagerArcade::run_info_phase_pipeline @ 0x00473870` is owned by the derived vtable at `0x006C154C` and compiles 220/220 exact from a narrow derived view. Only `+0x5BC` and `+0x5C4` are exposed as derived accessors because the intervening layout is not yet proved; this is preferable to inventing a large fake padding/member block. The override reuses the exact base reset/info/phase contract, exact screen-fade/async helpers, and exact BGM fade facade.

Preserve callee return contracts when a caller consumes only a byte. `is_scene_fade_in_progress` explicitly materializes integer EAX 0/1, while the Arcade override tests only AL. The caller source therefore narrows with `static_cast<unsigned char>(...)`; do not change the callee to `bool` merely to obtain `test al,al`. This is the same narrow-at-call-site rule used elsewhere in the reconstruction.

The adjacent Arcade override `0x00472F80` is a useful stop condition. Ordinary derived C++ matches all 198 target bytes except the ordering of one final branch: target loads the vptr, increments derived state `+0x5B0`, then loads vslot `+0x34`; ordinary virtual-call source increments first, while explicitly saving the complete slot loads it too early. Reproducing the exact split would require manual vtable/evaluation-order shaping. Leave the method review-pending rather than using hand-written vtable access, comma-expression tricks, or register coercion.

### Checked-container call ABI can be reconstructed from the call site

`CBattleManagerArcade::prepare_arcade_transition @ 0x00473050` closes a checked-list ABI without recovering the entire container implementation first. Current target passes `ECX = this+0x5B4`, loads the sentinel from `+0x5B8`, then its first-node pointer, and calls `0x0043FC40` with three stack components: node/current, owner/container, and hidden return storage. This matches an 8-byte iterator `{owner,current}` passed by value to a member returning an 8-byte iterator by value. A narrow `ArcadeTransitionListView::erase(ArcadeTransitionIterator)` declaration plus ordinary `list.erase({&list, list.sentinel->next})` makes VC8 generate the exact call sequence. Do not hand-write pushes or model the first node as a separate global.

The surrounding 189-byte function is a useful `/GS` reference. A real `char path[260]` naturally produces the 0x110 stack reservation and security-cookie lifetime; current setup services account for object-manager reset, stage/BGM publication through receiver `this+4`, BGM path formatting/loading, fighter-phase slot preparation, exact pair initialization, derived byte `+0x5B0`, and derived counter `+0x5C0`. The current path format at `0x006C1590` is the NUL-terminated `data/bgm/st%02d.ogg`. Because the probe only declares that external data symbol, its relocation is address-validated while canonical target bytes still attest the string; do not claim the probe object owns the literal.

Keep incidental EAX out of ABI decisions. The only current caller, exact Arcade override `0x00473870`, calls `0x00473050` and immediately continues without consuming EAX. The target helper itself has no semantic return setup after the checked-list call/security check, so the tracked source is a `void` member despite decompiler output presenting the residual callee value as an `int` return. Caller use plus source semantics outrank incidental register residue.

- The Direct3D backend/render-mode wave adds seven authored canonical-exact functions / 1,072 bytes. Current D3D9 method slots close one aggregate at `0x007036A0`: polymorphic `CCriticalSection` `+0`, `IDirect3D9* +0x1C`, `IDirect3DDevice9* +0x20`, `IDirect3DSwapChain9* +0x24`, `D3DCAPS9 +0x28`, `D3DPRESENT_PARAMETERS +0x158`, `D3DDISPLAYMODE +0x190`, and the checked reset-listener list at `+0x1AC`. Exact frame begin/finish/prepare (`0x00401000/0x00401040/0x004010C0`), state-22 setter (`0x00404A40`), 482-byte render-mode policy (`0x00404A90`), 329-byte device-reset callback (`0x004014C0`), and COM shutdown (`0x00414C50`) independently exercise this layout. Keep the aggregate identity instead of separate magic globals for device/swap-chain/presentation fields.
- `RenderModeManager::on_device_reset @ 0x004014C0` is a useful aggregate-lifetime/codegen reference. The target copies the seven dwords at `this+0x0C..+0x27` with `rep movsd`, then clears exactly 28 bytes. Writing a truthful `RenderModeState28 saved = state_0c; memset(&state_0c, 0, sizeof(state_0c));` makes pinned VC8 emit the target copy plus `xor ecx,ecx` / seven dword stores; seven independent field assignments allow later state-call preparation to interleave and produce the wrong 324-byte body. Two store-order details are also real target data flow: cache `gate7/gate14` before replaying render-state 23, cache state23 after the call; state-22 restore intentionally reloads the global backend device rather than `this->device_04`. With those source facts the callback is 329/329 exact.
- `RenderModeManager::apply_render_mode @ 0x00404A90` demonstrates that a shared source tail and physical case order can both be authored byte evidence. Keeping only one post-switch `current_mode = mode; mode_flag = flag; return result;` makes VC8 preserve the flag across the switch instead of reloading it per case. Ordering the source cases `4, 1, 2, 3` reproduces the target block order and accepted 482-byte body; VC8 places an 18-byte jump table after the accepted function body in the same COMDAT. Do not duplicate the tail or reorder cases for readability without replaying the target.
- Several neighboring D3D/render helpers remain intentionally pending. `toggle_d3d_window_mode @ 0x00414B30` reaches the exact target length 286 after correcting its caller-backed ABI to `void`, but the windowed metrics branch still selects a different callee-saved temporary allocation; do not force EDI/EBX with register variables. `reset_d3d_device @ 0x00414A10` is semantically closed at 277 vs target 281 and differs in stack-alignment/shrink-wrapped iterator lifetime; a single long-lived iterator hypothesis instead bloats to 362 and is rejected. `init_d3d_backend @ 0x004147E0` is current-semantics-closed but its sole caller exposes a private/LTCG register ABI (`EAX=width`, `ESI=HWND`) that must not be faked. Texture-stage bind `0x00404DF0` is 97 vs 107 because target preserves stage/device-vtable values across handle lookup, and `set_pair_state @ 0x00404940` remains a first/second-byte register-lifetime permutation. Leave all review-pending until stronger source/TU evidence appears.

### Render primitive and scene-consumer wave

Current Direct3D9 call offsets now close `SetSamplerState` at vslot 69,
`DrawPrimitiveUP` at vslot 83, and `SetFVF` at vslot 89.  The accepted
RenderModeManager primitive family is `present_d3d_frame @ 0x00401060` (81),
`submit_primitive @ 0x004010F0` (60), `submit_textured_primitive @ 0x00401130`
(77), `draw_rect_int @ 0x00401260` (51), and `set_mode @ 0x00404750` (137).
The two submit methods must reread `device_04` after SetFVF; caching the device
pointer across that COM call is a different, shorter alias assumption and does
not match the target.

RTTI independently names the recovered frame consumers `CFade`, `Ending`,
`CLogo`, `Opening`, and `CLoadingSV`; a 43-byte scene-render delegate is shared
by several scene vtables including `CBattleSV`.  Their exact render bodies add
43, 142, 132, 62, 53, and 217 bytes respectively.  `Ending` preserves the
failed `begin_frame` byte result directly, while the others use an explicit
false tail.  `CLoadingSV` reuses the existing exact `UiSprite94` vtable-cache
pattern, and its phase word is at `+0xB8`.

The current scene renderer pointer at `0x006FBCA4` is loader-zero in the
canonical PE and is published/cleared by current scene transitions before the
shared scene delegate calls virtual slot 14.  Keep this separate from the
battle phase block at `0x006FBCB4`.

- The battle-scene/network-lifetime wave adds twelve authored canonical-exact functions / 1,075 bytes. Current RTTI proves `CBattle -> IScene`, `CBattleSV/CL/Watch -> CBattle`, and `CLoadingWatch -> IScene`. Exact lifetime bodies are `CBattle` ctor/dtor/scalar (9/87/30) plus three 18-byte derived ctors; exact scene methods are base update/exit (72/20), folded SV/CL enter 193, Watch enter/update 230/171, and CLoadingWatch update 209. The SV and CL source symbols independently compare 193/193 against the one linked target body.
- Two independent network scene-enter functions prove a reusable 8-byte RAII wait object `{HANDLE handle; DWORD wait_ms}`. The second dword is initialized to 16 even though VC8 constant-propagates the actual `WaitForSingleObject` argument; `/GS` plus the real destructor-driven `CloseHandle` EH lifetime reproduces 193/193 and 230/230 without hand-written SEH. The three derived CBattle ctors provide a separate TU-visibility reference: same-TU base-ctor visibility inlines the 9-byte base constructor, while a declaration-only base in the derived-ctor TU naturally gives the target 18-byte bodies.
- `CBattleWatch::update` and `CLoadingWatch::update` are control/evaluation-order references. The Watch target owns shared return/prepare tails and a normal-path fallthrough into `while (continue_transition())`; LoadingWatch preserves pre-C++17 member-call receiver/argument evaluation ordering and an explicit state-1 branch. SV/CL updates remain pending despite closed semantics because their later receiver/cross-jump scheduling still differs; do not force them with volatile, registers, fake calls, or artificial CFG.

### ProfileMenu state-machine island and post-RET switch tables

The ProfileMenu recovery now includes exact state-four/state-five bodies
`0x0044CC50/0x0044CDE0` (371 bytes each), virtual dispatcher `0x0044D240`
(227), and profile confirmation helper `0x0043F870` (91), adding 1,060 authored
bytes.  Existing exact state-six, ctor, render and scalar-dtor bodies independently
anchor the class layout.  State four/five are true mirror workflows: they share
profile-pair selection, temporary-string assignment, confirmation/message gates,
and differ only in their copy/delete prompt suffix and final bool commit member.

Do not equate a VC8 COMDAT section tail with the authored function boundary.
Both 371-byte state methods live in 388-byte `.text` sections because their
17-byte switch tables follow the RET; the 227-byte dispatcher similarly lives in
a 256-byte section with 29 bytes of local table/alignment data.  The comparator's
reviewed ledger-size boundary is intentional and still resolves local DIR32 case
labels in the trailing table for codegen auditing.

The confirmation helper also corrects an ownership temptation: keep
`g_active_menu_input @ 0x006FD030` as the already-established independent input
global.  Only the loader-zero message UiDesignObject pointer at `0x006FD02C` is
newly named.  Address adjacency alone is not enough to collapse these globals
into one source aggregate, even though neighboring message-state storage is
layout-compatible with MenuCursorState.

- The Profile UI global lifetime now has a second exact-backed `MenuCursorState`
  at `0x006FD018`, distinct from the profile-list cursor at `0x006FCFFC`.
  `initialize_profile_ui @ 0x0043FCA0` is 666/666 exact from a real 0x128-byte
  configuration record, four design/list resources, and two texture loads;
  `render_profile_ui_frame @ 0x00440170` is 359/359 exact and proves the shared
  selection-menu container is traversed with checked `std::list::back()`;
  `shutdown_profile_ui @ 0x004402E0` is 129/129 exact and closes the matching
  design/texture teardown.  The initializer must publish its input pointer to
  the global and later reread it rather than keep the argument live across the
  function; that lifetime difference is byte-significant under VC8.

- Profile submenu lifetime recovery closes a natural `CProfileDeckEdit` size of 0x6B0 without padding arrays: Title/UI members lead into a 0x20 `DwordDeque4 + SpellTree` resource group at +0x398, checked `map<unsigned short,signed char>` at +0x3C0, checked `deque<map::iterator>` at +0x3CC, two `MenuCursorState` values at +0x3E4/+0x3F8, four state bytes, and `GuideOverlay[4] @ +0x410`. Natural import/export are 395/159 exact and the normal dtor is 353 exact. The same compile emits matching STL tree/deque helpers; keep generated ownership separate from authored callers. `CProfileCharacterSelect` size 0x288 and `CProfileKeyConfig` size 0x330 are independently vtable/ctor/dtor-backed; their exact lifetime members add 164/30/30 bytes while the 188-byte KeyConfig normal dtor deliberately remains pending on one scheduler-only swap.

- `Fighter::try_dispatch_action_202 @ 0x004940E0` and `try_dispatch_action_203 @ 0x004941D0` are a mirrored 226-byte pair. Current IDA shows the successful tail as `phase_reset; load vptr; load vslot[2]; ++byte[this+0x47F]; virtual set_action`. A layout-backed `FighterActionRepeatView` that exposes both the real third virtual slot and the observed repeat byte makes VC8 preserve that evaluation order; both bodies are canonical exact while the six earlier functions in the same TU remain exact. Keep the pair together when changing this view.
- Three near-exact retained-source blockers are useful negative evidence for future waves: `adjust_capped_counter_558 @ 0x0045CED0` is 126 bytes and differs only in register scheduling around the 16-bit adjusted amount; IDA shows the join as adjusted value in EAX, sign-extended counter in EDX. `GuideOverlay::update @ 0x0043F450` is 112 bytes and target uses `sub al,0x0F` plus a vslot load before alpha writeback, unlike VC8's current equivalent `add al,0xF1` scheduling. `try_group_a_vs_group_b_interaction @ 0x0046C930` is 164 bytes and the remaining semantic-width difference is target `movzx r32, word [frame+0x2A]` before a 16-bit store versus the current direct 16-bit load/store. Do not close any of these with volatile/register/assembly forcing; recover the source/layout distinction that naturally explains the target schedule.

### MenuConfig exact state machine and typed side effects

`CMenuConfig::update @ 0x004411B0` is a 665-byte current RTTI-owned virtual. Exact source keeps accept/cancel in the final `else` of the primary/secondary cursor chain; moving those tests after a successful secondary update extends the input-pointer lifetime and changes callee-saved allocation. BGM/SE percentages and grid width are unsigned, selecting the target multiply/shift division by 100. The four option indicators use explicit `if/else` white-versus-gray branches; a ternary chooses a different branchless lowering. The VC8 COMDAT continues past the reviewed RET into a local switch table, so authored bytes stop at 665 rather than the section tail.

The paired `MenuCursorState` binders `0x004204E0/0x00420520` are 63/63. They are real thiscall members: horizontal binds `input ? &input->hold.horizontal : 0`, vertical binds `input ? &input->hold.vertical : 0`, and both reset selection/window state while storing item/page counts.

Config audio scaling preserves literal **type**. Target qword `0x006C1660` is the double promotion of `0.01f`, not mathematical double `0.01`; using the latter gives matching arithmetic instructions but the wrong pooled relocation literal. `apply_config_se_percent @ 0x0043B1F0` walks the full 128-entry handle array `0x006FCA98..0x006FCC98`, establishing a verified `+0x200` addend on the existing aggregate. Its loop uses the target's signed address comparison and returns the final per-handle setter result. `close_config_menu @ 0x0043CDD0` similarly proves `0x006FBFB8/0x006FC2F4` are source objects whose addresses are passed to reset receivers; declaring pointer variables inserts loads and grows 48 bytes to 51.

### SpellData member ABI and generated erase equivalence

`SpellDataOwner::set_sequence_mode @ 0x00432520` is a 33-byte thiscall member taking an unsigned-byte mode. The historical free/stdcall hypothesis was wrong: Fighter initialization forms `ECX = Fighter+0x4F0`, and the callee accesses `SpellDataOwner+0x20/+0x34`. Ordinary `std::deque<short>` assignment plus `if (mode) shuffle_selection()` is 33/33. Making this definition visible in the Fighter initialization TU lets VC8 inline it, while the target keeps the out-of-line call, so preserve a declaration-only TU boundary there. `initialize_side_payload @ 0x004324F0` remains 35/35 after removing its historical `__declspec(noinline)` reconstruction debt.

The adjacent 108-byte `0x004323B0` body is compiler-generated `std::deque<short>::erase(iterator)`. A pinned VC8 probe regenerates a 108-byte / 41-instruction COMDAT with structural similarity 1.0000. Full 4,010-span replay finds the complete equivalence group `0x004323B0/0x00443A30/0x0045DC20/0x00617B50`; the committed manifest must resolve all four or fail closed. This is another case where review-only discovery cannot establish origin uniqueness.

Three near-exact bodies remain intentionally pending. `SpellDataOwner::shuffle_selection @ 0x00432420` reaches 196/196 with normal checked-iterator semantics and an out-of-line generated erase contract, but seven bytes differ only in scheduling an independent stack adjustment and saved short value. `initialize_fighter_battle_state @ 0x00460200` reaches the target 1247-byte / 290-instruction shape after correcting the SpellData member ABI, with only 29 allocator/scheduler bytes remaining. `CMenuConfig::update_secondary_for_primary @ 0x00440FF0` closes its five-case semantics and binder ABI but pinned VC8 still folds the target's redundant clamp topology. Do not use register variables, volatile/dummy dependencies, artificial gotos, inline assembly, or copied bytes to close these residuals.

### CScript command registration: pair conversion lifetime is authored codegen evidence

`CScenarioData::CScenarioData @ 0x00459C90` initializes a script registry at
`this+0x35C` and immediately registers the scenario command vocabulary.  Its
call sites plus the factory RTTI recover one authored template family rather
than twelve unrelated stripped functions: `CScript::register_command<Command>`
at `0x00452ED0/0x00453060/0x004531F0/0x00453380/0x00453510/0x004536A0/
0x00453830/0x00457170/0x00457300/0x00457490/0x00457620/0x004577B0/
0x00457940/0x00457AD0/0x00457C60`.
Every specialization is 385 bytes and the current factory vtable identifies its
`Command` type independently.  The family covers `CCommand0`, `CComCharacter`,
and the observed `TCommand1/2/3/4/5` combinations used by commands including
Character, Face, Action, Object, CrossFadeBgm, PlaySE, CG, Font, and Window.

Only the registry prefix is currently named: a checked
`std::map<std::string, CScript::CCommandFactoryBase *> @ +0x00` followed by the
default factory pointer at `+0x0C`.  Empty command names replace that default
factory; named commands allocate a two-dword polymorphic
`CCommandFactory<Command>` and insert it into the map.  All fifteen target bodies share folded VC8 EH handler `0x006BAAA8`.  The three
additional timeline specializations publish RTTI-owned factory vtables
`0x006C25F8/0x006C2604/0x006C2610` for
`TCommand5<int,string,int,int,int>`, `TCommand5<int,float,float,float,int>`,
and `TCommand5<int,float,float,int,int>` respectively.

The exact source-shape detail is reusable for other checked-map recovery.  The
named insertion must be written as an *unnamed non-const pair expression*:

```
commands.insert(
    std::pair<std::string, FactoryBase *>(name, factory));
```

That source has one `pair<string, FactoryBase*>` expression temporary and one
conversion into the map's `pair<const string, FactoryBase*>` value type, which
naturally reproduces the target's two string/value copies and shared destruction
tail.  Direct `map::value_type(name, factory)` collapses one copy and emits 286
bytes; `std::make_pair` adds another return temporary and emits 395; a named
`std::pair` local emits 391 and introduces an additional `/GS` lifetime.  The
unnamed explicit `std::pair` form produces 385/385 and 117/117 instructions for
all fifteen specializations under ordinary pinned VC8 `/O2 /GS`, with no
register/volatile/assembly forcing.


`SceneTimelineRegistry::register_builtin_commands @ 0x00453BC0` closes the
shared Opening/StaffRoll command vocabulary on top of the same template family.
Opening constructs this object at its `+0x08` subobject and StaffRoll at `+0x04`;
`0x004540F0` independently proves the embedded `CScript` begins at timeline
`+0x18`.  Natural source emits the target 721-byte body exactly while registering
`bpm(0)`, `bgm(2)`, `pic(1)`, transform channels `x/y/u/v/sx/sy/angle`, color
channels `r/g/b/alpha`, and `play/interval/notify/end`.  The current `.rdata`
literals form one contiguous target-backed table at `0x006C2684..0x006C26E0`;
the canonical literal validation caught and corrected an early `pic`/`bpm`
source swap even though relocation-masked code bytes were otherwise identical.

Exact-TU origin replay from this registry is also reusable denominator evidence.
A fresh pinned `/GS` build regenerates nine checked `std::_Tree`/`std::less`
COMDAT fingerprints whose **complete 4,010-candidate hit sets** contain 19
review functions / 3,876 bytes.  A separate exact CharacterObjectEx destructor
TU regenerates checked deque pop/copy/copy-backward/erase fingerprints covering
6 review functions / 1,304 bytes.  The committed manifests require compiler and
target SHA pins, relocation-masked non-relocation coverage, optional one-byte
`0xCC` tails, and exact equivalence-group hit sets.  Critically, the 385-byte
`register_command` bodies are absent from the generated rules: the same scan
identified the three missing TCommand5 specializations as authored rather than
using clone shape to erase them from the denominator.


### Scenario core layout, result-resource island, and member-pointer ABI

Current `CScenarioData` vtable ownership and constructor/unwind paths now close a **0x524 physical object**, correcting the earlier 0x510 prefix-only claim. The stable layout is `SystemEffectManager<SystemEffectObject> +0x004` (0x60), scenario id `+0x064`, `ScenarioEffectOwner194 +0x068`, `ScenarioEffectConfig128 +0x1FC`, state dwords `+0x324/+0x328`, checked `map<string, ScenarioTextEntryDC> +0x32C`, three checked `list<ScenarioRenderEntry9C>` values at `+0x338/+0x344/+0x350`, `CScript +0x35C`, `ScenarioResultResource +0x390`, strings at `+0x4B8/+0x4D4/+0x4F0`, state bytes `+0x50C..+0x511`, and runtime dwords `+0x514/+0x518/+0x51C/+0x520`. The size is independently cross-checked by the containing battle-manager layout: `CBattleManager` constructs this subobject at `+0x8C`, while already-exact Arcade-derived storage begins at `+0x5B0`; `0x8C + 0x524 == 0x5B0`. The old +4 string shift came from a false 0x12C result-owner hypothesis; current ctor stores and EH unwind independently prove the result owner is 0x128.



The text-entry subobject is also more specific than the old facade. `ScenarioTextEntryDC` is 0xDC and contains a **real polymorphic 0x94 sprite member at +0x04**, a string at +0x98, four bytes at +0xB4, seven ints at +0xB8, and six signed state bytes from +0xD4. That static polymorphic type is byte-significant: `ScenarioTextEntry::render_dynamic @ 0x00454DA0` is 171/171 only when VC8 knows the sprite member owns virtual slot 1 for color and slot 7 for render. A hand-written function-pointer facade produces 175/182-byte variants and loses the target vptr lifetime. The paired per-frame state machine `update_dynamic_state @ 0x00454E50` is 107/107 with ordinary integer control flow; these two methods jointly anchor the text-entry tail used by the 6,121-byte Scenario interpreter.

`CScenarioData::configure @ 0x00454FE0` (90/90) and `set_effect_color_bytes @ 0x00454EC0` (68/68) show another reusable lifetime rule. Keep a real `ScenarioEffectConfig128 *config = &effect_config_1fc` live across the six byte stores and the owner call. VC8 then naturally keeps the config receiver in EAX and reuses CL for the color bytes, matching the target; spelling every store through the owning member changes call preparation even though the data is identical.

`CScenarioData::reset_runtime_state @ 0x00457E40` remains review-pending at 339/334 after recovering its full semantics and out-of-line STL visibility. The target calls the checked text-map iterator increment at `0x00435C00`, `std::string::assign(const char*,size_type) @ 0x004021C0`, and the Scenario render-list clear path `0x00435FF0`; letting those bodies inline grows the function to 711/823 bytes. Declaration-only member ABI gates recover the real TU partition, leaving only a five-byte-level checked-iterator/register-lifetime residual. Explicit saved-end iterators and global `/Ob0`/`/Ob1` experiments are worse. Do not force EDI/ESI/EBP with fake locals or register annotations.

`CComCharacter` is the custom authored bridge between the exact CScript registry and the Scenario command interpreter, not a generic `TCommandN` clone. RTTI names the two-slot vtable as `.?AVCComCharacter@@`; the physical command is 0x30 bytes with `std::string +0x08`, ints `+0x24/+0x28`, byte `+0x2C`, and RGB bytes `+0x2D..+0x2F`. `CComCharacter::parse @ 0x00455EF0` is 505/505 when the input `char *` parameter is reused as the `strtok_s` context, and the inline hex-nibble helper takes/returns ordinary **signed `char`** while doing range tests through unsigned-byte casts. Making that helper unsigned-char shortens the body to 500 and changes the second-nibble temporary allocation. This is source type evidence, not register forcing.

`CScript` is a complete 0x34 layout: checked command map `+0x00`, default factory `+0x0C`, intentionally unnamed/uninitialized cell `+0x10`, checked pending-factory deque `+0x14`, and runtime cells `+0x28/+0x2C/+0x30`. `CScript::CScript @ 0x00405600` is 65/65 only when runtime cells are member-initialized and `default_factory_0c = 0` occurs in the ctor body afterward. All fifteen 385-byte `register_command` specializations remain exact with the full header. Its dtor/clear lane still calls target-private helpers (`0x00405CD0` consumes EAX, `0x00405770` consumes ESI); do not fake those ABIs.

`ScenarioResultResource` is a reusable 0x128 core owner: `CDesignBase +0x08`, four primary pointers `+0x3C`, three secondary pointers `+0x4C`, texture `+0x58`, `UiTileA4 +0x5C`, values `+0x100`, colors `+0x10C`, scales `+0x118`, and alpha `+0x124`. Seven methods are canonical exact from this layout: `0x00454780` (76), `0x004547E0` (93), `0x00454840` (67), `0x00454890` (187), `0x00454950` (141), `0x004549E0` (99), and `/GS` dtor `0x00454BF0` (116). Exact reset code loads each position-object pointer once and reuses it for x/y stores; exact render walks the parallel arrays with one `UiTileA4 *` plus one `float *scale` cursor. A normal index loop changes VC8 lifetime/codegen. The 93-byte nonnegative adder preserves target source data flow `arg + value < 0 ? 0 : arg + value`; replacing it with `+=` plus clamp compiles shorter. `set_entry_level` is caller-backed `void`; its exact double literals are 7.5 at `0x006C2710` and 1.0 at `0x006CD160`.

Object-manager invoke-each calls such as `0x0058BEE0` take an MSVC multiple-inheritance pointer-to-member function, not a free callback plus invented offset. `SystemEffectObjectBase` is multiply inherited; a normal `void (SystemEffectObjectBase::*)()` declaration naturally emits the two-word `{virtual thunk,this-adjustor}` representation seen in current callers.

Large neighbors remain deliberate stops: `CScenarioData` ctor `0x00459C90` reaches 1528/1527 with 404/404 instructions after the real long-lived config lifetime and stops on persistent EDI/EBP allocation; `CScenarioData::render @ 0x00457F90` reaches 930/930 and 281/281 after recovering the true 0x9C list value, reused checked-list iterator, and member-pointer ABI but stops on EBX/EBP allocation; `reset_runtime_state @ 0x00457E40` is 339/334 after the correct out-of-line STL partition and stops on checked-iterator allocation; result update `0x00454A50` is 403/403 after correcting phases to 0/1/3 and phase-1 increment order; result ctor `0x00454C70` is 298/298 with six CTile/EH scheduling bytes; SystemEffect release `0x00436E60` is 119/119 with only EAX/EDX handle scheduling. Vslot 2 `CScenarioData::update @ 0x00458440` is the 6,121-byte command interpreter; its command registry, custom CComCharacter parser, text-entry update/render and effect-color/configure edges are now exact. Continue it by recovering real command object/source boundaries rather than transcribing decompiler output. Do not use register variables, volatile, fake locals, gotos, manual vtables, assembly, or copied bytes to close these.


### Shared 0x194 text/effect owner: typed layout versus private LTCG entry ABIs

Scenario and profile text paths share the same 0x194 owner shape already visible
through `ScenarioEffectOwner194`: 0x128 config at `+0x0C`, color-table pointers
`+0x154/+0x158`, checked payload list `+0x15C`, packed color `+0x168`, and
`std::string +0x170`.  The standalone config default constructor
`0x004113E0` naturally reproduces 67/67 with `duration=400`, three zero flags,
`interval=100000`, and zero mode/state/options, confirming that these repeated
writes in larger ctors are one real config-constructor contract.

Do not promote the rest of this island from standalone facades yet.  Physical
owner ctor/dtor/configure live at `0x00411430/0x004114C0/0x004115A0`; glyph/text
helpers around `0x004118F0..0x00411DF0` expose target-private EAX/ESI entry
assumptions and LTCG-visible STL/allocator bodies.  Ordinary `std::list` and
`std::allocator<unsigned>` source changes those call boundaries, while forcing
registers would violate the project rules.  Preserve the typed 0x194 layout and
config-constructor evidence, but continue only when a real owning TU recovers
the private ABI naturally.

### CHandleManagerEx release tokens: checked subscript visibility and dead-parameter reuse

`CHandleManagerEx<SystemEffectObject>::release_handle @ 0x0056D8F0` is the
canonical 170/170 physical release body shared by the object-manager lifetime
lane.  Current SystemEffect manager construction/destruction, the
`CHandleManagerEx<SystemEffectObject>` vtable, and the SystemEffect
`TObjectManagerBase` release caller independently identify the specialization;
the pinned VC8 object then compares byte-for-byte after relocation resolution.

The source contract is reusable.  A handle is one 32-bit value with a low
16-bit slot and high 16-bit generation, exactly mirroring the already-exact
handle acquire family.  The generation vector must use checked
`generations_14[index]` twice: under the pinned VC8 checked-STL configuration,
`operator[]` naturally inlines the two target bounds checks.  Replacing those
accesses with `.at()` emits an out-of-line call, while manually spelling the
checks gives a different register schedule.  The object slot deliberately uses
`slots_04.at(index)` instead; target `0x0056D450` is the out-of-line checked
pointer-vector accessor.

After virtual destruction, the slot index is assigned back into the now-dead
`handle_token` parameter and that parameter is passed by reference to
`free_slots_24.push_back`.  This is real source-storage lifetime, not stack
coloring: a separate address-taken index local introduces another stack slot
and changes the prologue.  With parameter reuse, VC8 naturally emits the target
list `_Buynode` / `_Incsize` sequence and all 170 bytes without asm, volatile,
register forcing, fake dependencies, or copied bytes.

The three higher-level `release_all_tracked_objects` bodies at `0x00436E60`,
`0x0046EEB0`, and `0x0056DA90` remain deliberate stops.  They are respectively
119/119, 122/122, and 122/122 with the same semantic checked-list loop and only
the handle-load register differing (target EAX, standalone VC8 EDX).  Making
the exact release_handle template visible in the same TU does not change that
residual.  Treat it as allocator/TU evidence; do not coerce the callers.

### Stable CTile 0xA4 class island across TH105 1.06 and 1.06a

The ScenarioResult render path exposed a high-leverage shared `CTile` island.
Current 1.06a owns `set_texture @ 0x004098B0` (37 bytes), the full texture/UV
initializer `0x004098E0` (266), and `render @ 0x004099F0` (756).  The pre-reset
TH105 1.06 ledger has the same three bodies at `0x00409410/0x00409440/
0x00409550` with identical sizes and relative spacing; the 37-byte forwarding
wrapper is raw-identical across versions.  This is a stable engine class, not a
Scenario-only facade.

Current target code plus the 164-byte physical copy-constructor probe establish
a natural 0xA4 layout: vptr `+0x00`, signed texture handle `+0x04`, four 0x1C
vertices from `+0x08`, float size `+0x78/+0x7C`, anchor `+0x80/+0x84`, scale
`+0x88/+0x8C`, rotation `+0x90`, UV origin `+0x94/+0x98`, and UV step
`+0x9C/+0xA0`.  Vtable `0x006C0718` has only destructor plus the three IColor
methods; the texture setters and render method are non-virtual.  Declaring the
37-byte wrapper virtual changes it to an indirect 39-byte body and is rejected.

The 266-byte full setter also proves authored parameter-slot reuse.  After
publishing the requested tile size/anchor, it reuses dead incoming argument
slots as the two outputs of the texture-size query and later reads those slots
as unsigned texture dimensions.  This is the same legitimate source-lifetime
pattern seen in the exact HandleManager release path.  The setter initializes
all four vertex colors, normalized UV origin/step, z=0.5f, rhw=1.0f, unit scale,
and zero rotation.

Do not promote the two large CTile methods yet.  Ordinary pinned `/O2` reaches
242/266 for the full setter and roughly 734/756 for render because VC8 CSEs
texture-dimension conversions and chooses different x87 lifetimes.  A bounded
profile diagnostic found `/fp:strict` highly informative: the full setter grows
to 270 bytes with a 204-byte exact non-relocation prefix and reproduces all four
independent unsigned-dimension conversions; render reaches 754/756.  Its final
constant-store/x87 schedule still differs, while `/fp:fast`, `/O1`, `/O2 /Os`,
and `/fp:except` are clearly wrong.  Keep `/fp:strict` as TU-profile evidence,
not an acceptance shortcut, and continue from the real CTile class/parameter
lifetime rather than register, volatile, fake-local, or assembly experiments.

### Render-state policy, BGM playback, and shared timeline recovery

The RenderModeManager state-policy island now closes three more authored bodies / 354 bytes. `set_blend_mode @ 0x004047E0` is 256/256 from ordinary C++ using the target transition key `new_mode + 8 * old_mode - 1`; the four stable blend tuples are `(BLENDOP,SRC,DST) = (1,5,6), (1,5,2), (3,5,2), (1,1,3)`. Source fallthrough is real byte evidence: transitions `0 -> 32 -> 16`, `1 -> 33 -> 9`, and `3/27 -> 11/19` share state updates, and placing case 25 before cases 2/34 reproduces the target physical block order. VC8 then emits the exact 256-byte callable plus the same compressed jump/index tables without a manual jump table, asm, volatile state, or register forcing. `set_gate_states @ 0x004049C0` (65) and `set_state_23 @ 0x00404A10` (33) are ordinary SetRenderState-and-cache leaves owned by current render initialization/battle state callers.

The BGM playback/service lane adds five authored bodies / 420 bytes. `load_bgm_source @ 0x0043CF00` (123) and `play_bgm @ 0x0043CF80` (168) use a real four-byte `BgmHandleResult` return from `allocate_handle @ 0x004036C0`; VC8 naturally reuses the dead input path slot as hidden return storage, matching the target without parameter aliasing. `play_bgm` must keep the non-null path as the hot fallthrough and the null `schedule(handle,0)` path at the cold tail; an early-null return shortens the body. `BgmService::link_handles @ 0x004039E0` is 73/73 with the service lock at +0x20 and handle manager at +0x38. BGM construction `0x00403460` independently owns the two 28-byte stdcall worker entries `0x00403B50/0x00403B70` by passing them to CreateThread; both are ordinary `CoInitialize(0) -> worker -> CoUninitialize` wrappers, with their OLE32 IAT slots fail-closed in the relocation ledger.

Opening and Ending share one typed render-timeline island rather than unrelated facades. `RenderTimelineList::render @ 0x00450FC0` is 145/145 with native checked `std::list<RenderTimelineEntry>` at +0x58; exact Opening/Ending callers independently reach the same body. `RenderTimelineEntry::render @ 0x00450EA0` is 282/282 and contains native checked `std::deque<CSpriteEx>` at +0x14. This element type matters: the already-origin-proven generated `operator[] @ 0x004399C0` is the block-size-one large-element form and returns the `CSpriteEx` element address directly; modeling a pointer deque adds a false dereference. A real `float y_scale` is loaded before `scale_x` and remains live across that call, creating the target four-byte spill before `scale_y`. This is a source lifetime, not artificial stack padding.

Several nearby bodies remain useful stop conditions. BGM `schedule_or_start @ 0x00403840` is 142/143 with only lock/delay register allocation different even after naming the real lock pointer. BGM `stop @ 0x004038D0` is 96/98 because standalone VC8 CSEs the stream buffer pointer. BGM/SE master-volume bodies reproduce their business/DirectSound loops but retain target-only 8-byte-aligned EBP frames; `/fp:strict`, double clamp locals, saved list-end iterators, and `/Oy-` diagnostics are all wrong. RenderModeManager `set_pair_state @ 0x00404940` remains a first/second-byte lifetime permutation. Do not use fake aligned locals, register variables, volatile state, inline assembly, or private calling-convention inventions to erase these residuals.

### BattlePhaseBlock Story-script layout and temporary lifetime

The Story-script runtime closes two authored BattlePhaseBlock members / 430 bytes from one target-backed physical layout. `advance_scenario_script_page @ 0x0045A2E0` is 118/118 and `initialize_character_story_script @ 0x0045A4E0` is 312/312. The checked `String28` begins at object `+0`, not behind a fabricated vptr; its SSO state therefore lands at the target `+0x04/+0x18`. `CsvReader` begins at `+0x38`, the script subobject at `+0x68`, selected input/column at `+0x58C/+0x590`, and page/story bytes at `+0x594/+0x595`. A real `char path[260]` in the page advance method explains the target `/GS` frame naturally.

Temporary lifetime is byte-visible in the 312-byte initializer. The exact source is one chained expression, `"data/csv/" + character_name + "/story.csv"`, assigned to the final `String28 full_path`. VC8 then treats only the first `operator+` result as the short-lived intermediate and reproduces the target EH states. Naming both `prefix_path` and `full_path` keeps an extra String28 lifetime and emits 316 bytes. Prefer the expression/lifetime proved by the target over manual stack-slot reuse.

The adjacent `parse_scenario_event_row @ 0x0045A7A0` remains deliberately pending. `/GS` plus reuse of the real `selected_value` int for the later source-list value reduces the fresh frame from `0x38` to the target `0x34`, proving one original local lifetime. Pointer/reference/state/reader declaration variants still leave only the target-EDI versus fresh-EBX destination/zero allocation difference. Keep that positive source fact, but do not finish the parser with register forcing, volatile state, dummy locals, or assembly.

### FighterPhaseContext native checked-vector phase family

The five current FighterPhaseContext phases at `0x00464840/0x00464990/0x00464A20/0x00464A80/0x00464AD0` are now 334/133/87/71/71 canonical exact from one ordinary C++ TU. Exact `run_shared_battle_phase_sequence` independently owns all five calls. The critical layout fact is that the **VC8 checked `std::vector<Fighter*>` object starts at context `+0x38`**. Its checked-build proxy/allocator prefix occupies the first dword, so the target's observed begin/end pointers appear at `+0x3C/+0x40`. Treating `+0x3C` as the vector object base shifts every field by four bytes; one corrected member offset fixes all five bodies and the exact caller cold-replays unchanged.

`phase_463820` adds two source-type/order constraints that are independently visible in the target. Fighter action state at `+0x13C` is signed-short compared against 300, and the shared ordering cell `0x006FD38C` is a **signed char** (`movsx` on both later reads). The target keeps the `action1 >= 300` branch as the physical first block; ordinary equivalent source in that order makes VC8 emit the exact threshold CFG. The phase then uses exact RenderModeManager gate-7/gate-14 and state-23 setters, dispatches Fighter virtual slot `+0x2C` in the selected order, clears the gates, and visits Fighters 2..N.

The four siblings are reusable evidence for the same checked-vector contract: `phase_463970` walks Fighter `+0x658` subobject vslot 6 then Fighter vslot 12; `phase_463a00` forwards its signed phase value to subobject vslot 5; `phase_463a60` forwards each Fighter `+0x674` embedded render-state to `0x0045AA00`; and `phase_463ab0` forwards Fighter `+0x3D0` state to current finalizer `0x004653F0`. Keep these as normal high-level vector loops rather than lowering them into manual begin/end arithmetic.

### Fighter +0x674 render-state and BackgroundBase lifetime wave (2026-08-27)

The Fighter phase render lane is now a typed application owner rather than a
callee-only offset. `FighterPhaseRenderState::render @ 0x0045AA00` is 333/333
and `enqueue @ 0x0045AD40` is 240/240. Exact
`FighterPhaseContext::phase_463a60 @ 0x00464A80` independently owns the state at
Fighter `+0x674`. Its native checked `std::list<PhaseRenderEntry>` begins at
state `+0`; one entry is a real `CSpriteEx` followed by a signed blend byte at
entry `+0xE8`. State fields are signed period `+0x0C`, signed-short alpha
divisor `+0x0E`, and color mask `+0x10`. Render keeps one real local
`CSpriteEx`, selects blend modes through exact `RenderModeManager`, copies the
entry sprite, derives alpha from the source high color byte and list index,
then calls exact battle-layout transform and sprite finalization. Enqueue is
ordinary checked-list `push_back` / `back` / `pop_front`; its linked
`_Buynode`, `_Incsize`, assignment and erase boundaries remain generated/helper
evidence rather than authored progress.

A trivial base constructor can be byte-significant TU visibility. The local
`CSpriteEx` in the 333-byte renderer reaches the target only when the truthful
`IColor() : value_04(0) {}` definition is visible in the including header.
Moving that trivial constructor from the old out-of-line TU into the class body
leaves the already-exact CEffectSprite/IColor lifetime unit unchanged while
allowing pinned VC8 to emit the target local sprite construction naturally.
This is source-partition evidence; do not replace it with `forceinline`,
register variables, duplicated external definitions, or assembly.

`BackgroundBase` is independently closed as a **0x64 polymorphic owner**. The
original ctor/dtor wave proved the actively touched 0x5C prefix: checked
`vector<unsigned> +0x04`, `vector<CSpriteEx> +0x14`, a second handle vector
`+0x24`, a second sprite vector `+0x34`, and six floats `+0x44..+0x58`.
Current factory allocation, `set_transition @ 0x004654F0`, and the exact shared
renderer now extend the physical class with signed transition state `+0x5C`
and float transition value `+0x60`. Ctor/dtor/scalar at
`0x00466020/0x00465E10/0x00465FD0` remain 115/377/30 exact because those two
tail fields are intentionally not initialized by the base ctor. Destruction releases handles from the second lane then the
first through exact TitleResourceManager, after which ordinary reverse member
destruction reproduces the target CSpriteEx/vector teardown. The constructor's
six zero floats are one chained C++ assignment,
`v44 = v48 = v4c = v50 = v54 = v58 = 0.0f`. Right-associative assignment makes
VC8 emit one `fldz` and target-observed reverse `fst` stores `+0x58 -> +0x44`.
Independent initializer-list assignments store in member order and are wrong.
The nearby `0x00465F90` body remains SHA-pinned compiler-generated
`vector<CSpriteEx>` machinery and must not be counted as authored merely because
this class now proves the vector element type.

### BattleObjectManager native owner, phase identity, and worker lifetime (2026-08-27)

Current 1.06a closes `BattleObjectManager` as one ordinary **0x118-byte C++ owner**, not a collection of offset facades. The physical member sequence is checked `std::list<BattleRenderRequest> @ +0`, two `BattleThreadHandle` members at `+0x10/+0x18`, event handles `+0x20/+0x24`, checked `std::list<BackgroundBase*> @ +0x28`, checked `std::list<BattleOwnedObject*> @ +0x34`, `BattleBaseEffect @ +0x40`, render state through `+0xD7`, and texture handles `+0xD8/+0xDC`. Natural `/GS` construction/destruction plus the phase/worker methods reproduce nine authored bodies / 1,258 bytes exactly.

Do not reuse the historical `phase_465b00/phase_465b70/phase_465bd0` labels as target addresses. Exact `run_shared_battle_phase_sequence @ 0x0046BE30` calls `0x00466980`, `0x004669F0`, and tail-calls `0x00466A50`; those are the actual manager phases. Current `0x00465B50/0x00465B90/0x00465BD0` are unrelated generated/helper bodies. Caller-backed target identity should override mnemonic/address names inherited from an older reconstruction. The three exact phases are normal checked render-entry list walks around already-identified EventEffectState calls.

Constructor lowering preserves **container specialization identity even when later teardown folds**. `list<BattleRenderRequest>` needs a 0x10-byte node and calls `_Buynode @ 0x00464B20`; the two pointer lists use 0x0C-byte nodes whose `_Buynode` folds at `0x00435E50`. In the destructor, all three element types have trivial destruction, so their `_Tidy` specializations can ICF-fold to `0x00435EB0`. Never infer node size or element type from a folded destructor helper alone; pair constructor allocation extent with typed consumers. The ctor then naturally builds two thread handles, the BattleBaseEffect, the 0x4D0 effect blob, two events, both worker entries and priority -15.

The two worker lanes are also source-partition evidence. `manager_worker_loop @ 0x00466C70` waits on event `+0x24` and drains `list<BattleOwnedObject*>`; `render_worker_loop @ 0x004677B0` waits on `+0x20`, forwards each `{unsigned,float}` request to pending `dispatch_request @ 0x00467380`, then pops it. Their 32-byte stdcall entries are ordinary `CoInitialize(0) -> member loop -> CoUninitialize` functions and are independently passed as function pointers by the exact ctor. An exact caller proves the dispatch address/ABI, **not** the dispatch body itself; keep the 986-byte `0x00467380` root pending until it independently canonical-compares.

Deleting background pointers is target-backed virtual ownership: `BackgroundBase` vslot 0 is its virtual destructor, followed by pure virtual phase/render slots `+0x04/+0x08/+0x0C/+0x10/+0x14`. This shared type is validated by the 379-byte manager destructor, 51-byte `dispatch_render_sprite`, exact BackgroundBase lifetime, and the later 986-byte factory; cold-replay all of them when changing the interface.

### Background factory exactness can reveal the real list static type (2026-08-27)

`BattleObjectManager::dispatch_request @ 0x00467380` is a useful example of recovering a **higher-level static type from a stack temporary**. Current target and RTTI now establish a 0x64 polymorphic `BackgroundBase`: scalar deleting destructor at vslot 0 and five purecall slots at `+0x04..+0x14`. The manager's +0x28 list was initially modeled as `std::list<BattleObjectRenderEntry*>`, which is layout-compatible and was sufficient for the earlier 51-byte back()/render call and phase loops. But a typed factory creating `BGCommon/BG02/BG04/BG16` then has to convert the derived pointer to the generic base type before binding `push_back(const T&)`; pinned VC8 materializes that converted pointer in an extra 4-byte local. Target has no such slot. Changing only the list static element type to the already-proven `BackgroundBase*` removes the temporary and makes the 986-byte factory exact **while every pre-existing BackgroundBase/manager/render caller remains exact**. Treat cross-unit elimination of a language-mandated temporary as stronger type evidence than an offset-compatible facade.

The factory itself is ordinary C++ rather than a handwritten allocator. Switch cases 0..18 use normal `new`: BGCommon has target allocation extent 0x64 and BG02/BG04/BG16 have 0x68; their constructors stay declaration-only and out of this TU. Pinned VC8 naturally emits the target 17 EH cleanup states and external constructor calls. The target-proved tail is source-order-sensitive: nonempty `renderers_28` is the fallthrough, empty is cold; within the nonempty arm, `value != 0.0f` is the fallthrough state-0 transition while zero selects state 1. This second spelling also preserves the target x87 parity/NaN behavior. Existing backgrounds receive state 2 with single-precision -0.01f before native checked `list<BackgroundBase*>::push_back`. With those semantic choices all 986 callable bytes match; the same COMDAT contains jump/EH tables beyond the reviewed function boundary and they are not counted as authored function bytes.

`BattleObjectManager::publish_stage @ 0x00467850` closes the caller side of the same owner. Exact `BattleAudioSetupView::set_stage_music_433860` ignores EAX, so the truthful member ABI is `void(int stage, int count, bool notify)`. The method stores the low stage byte to `g_battle_transition_mode`, computes `count ? 1.0f / count : 0.0f`, and uses the manager's real list sizes: only `notify && !renderers_28.empty() && count > 0` creates a `BattleRenderRequest`, calls the generated `list<BattleRenderRequest>::push_back @ 0x00466DB0`, and signals event `+0x20`; all other cases synchronously call the exact dispatcher. Ordinary source gives 120/120 and the 40-byte caller remains exact. This is also an evidence boundary: the generated push_back/_Incsize helpers are not authored simply because an authored exact caller reaches them.

### BackgroundBase 0x64 runtime and derived-vtable wave (2026-08-27)

The background runtime continuation closes **10 authored functions / 827 bytes**
without changing the evidence bar. `BackgroundBase::render_base_sprites @
0x004659E0` is 362/362 exact. Its source is a signed `int` 0..29 loop; checked
`std::vector::operator[]` still performs the implicit unsigned bounds compare.
Only the nonzero-handle arm creates a `CSpriteEx&`, which VC8 naturally carries
through reset/translate/placement/visibility/render while strength-reducing the
0xE8 element stride. The four visibility tests are written as failure
conditions (`x < 0`, `x > 640`, `y < 0`, `y > 480`) rather than one positive
conjunction. That preserves the target x87 parity/NaN behavior and is why the
final 362-byte body is exact.

`BGCommon::BGCommon @ 0x004694A0` is 97/97 from ordinary `/GS` C++ with an
out-of-line `initialize_common_4664a0`; this keeps the already-exact 986-byte
factory's constructor boundary intact. BG02/BG04/BG16/BGCommon vtables also
close four class-owned 36-byte scalar deleting wrappers. The target wrappers
require **user-defined empty derived destructors**: VC8 republishes the derived
vptr, calls `BackgroundBase::~BackgroundBase`, then conditionally deletes. This
is the opposite source choice from the CharacterObjectManager implicit-dtor
family and must be decided from the local vtable/lifetime evidence, not a global
style rule.

The physical 5-byte `++state_64` body is shared by BG02/BG04/BG16, and the
5-byte base-render forwarder is shared by BG04/BG16/BGCommon. BG02/BG04/BGCommon
also share the 107-byte sprite transform/color override while BG16 owns a
107-byte sibling differing only in final color (`0x40FFFFFF` versus
`0x80000000`). Tracked source keeps ordinary identical virtual definitions;
linker ICF explains the one physical target body. Count the physical target
function once rather than inventing aliases or deleting legitimate class
methods from source.

Two nearby roots remain deliberate stops. `set_transition @ 0x004654F0`
reaches the 407-byte target boundary only after recovering cached vector counts,
`if(count) do/while` loops, and **unsigned** alpha conversion (which naturally
emits the target control-word truncate + `fistp qword` sequence); only two real
stack slots are colored in the opposite order. `BG04::BG04 @ 0x00468570` is
335/335 and later work below closes its frame/local coloring, but a target-only
CEffectSprite-vs-CSpriteEx temporary identity remains. The old BG04 phase stop is
superseded by the exact phase wave below. Do not close the remaining roots with
fake stack objects, volatile/register forcing, manual vptr stores, inline
assembly, copied bytes, or relocation aliases between distinct RTTI classes.

## Result/network exact-reconstruction patterns (2026-08-27)

The current result-input/network island adds three ordinary-VC8 exact bodies at
`0x0043B300`, `0x0043B410`, and `0x0044DCB0`.  The useful lessons are broader
than these three functions:

- For a checked contiguous record accessor, mathematically equivalent unsigned
  predicates are not codegen-equivalent in VC8.  `count <= index` emitted
  `cmp count,index / ja`, while the target spelling was recovered by keeping
  the same fall-through invalid-parameter lifetime and writing
  `!(index < count)`, which emits `cmp index,count / jb`.  Do not replace the
  invalid branch with an early-return form: that changes register lifetimes and
  the whole prologue.
- Keep small checked accessors in their own translation unit when the target
  contains a real call edge.  Defining `ResultInputRecordTableView::at` in the
  caller TU caused VC8 to inline it into `prepare_result_match`; a declaration
  in the caller plus an out-of-line definition preserves the target ownership
  boundary naturally.
- Assignment ownership order can be enough to recover VC8 scheduling without
  fake dependencies.  In the 136-byte network result binder, publishing the
  primary/alternate selected-input pointers before the two enable flags makes
  VC8 emit the target `LEA -> flag stores -> vptr load -> selected stores`
  schedule.  Typed-layout facades alone did not change the mismatch.
- Large UI/network roots often reuse already-proven infrastructure verbatim.
  The 623-byte result finalizer uses the same 0x128 config / 0x194 text-owner
  pipeline and Shift-JIS label as the exact player-slot color path.  Modeling
  the two source fields as real C strings, composing the target markup with a
  real `std::string`, calling `TitleResourceManager::create_text_texture`, and
  using genuine virtual 0x94 sprite `set_texture` calls reproduces the complete
  `/GS` + EH body exactly.  Prefer this reuse over manual vtable arithmetic or
  copied decompiler temporaries.
- Absolute EH handlers and target-owned literals must be promoted into
  `reccmp-relocations.csv` with attested mapped bytes before canonical compare;
  a raw address override is intentionally insufficient for `dir32` proof.


### BG04/BG16 exact phase source-shape wave (2026-08-27)

Three substantial virtual roots close together for **2,940 authored bytes**:
`BG04::slot_08 @ 0x00468360` is 515/515, `BG16::slot_08 @ 0x00468D80` is
889/889, and `BG16::slot_0c @ 0x00468780` is 1536/1536.  All three are ordinary
`/O2` VC8 C++ over the already-proven 0x64 `BackgroundBase` / 0x68 stateful
background layout, checked `vector<CSpriteEx>` storage, real RenderModeManager,
and the current battle-layout object.  They do not use inline assembly,
register annotations, volatile state, dummy locals, manual vtables, copied
bytes, or fake ABI declarations.

The BG04 phase closes a reusable x87 lifetime rule.  A named one-use
`uv_width` and a named one-use translated-X value caused VC8 to reserve two
persistent four-byte frame slots, giving 519 bytes and `sub esp,8`.  Writing the
same calculations directly as the `set_uv_size(...)` and `translate(...)`
arguments lets VC8 reuse one floating spill slot and naturally emits the target
`push ecx` frame, with all 515 bytes exact.  Keep the short `CSpriteEx&` only
across the three scale calls, where the target really reuses the checked vector
reference.  A readability local is not free under x87; when a value has exactly
one consumer, target spill reuse is evidence for an unnamed expression rather
than permission to fabricate stack state.

`BG16::slot_0c` gives a stronger loop-recovery example.  Named angle and
translation temporaries first left a 4-byte frame hole; moving the one-use
values into call expressions restored the target 0x34 frame.  The second angle
must be written with single-precision source visibility so VC8 emits target
`fild -> fchs -> fdiv` rather than `fdiv -> fchs` or a folded negative-5.0
constant.  The decisive loop fact is that source should **not** carry an
explicit `angle_step` induction variable.  Write the semantic angle as
`i * 90 + state_64 * (i + 4)` inside a normal `for (int i=0; i<4; ++i)`.
Pinned VC8 strength-reduces `i * 90` into EBP, increments it by 90, keeps EBX as
the true array index, and canonicalizes the latch to `cmp ebp,360`; this
reproduces the otherwise-mysterious three-byte-longer target loop tail exactly.
When the target contains two related induction registers, first test whether
one is compiler-derived from a source multiplication before declaring two
source counters or trying to influence register allocation.

`BG16::slot_08` independently closes the same static types and constants over a
smaller mirrored phase: gate/state setup, orientation-sine Y offset, mirrored
X scale, placement, final sprite render and state restoration all reproduce
889/889.  Its success plus the 1536-byte sibling is strong cross-method evidence
that the recovered BG16 layout and battle-layout ownership are source-level,
not offset facades.

The neighboring `BG04::BG04 @ 0x00468570` remains a bounded stop for a more
specific reason than the old "frame +4" note.  Scoping texture width/height to
the load call and representing the two throwaway out-params as one 8-byte
aggregate recovers the exact 0xF8 target frame and exact scalar stack coloring.
The first remaining canonical mismatch is then the local fill object's vptr:
target stores RTTI-backed `CEffectSprite` vtable `0x006C072C`, while an ordinary
`CSpriteEx` fill stores RTTI-backed `CSpriteEx` vtable `0x006C06E8`.  MSVC RTTI
parsing confirms these are distinct types even though their leading virtual
slots overlap.  The target temporary exposes only an approximately 0xE8 base
slice although the complete CEffectSprite is 0x12C, suggesting inlined/LTCG
dead-tail elimination.  Do **not** map one vtable relocation to the other or
manually write the derived vptr; continue only with natural constructor/TU
visibility evidence.

Parallel bounded stops from the same pass should also be reused rather than
restarted.  `prepare_result_match @ 0x0043B8F0` reaches the exact 286-byte
boundary when mode 6 falls through to the common async-loader tail; VC8 then
tail-duplicates the `call/pop/ret` sequence exactly, but the target preserves
`player` before `mode` in global-store scheduling and current ordinary-source
variants do not.  `update_player_input_counters_from_raw @ 0x00409DA0` remains
on zero/mask lifetime: target reserves EBX for keyboard mask 0x80 and creates
branch-local zero in EDI, while ordinary source hoists zero into EBX.  `OggDataSource::open
@ 0x00418A10` exposes a target-private ESI-as-this entry ABI despite a normal
standalone `__thiscall` body, so treat it as an LTCG/private-ABI stop rather than
lying about the function signature.

## Score music lookup and generated tree-family wave (2026-08-27)

The current ScoreData/music lane adds two ordinary authored canonical-exact
bodies for **392 bytes**: `case_insensitive_crc32 @ 0x00406930` is 293/293 and
`ScoreMusicLookupView::contains_bgm_path @ 0x0042D880` is 99/99.  The same
investigation removes **19 VC8-generated std::_Tree COMDATs / 1,900 bytes** from
origin review with three SHA-pinned, inventory-unique origin manifests.  At this
checkpoint the working census is **933 authored exact / 153,066 exact authored
bytes / 295 units; 1,028 excluded; 2,049 review-pending**.

The 293-byte hash closes a reusable source-lifetime rule.  Its semantics are an
ASCII-case-folded CRC: every byte is masked with `0xDF`, the CRC table index is
`(crc ^ folded_byte) & 0xFF`, blocks are processed eight bytes at a time, and a
short tail follows.  Keep the incoming `length` itself as the tail counter and
subtract `blocks * 8` from it; introducing a separate cross-branch `remaining`
local makes EBP live from the prologue and destroys the target's branch-local
EBX/EBP callee-save shrink wrapping.  Likewise, preserve the explicit `& 0xFF`
index expression.  Replacing it with an `unsigned char` cast makes VC8 extract
the low CRC byte into a separate register and produces a shorter non-target
body.  Repeated `*bytes++` expressions then reproduce the target's pointer
lifetime and all 293 bytes naturally.

The 99-byte ScoreData lookup shows that naming an STL temporary can be
byte-significant even when the semantics are identical.  The target hashes the
path and performs a checked `std::set<unsigned>::find` on the tree at ScoreData
`+0x44`.  Writing a named iterator followed by `it != tree.end()` lets VC8 know
the hidden-sret local directly, discards the returned EAX value, and reloads the
end node after `find`, yielding 98 bytes.  Writing the ordinary expression
`tree.find(key) != tree.end()` lets VC8 evaluate/cache the end node before the
call and preserve the returned iterator pointer across the checked comparison,
which is canonical exact 99/99.  `tree.count(key)` is not equivalent for
matching purposes: VC8 lowers it through `equal_range`, a different target call
edge.

The generated-origin side is deliberately compiler- and inventory-backed, not
a clone-guessing shortcut.  `scripts/probes/tree_family_generated.cpp` forces
three independently target-supported node layouts with the SHA-pinned VC8 SP1
compiler: an unsigned set with nil byte at node `+0x11`, a signed-int/small-map
layout with nil at `+0x15`, and an unsigned-key/eight-byte-mapped layout with nil
at `+0x19`.  Current caller `0x006B3DA0` independently reads/writes the last
layout's two mapped dwords at node `+0x10/+0x14`.  The three origin manifests
cover find/insert, checked iterator increment/decrement, equal-range/distance,
node allocation/erase, left/right rotations and min/max helpers.  Every anchor
must rebuild from tracked source, match at least 70% non-relocation bytes, and
have a **complete unique hit set across all 4,010 current candidates**.  This
moves 19 functions / 1,900 bytes from `unknown/review` to
`compiler_generated/exclude` without inflating the authored numerator.

Two bounded negative results from the same pass should not be restarted without
stronger TU evidence.  `CMenuSelect::update_primary_selection @ 0x00447D20`
has target code through the switch cases but a target-only tail-duplication
choice: natural structured variants converge on VC8 jump-table offsets `0x288`
(680-byte section tail) or `0x228` (584-byte tail), while the target keeps a
separate final true epilogue and places the table at function-relative `0x2A0`.
Explicit gotos/default labels do not change the 680-byte fixed point.  Treat it
as TU/optimizer tail ownership, not permission for artificial control flow.
`InputStateTableStorage::update_input_state_table @ 0x0040A550` and
`add_tracked_key @ 0x0040A6C0` likewise expose a target `push ebp; mov ebp,esp;
and esp,-8` entry while their otherwise-proven ordinary sibling TU does not;
this is real stack-alignment/source-partition evidence, not a reason to invent
aligned locals or change the project optimization profile.

## CMenuMusic / MusicList exact class island (2026-08-27)

- Current RTTI closes `MusicList` as a real `CFileList`-derived 0xDC class: the inherited 0xB4 base is followed by two `std::deque<std::string>` members at `+0xB4` and `+0xC8`. `MusicList::MusicList @ 0x00445550` is 80/80 exact when the two inherited policy words are published as `field_ac = 0; field_a8 = 0;` in that order. The reverse source order only swaps the two final stores. Keep the constructor in its own TU: `CMenuMusic` target code calls it out of line.
- Do **not** hand-write an empty `MusicList::~MusicList`. Leaving the derived destructor implicit lets VC8 emit the target 54-byte scalar deleting lifetime at `0x004455D0`: two deque `_Tidy` calls, `CFileList` destruction, conditional operator delete. An explicit empty virtual destructor republishes the MusicList vptr and grows the wrapper.
- `CMenuMusic` is a real 0x1EC class: `MusicList +0x04`, 0x34-byte design resource `+0xE0`, bound design object pointer `+0x114`, two contiguous 0x14-byte `MenuCursorState` records `+0x11C/+0x130`, and `GuideOverlay +0x144`. The normal destructor `0x00445610` is 185/185 exact from ordinary body calls (`music_list.shutdown()`, design virtual cleanup) followed by automatic member/base destruction; the scalar deleting wrapper `0x00445810` is 30/30 exact.
- `CMenuMusic::CMenuMusic @ 0x004456D0` is 318/318 exact only after recovering the same semantic publication order as the independently exact cursor binders. Cache `CInputManager *input = g_active_menu_input` once; for each cursor publish `input_counter`, then `selection`, `window_start`, `item_count`, `page_size`. Loading the global twice or publishing count/page before input changes the schedule by one byte even though the values are identical.
- `CMenuMusic::render @ 0x00445240` is a real two-column 2x16 grid, not a single scrolling column. Horizontal cursor selection contributes `selection * 256`, vertical selection contributes `selection * 16`; every loop row renders both `row` and `row + 16`. The loop index is unsigned but is converted to **float first**, then multiplied by the double 16.0 expression. That source shape produces the target m32real `2^32` correction (`__real@4f800000`); casting the row directly to double produces the wrong 64-bit correction literal. The exact body is 281/281.
- `CMenuMusic::update @ 0x00445520` is just `guide.update(); if (music_list.item_count()==0) return false; return update_primary();` and is 38/38 exact. Preserve the final tail call.
- `CMenuMusic::update_primary @ 0x00445360` is now canonical exact 442/442. The decisive source shape is semantic, not register forcing: take one short-lived `MenuCursorState *cursor = cursors_11c` only for the two update calls; double the row-change byte into bit 1, OR the page result in AL, then let the cursor alias die so VC8 naturally reuses EDI as the shared zero while keeping `this` in ESI and active input in EBP. The three input paths are one `if / else if / else if` chain followed by the final `return true`; that makes an unavailable BGM jump to the common true tail instead of a local epilogue. Finally, `install_menu_object @ 0x0043B0F0` must retain its real C++ linkage: with the chained CFG it naturally merges the `new CProfileMenu` null/non-null paths, closes the new-expression EH state, and emits one install call. The accepted source uses no `register`, volatile/fake liveness, inline assembly, dummy locals, or manual EH manipulation.
- With `update_primary` closed, the current CMenuMusic/MusicList island contributes **8 authored exact functions / 1,428 bytes**. Working checkpoint is **941 authored exact / 154,494 exact authored bytes / 297 units; 1,028 excluded; 2,041 review-pending**.
- `MusicList::populate @ 0x00445830` also remains review-pending. Full semantics are recovered (clear three deques, parse `data/csv/system/music.csv`, append path/title, consult the exact ScoreData music hash set, substitute the 38-byte grey locked label, finalize). Best normal source is currently 912/867. Target stack coloring keeps `CsvReader` at the low slot, the path string in the high string slot, title in the middle slot, and a branch-only locked-track temporary in the low string slot. Named and anonymous temporary experiments move between stable VC8 coloring fixed points; do not add dummy stack padding or derived fake string types.

## Continue/End and Profile UI runtime exact patterns (2026-08-28)

- `CMenuContinue::update @ 0x00444C70` and `CMenuEnd::update @ 0x00444FC0` close as ordinary VC8 C++ state machines. Preserve shared-result/control-flow ownership instead of replacing it with many early returns. In particular, the End state-3 confirmation test is naturally `switch (update_profile_confirmation()) { case 1: case 2: ... }`: VC8 lowers this to the target `add eax,-1; cmp eax,1` range check. Equivalent hand-written arithmetic is CSE'd against an already-live constant and changes the body/table alignment.
- Continue state 11 allocates a physical `0x44` block before calling the exact `CMenuEnd` constructor, while the independently proved named `CMenuEnd` prefix remains `0x40`. Keep the existing allocation-only dispatch view pattern; do **not** invent a trailing member merely to make `sizeof(CMenuEnd)==0x44`.
- `is_scene_fade_in_progress @ 0x0043B2C0` has an independently tracked integer implementation, but `CMenuContinue::update` consumes only AL. A caller-local `bool` declaration reproduces the target `test al,al`; this is a narrow call-site view, not a claim that the callee's global source signature changed.
- `refresh_continue_network_state @ 0x0043BAF0` is canonical exact 147/147. Network mode passes two inline 0x20-byte name buffers at session `+0x04/+0x24`. Offline mode uses the two PlayerSlot records at stride `0x33C`, specifically the VC8 `std::string` at record `+0xB4` (storage/capacity `+0xB8/+0xCC`; slot-1 addends `+0x3F4/+0x408`). The helper then calls the player2 replay object's filename builder `0x0042A560` and writer `0x00428D90`. This caller sees the session-option storage as non-volatile; declaring it volatile grows the target memory compare by one byte.
- `trim_profile_ui_menu_stack @ 0x00440010` is canonical exact 185/185 from the real checked `std::list<UiSelectionMenu*>` aggregate at `0x006FD288`: sentinel pointer `+4`, count/state dword `+8`. The source is LIFO: `delete stack.back(); stack.pop_back();`. `front()/pop_front()` produces a shorter, wrong next-link path. The target's repeated invalid-parameter branches come naturally from VC8 checked `back()/pop_back()`, followed by virtual scalar deletion, unlink/free, and decrement of the aliased count/state field.

## Practice / replay inline-cursor lifetime patterns (2026-08-28)

- `CMenuPractice` now has one shared 0x288 source layout (`src/ui/MenuPractice.hpp`) backed by independent current-target exact roots: the existing `render @ 0x00445BA0` (211 bytes), `configure_subcursor @ 0x00445C80` (269), normal destructor `0x004464B0` (186), and scalar deleting destructor `0x00446570` (30).  The closed member boundaries are settings pointer `+0x004`, `GuideOverlay +0x008`, texture `+0x0B4`, `UiTileA4 +0x0B8`, `CDesignBase +0x160`, ten category anchors `+0x194`, ten checked `std::vector<UiDesignObject *>` values `+0x1BC`, two consecutive `MenuCursorState` values `+0x25C/+0x270`, and owner `+0x284`.
- Repeated cursor initialization is byte-significant source abstraction, not merely equivalent assignment syntax.  Hand-expanding `input_counter/selection/window/item_count/page_size` let standalone VC8 eliminate or move the semantic zero and changed callee-save/receiver scheduling.  Reusing a small ordinary inline binder with the same source semantics as the independently exact `MenuCursorState::bind_horizontal/bind_vertical` siblings closes three former residuals naturally: `CMenuPractice::configure_subcursor` 267 -> **269/269**, `CMenuReplay::refresh_file_list` 231 -> **229/229**, and the `CMenuReplay` ctor's one receiver-hoist mismatch -> **350/350**.  Do not replace this with `register`, volatile state, dummy zero locals, fake dependencies, or assembly.
- `CMenuPractice::configure_subcursor` also confirms source-level control flow: category 1 is the cold `switch` case; all other categories form the hot default path.  The non-empty path evaluates `groups[category].size()` once for the branch and again for `item_count`, while all three paths use the shared horizontal binder and then restore the saved category selection.  Saving `size()` to a convenience local or spelling the input assignment as a ternary changes VC8 scheduling.
- The Replay menu dispatcher `CMenuReplay::update @ 0x00446C90` is canonical exact 132/132 when its state 0/1/2 calls are expressed through narrow bool call-site views while the independently exact helper TUs retain their byte-sized source return types.  `refresh_file_list @ 0x00446770` is 229/229 and the ctor `0x00446DE0` is 350/350 under the same vertical cursor binder abstraction; the existing dtor/render/mode/delete roots remain unchanged.
- Two Practice roots remain intentionally review-pending.  The ctor `0x00446170` currently reaches 882 bytes versus target 826 after restoring the real local count set `{0,1,6,4,4,5,2,4,0,0}` and pointer-walking design/group loop; target stack coloring and inner-loop source partition still differ.  `update @ 0x00445D90` has full state-machine semantics, but natural standalone forms land at 348 or 403 bytes versus target 381 because the target partially inlines checked `vector::size/at` while retaining branch-local bounds checks.  These are TU/inlining/allocator stops; do not manufacture redundant checks or liveness to force them.
- `CDesignBase` map lookup `0x0040CDA0` is semantically the same checked `std::map<unsigned, UiDesignObject *>::find` family as the known generated tree roots, but a fresh VC8 specialization uses a 0x10 stack frame while current `0x0040CDA0` uses 0x14.  It therefore remains origin-review rather than being excluded by similarity alone.

## Fighter render-history and generated-container patterns (2026-08-28)

- The current Fighter render-history subobject is anchored at Fighter `+0x674` with a checked `std::list<PhaseRenderEntry>` at `+0`, signed period `+0x0C`, signed-short alpha divisor `+0x0E`, and color mask `+0x10`; `PhaseRenderEntry` is a real `CSpriteEx` plus signed blend byte at `+0xE8`. Four newly recovered members are canonical exact from the same `/GS` TU: constructor `0x0045AC00` (39), period setter `0x0045A9C0` (10), color-mask setter `0x0045A9D0` (10), and guarded pop-front `0x0045AC30` (30). The constructor must assign `alpha_divisor_0e = 15` before `period_0c = 5` in the body; an initializer list reverses the two stores. The guarded pop must be written as `entries.size() > 0`; `!entries.empty()` lowers to `je`, while the shipped body uses the unsigned `jbe` path before `erase(begin())`.
- `Fighter::configure_render_history @ 0x0045E920` is canonical exact 45/45 from one truthful `+0x670/+0x674` aggregate: publish countdown, call the period setter, then tail through the color-mask setter. This caller independently owns the signed-byte period ABI and the render-history location; no register selection or fake dependency is needed.
- The exact render-state and BattleObjectManager sources expose reusable VC8-generated container families. SHA-pinned rebuild plus complete 4,010-span relocation-masked replay now classifies five `std::list<PhaseRenderEntry>` COMDATs (`0x0045A9E0/0x0045AB50/0x0045AB90/0x0045AC50/0x0045ACA0`, 430 bytes), the `std::list<BattleRenderRequest>` value-node allocator `0x00466940` (59), and two previously uncovered `std::vector<CSpriteEx>` helpers (`_Tidy @ 0x00436420`, checked `operator[] @ 0x004654B0`, 140). Keep these out of the authored denominator; the origin manifests rebuild their actual source/probe and require each complete current fingerprint hit set.
- The Fighter sequence controller is a checked deque-like owner whose physical slot is 0x98 bytes. A tracked witness using `{short record_id; unsigned short field; CSprite sprite;}` reproduces three inventory-unique VC8 deque COMDATs: nontrivial `_Copy_backward_opt @ 0x0045D390` (186), `_Copy_opt @ 0x0045D450` (186), and pointer-map `_Growmap @ 0x0045DA40` (341). Exact `FighterSequenceController::enqueue_record @ 0x0045DBA0` already calls the same grow-storage body. Do not promote nearby iterator/range wrappers merely by adjacency: `0x0045CF60` and `0x0045DC20` still require their own exact generated fingerprints or authored evidence.
- The shared Fighter render virtual at `0x0045E6E0` is semantically closed but deliberately not exact. Ordinary source with branch-local duplicate `sprite.translate(x, -y, 0)` calls lets VC8 common-tail merge and reaches the exact 570-byte size with 153/153 normalized instructions. The only remaining structural difference is one scheduler move: target starts the first `fild frame_x` before `push edi`, while five normal local-declaration forms all emit `push edi` first. Named pivot/render-X locals are worse (521 bytes); recomputed pivots plus a ternary X are 549; the two real if/else calls recover all missing instructions. Treat the remaining one-instruction ordering as a TU/backend scheduler stop; do not use `register`, `volatile`, fake uses, assembly, or padding to swap it.
- `CollisionContext::test_descriptor_shapes_overlap @ 0x0046BA10` is likewise bounded honestly. Replacing the final signed gate with the already exact sibling's bit-31 predicate restores the target 252-byte length (`and` then `test`/`jns` rather than a fused `test eax,edx`), but target still hoists `second->top` before saved-register setup and fresh VC8 does not. A real `second_top` local worsens the body to 270 bytes and regrouping the arithmetic is codegen-neutral. Preserve this as an early-load scheduler/TU stop.
- `GuideOverlay::update @ 0x0043F450` is 112/112 but remains backend-pending: target emits `sub al, 0x0F` and takes the second-level sprite method pointer before publishing `alpha_a4`; fresh ordinary source canonicalizes the decrement to `add al, 0xF1` and schedules the vtable load after the member store. A typed method-pointer local grows the body to 116. `GuideOverlay::~GuideOverlay @ 0x0043F330` under the truthful `/GS` profile is 90/92 with the same 27 normalized instructions; target compares `texture_04` in memory then reloads it, while fresh VC8 CSEs the load through EAX. The target EH handler at `0x006B905B` is now separately attested in the relocation ledger. Do not force either root with alias/volatile tricks.
- `FighterSequenceIterator::dereference_checked @ 0x0045CF60` reaches the exact 67-byte size when the real owner/capacity locals and wrapped-index fallthrough are restored, but all 28 instructions retain a pure ESI/EDI role swap (`target: ESI=this, EDI=position`; fresh: the reverse). This is an allocator stop, not missing behavior. The neighboring `0x0045DC20` is semantically a checked deque erase-one member ABI (ECX owner, hidden return iterator plus by-value 12-byte iterator, `retn 0x10`) that delegates to range erase `0x0045D830`; do not label it compiler-generated until a fresh specialization reproduces that exact wrapper.

## Match/runtime preload exact island (2026-08-28)

- Five current authored roots close one coherent scene/match bootstrap island for **1,142 canonical-exact bytes**: `prepare_match_runtime_by_mode @ 0x0043AC60` (521), `load_indexed_event_wave_resources @ 0x0043AA50` (116), `render_scene_fade_overlay @ 0x0043AAD0` (180), `initialize_default_player_profiles @ 0x0043AF30` (167), and `async_scene_load_worker @ 0x0043B580` (158). All are ordinary pinned-VC8 C++; no inline/native assembly, copied target bytes, volatile/register forcing, fake liveness, or padding-for-codegen is used.
- `prepare_match_runtime_by_mode` fixes several reusable source-ownership facts. The contiguous match-runtime owner remains at `0x006FCA4C`; PlayerSlot records have the already-backed `0x33C` stride and expose the mode-runtime embedded deck at `+0x138`; network runtime uses `+0x1F0/+0x1F8` deck pointers, selected source pointer `+0x200`, and published value `+0x208`. The shipped switch body order is **2/8, 1, 4, 5, 3**, not numeric order. Directly evaluating the selected-input pair as one 16-bit global lets VC8 keep it in AX and naturally emit `cmp al,FF / cmp ah,FE / movzx eax,ah`; naming a 16-bit local creates the wrong eight-byte frame. The player-slot deck selector is source-consistent with a signed-byte formal plus a byte-sentinel comparison: that callee remains 59/59 exact while the network-server special case naturally becomes the target two-byte `push -1`.
- The same 521-byte body requires the ordinary ternary `mode == GAME_MODE_PRACTICE ? 3 : 1` after publishing flag54=0, flag74=1, flag56=1. VC8 then emits the target `setz` followed by the independent stores and `lea eax,[eax+eax+1]`; prematurely narrowing the expression to byte produces `add/add` and moves the stores. `g_match_identifier_source @ 0x006FC5FC` is in the PE `.data` virtual zero-fill tail, so canonical mapping is loader-zero even though an IDA database may display a non-file-backed value.
- `load_indexed_event_wave_resources` reuses the exact-backed wave service at `0x00702288/0x00401AF0`: a real 260-byte `/GS` path buffer formats `data/se/%03d.wav`, fills all 128 handles at `0x006FCA98`, then tail-enters the now exact shared battle-runtime bootstrap `0x00438F50`. This is the system-SE analogue of the already exact per-character wave loader; reuse the same service ABI rather than inventing a second loader facade.
- `render_scene_fade_overlay` is a **byte-return** helper, not a C++ `bool` root. `bool` return normalization grows the body to 181 bytes; `unsigned char` reproduces the target 180/180 and preserves the final `test al / setnz al`. The alpha path is an unsigned conversion of `(unsigned char)(fade_color >> 24) * fade_value`, which naturally generates the target x87 control-word conversion before drawing the 640x480 rectangle. The exact profile-UI frame renderer keeps its real `bool` declaration; the outer helper itself is byte-return, so VC8 naturally consumes the callee result through AL and emits the target `test al / setnz al` without a narrow callee ABI view.
- `initialize_default_player_profiles` operates directly on the two 0x33C PlayerSlot records. Each side short-circuits `reload_profile()` then fixed-file load (`Profile1P.dat` / `Profile2P.dat`), initializes and saves only if both fail, then publishes colors `(160,160,255)` and `(255,128,128)`. Preserve the two `&&` chains and the final void color call; the decompiler's residual EAX is not a source return contract.
- `async_scene_load_worker` belongs in the same TU as the already exact 171-byte launcher. After successful COM initialization its request cases 1..5 set thread priority and dispatch system-wave preload, mode-resource preparation, or the three exact Story setup leaves, then uninitialize COM and clear the request. The worker's COFF section is 180 bytes because the local five-entry switch table/alignment follows the callable body; the canonical function boundary is 158 bytes and is zero-difference. Adding the worker definition does not disturb the launcher, which still cold-replays 171/171 exact.
- The neighboring deck CSV loader `0x00431E80` remains deliberately pending, but its lifetime evidence is stronger now. Its EH unwind funclet at `0x006B81B8` loads the local reader and jumps to `0x0042E980`; that 5-byte target thunk is exactly `jmp 0x0040FDB0`, proving a real `CsvReader` destructor whose semantic body is `clear_rows()`. The target default lifetime zeroes only reader `+0x04..+0x28`; the path is the real chained `"data/csv/" + std::string(character_key_to_name(key)) + "/deck.csv"`; normal destruction clears the reader before the owning full-path string. Standalone VC8 still settles at the known 487/463 stack-coloring fixed point, with the reader and anonymous string temporary occupying different slots from the shipped TU. Do not force this residual with dummy padding, fake derived strings, register/volatile state, or manual EH manipulation.
- `0x0043B340` is not an authored progress candidate: current semantics are a checked-tree iterator increment (owner validation, nil flag at node `+0x15`, leftmost/parent walk). Treat it as generated-origin work unless a fresh pinned specialization proves otherwise. `0x0043B4A0` also needs boundary review before source promotion because its 209-byte ledger body tail-jumps into shared profile-UI code at `0x004400D0`; do not copy the shared tail into a fabricated standalone function.

## Scene-manager lifecycle / shared battle-runtime patterns (2026-08-28)

- One current lifecycle island closes **6 authored canonical-exact roots / 1,148 bytes**: normal scene setup `0x00439130` (166), Arcade setup `0x004392C0` (166), Story setup `0x004393A0` (211), shared battle-runtime bootstrap `0x00438F50` (465), scene-manager teardown `0x00439480` (95), and mode dispatcher `0x0043AC10` (45). All accepted source is ordinary pinned-VC8 C++; no inline/native assembly, copied target bytes, register/volatile coercion, fake liveness, manual vtable loads, or padding-for-codegen is used.
- Normal and Arcade setup are simple target-backed new-expression owners. The shipped image allocates physical extents **0x5B4** and **0x5C8** respectively, calls current ctors `0x00438CC0/0x00438D80`, publishes the shared scene-renderer pointer, then allocates the exact-backed **0x688** normal `CInfoManager` and publishes `g_info_manager`. Both use the same VC8 EH handler at `0x006B89E6`. Allocation-only source views are appropriate here: the extents and ctor edges are target facts, while unresolved internal fields remain unnamed.
- Story setup uses the same natural new-expression machinery but has stronger type ownership. A target-proved **0xA8** derived scene manager first calls exact `CBattleManagerBase::CBattleManagerBase @ 0x0046B200`, then VC8 naturally publishes RTTI-owned `CBattleManagerStory` vtable `0x006C1374`; do not hand-write the vptr. It then allocates the exact-backed **0x610** `CInfoManagerStory` and a **0x5B0 BattlePhaseBlock** via ctor `0x00439200`. The final global is the already-established `g_battle_phase_block @ 0x006FBCB4`, not a new Story-only auxiliary pointer. Story setup EH handler is `0x006B8A31`.
- `destroy_scene_managers @ 0x00439480` proves the reverse owner chain. Ordinary `delete` on the scene-renderer and info-manager interface views produces the target virtual scalar-delete calls with flag 1; deleting the BattlePhaseBlock naturally gives dtor `0x00438E90` followed by operator delete and then clears `g_battle_phase_block`. Do not replace these with manual vtable-slot calls merely because the decompiler shows them explicitly.
- `prepare_scene_resources_by_mode @ 0x0043AC10` is a 45-byte destroy-then-tail-dispatch helper. The target arithmetic checks modes **0, 1, and raw value 7**, then defaults to normal setup; value 7 tail-enters existing `initialize_story_managers @ 0x00434D40`. `GameMode.hpp` does not yet have a target-backed semantic name for 7, so the source deliberately uses `static_cast<GameMode>(7)` rather than inventing an enum label. Its only current caller is the async worker and does not consume EAX, supporting the truthful void lifecycle chain.
- `initialize_shared_battle_runtime @ 0x00438F50` closes the 465-byte shared bootstrap. Five natural allocations publish current owners: FighterPhaseContext **0x54**, BattleObjectManager **0x118**, EventEffectState **0xE8**, EffectSink **0x94**, and a target-proved **0x30 allocation view** at the global currently named `g_battle_input_gate`. The 0x30 view is intentionally local: it proves this allocation extent but does not claim that the larger shared `BattleInputGate` header type has size 0x30. The effect sink then loads target literal `data/effect/effect.pat @ 0x006C15A4`, EventEffectState initializer `0x0046A260` runs, and a 260-byte path buffer drives the BGM lookup loop.
- The live lookup object at `0x006FBD2C` is the already-proved `std::map<unsigned,unsigned short>`. For indices 0..22 the source formats `data/bgm/st%02d.ogg`, computes exact-backed case-insensitive CRC32, and inserts `(hash, ushort(index + 900))`. Native VC8 `map::insert` automatically produces the target 12-byte `{iterator,bool}` hidden-sret temporary and calls current `0x004366A0`; no hand-modeled tree node or return buffer is needed. This is a useful example where exact consumer data-flow should determine the mapped type while generated template fingerprints only establish code ownership.
- The system-wave preload `0x0043AA50` should now be modeled as **void**, not the earlier residual-int hypothesis. It fills 128 wave handles and tail-enters the exact void `initialize_shared_battle_runtime`; changing both declarations from int to void leaves the body canonical 116/116. The same void declaration in `async_scene_load_worker` leaves worker 158/158 and launcher 171/171 exact. Residual EAX at a tail boundary is not sufficient evidence for a source return value when every current caller ignores it.

## Scene-owner lifetime exact patterns (2026-08-28)

- A current class-lifetime wave closes **10 authored canonical-exact roots / 1,199 bytes**: `CBattleManager` ctor/dtor/scalar `0x00438CC0/0x00438D20/0x004391E0` (89/91/30), `CBattleManagerArcade` ctor/scalar `0x00438D80/0x00439370` (103/43), `EventEffectState` ctor/dtor `0x00438DF0/0x00438C10` (160/173), `BattlePhaseBlock` ctor/dtor `0x00439200/0x00438E90` (183/185), and shared runtime teardown `0x004394E0` (142). Every accepted body is ordinary pinned-VC8 C++; no inline/native assembly, copied bytes, register/volatile forcing, fake liveness, manual vtable loads, or padding-for-codegen is used.
- **Implicit destructor source ownership is byte-significant.** For the 0x5B4 normal manager, `CBattleManagerBase` occupies 0x74 bytes and the target-backed ScenarioData lifetime begins at `+0x8C` with extent 0x528. An explicit empty derived virtual destructor makes VC8 republish the derived vptr and emits 97 bytes; leaving the destructor implicit naturally emits the shipped **91-byte** dtor and its **30-byte** scalar wrapper. The same rule matters for Arcade: `std::list<int> +0x5B4`, dword `+0x5C0`, dword `+0x5C4` give exact size 0x5C8; an explicit empty Arcade dtor makes the scalar wrapper 49 bytes, while the implicit destructor naturally inlines list `_Tidy`, calls the 91-byte normal dtor, and gives target **43/43**. Do not "fix" this with manual vptr stores or noinline/register tricks.
- Current vtable anchors independently support those lifetimes: `CBattleManager @ 0x006C1504` begins with scalar deleting dtor `0x004391E0`; `CBattleManagerArcade @ 0x006C154C` begins with scalar deleting dtor `0x00439370`. Normal ctor/dtor share EH handler `0x006B87F8`; Arcade ctor uses `0x006B8828`. The lifetime sources keep unresolved virtual slots outside the narrow class view while mapping the compiler-generated vtable publication to those target-owned anchors.
- `EventEffectState` is no longer an opaque prefix. Current initialization, publish, and destruction paths close the **0xE8** layout: `int current_event_id +0x00`; byte state at `+0x04`; checked `std::vector<unsigned> +0x08`; checked `std::vector<CSpriteEx> +0x18`; checked `std::deque<BackgroundRenderEntry> +0x28`; checked `std::map<signed char,std::vector<signed char>> +0x3C`; exact `CEffectManager<WeatherEffectObject> +0x48`; and a 0x0C runtime tail at `+0xDC`. The map key signedness is target-visible: current tree search `0x004698E0` uses signed byte `jge/jl`, while mapped-value copy `0x00469CC0` moves byte elements. Runtime initializer `0x0046A260` independently proves the first vector is dword handles and the second vector indexes 0xE8-byte `CSpriteEx` values. Natural STL ctor/dtor code gives **160/173 exact**, including map `_Buynode/erase`, vector/deque cleanup and the Weather effect-manager lifetime.
- `BattlePhaseBlock` physical layout is likewise closed to **0x5B0**: checked `String28 +0x000/+0x01C`; CsvReader lifetime `+0x038`; polymorphic 0x524 story-script subobject `+0x068`; `selected_input +0x58C`, `selected_column +0x590`, `page_index +0x594`, `story_character +0x595`; and two real `std::list<int>` values at `+0x598/+0x5A4`. Exact ScenarioEventParser consumers independently use those two tail lists. The destructor body does only the script virtual shutdown; C++ reverse member destruction then gives list/list -> script dtor `0x00438A70` -> CsvReader cleanup -> two strings, exactly matching target order.
- CsvReader cleanup has a specific EH property. Target unwind funclets use the 5-byte cleanup thunk `0x0042E980 -> 0x0040FDB0`, while normal BattlePhaseBlock destruction calls `clear_rows @ 0x0040FDB0` inline. A normal throwing declaration inserts a five-byte EH-state publication and gives 190/185; declaring both the local truthful CsvReader dtor and `clear_rows()` with the C++03 non-throwing specification `throw()` naturally removes that state write and produces **185/185**, with no manual EH state manipulation. The ctor correspondingly zeroes only CsvReader `+0x04..+0x28`; its proxy/base dword remains outside the default initialization stores.
- `destroy_shared_battle_runtime @ 0x004394E0` closes the reverse side of the earlier shared bootstrap. It calls exact `destroy_scene_managers`, invokes EventEffectState shutdown as a **thiscall member** (`0x00469EC0`), dispatches the EffectSink shutdown virtual, directly frees the target-proved 0x30 input-gate allocation, then uses ordinary C++ delete for EffectSink and nonvirtual EventEffectState/BattleObjectManager/FighterPhaseContext lifetimes. Treating `0x00469EC0` as cdecl adds exactly four bytes (push/add-esp) and is rejected; current ECX call shape fixes the member ABI.


### EventEffectState/FighterPhaseContext runtime exact wave (2026-08-28)

- EventEffectState runtime closes three authored bodies / **362 bytes**: `publish_stage_state @ 0x00469D50` 153/153, `prepare_shutdown @ 0x00469EC0` 123/123, and `reset_runtime_state @ 0x00469F40` 86/86. The live tree is `std::map<signed char,std::vector<unsigned char>> @ +0x3C`; pinned generated `_Insert_n @ 0x004699C0` proves unsigned-byte mapped rows, while target tree comparisons prove a signed-byte key.
- `publish_stage_state` requires the unnamed expression `stage_rows.find(state)->second[current_event_id]`; naming the iterator removes the hidden-sret pointer and shortens VC8 output. Preserve the structured nonnegative sprite branch as well.
- `initialize_effect_runtime @ 0x0046A260` remains a 941-byte shipped-TU stack-coloring stop. Semantics and the `/GS` frame are closed, but target reuses the 0xE8 `vector<CSpriteEx>::resize` default-value area with the later path buffer while standalone VC8 uses a separate outgoing temporary. Do not force this with artificial source dependencies.
- FighterPhaseContext runtime closes three authored bodies / **642 bytes**: dtor `0x00464BA0` 413/413, released-worker `0x00464DD0` 126/126, and reset-slot `0x00465040` 103/103. The 0x54 owner is `list<FighterPhaseInitRequest> +0`, two 8-byte thread owners +0x10/+0x18, events +0x20/+0x24, three owned handles +0x28, active bytes +0x34..+0x36, checked `vector<Fighter*> +0x38`, and `list<int> +0x48`.
- In reset-slot, pass the owned member itself by reference to `vector::push_back`; a materialized pointer temporary is longer. In the dtor, early thread calls are stop/join, while true thread-member destruction remains in implicit reverse member teardown.
- FighterPhaseContext ctor `0x00464E90` remains a bounded 241-byte scheduler stop: layout/order are closed, but standalone VC8 keeps zero in a long-lived register while target keeps the two thread-member addresses live. Ordinary reference aliases optimize away, so leave this pending until stronger TU evidence appears.

## GameConfig load/profile-editor exact patterns (2026-08-28)

- `GameConfig::load @ 0x00427800` is canonical exact **953/953** from an ordinary `/GS` TU with two real VC8 `std::string` values at `+0x00/+0x1C`, checked `std::deque<std::string> @ +0x48`, and the current CFileReader vtable/lifetime. The byte-significant source detail is the recent-entry insertion: keep `recent_entries.push_back(std::string(buffer))` as an **unnamed temporary**. Naming `std::string value(buffer)` preserves all 333 instructions and the 953-byte size but swaps the string and deque-internal stack-color slots by four bytes. Do not repair that with dummy stack objects; recover the expression lifetime instead.
- The same exact load plus current constructor/save data flow fixes the GameConfig physical prefix through `+0x71`: profile strings `+0x00/+0x1C`, timeout `+0x38`, four 16-bit values `+0x3C`, timeout/state `+0x44`, recent string deque `+0x48`, bytes `+0x5C..+0x5E`, dwords `+0x60/+0x64/+0x68/+0x6C`, and bytes `+0x70/+0x71`. `initialize_player_profiles_from_game_config @ 0x0043BA10` is **217/217** and independently proves both string `c_str()` SSO addends (`+0x04/+0x18` and `+0x20/+0x34`) before the exact PlayerSlot load/default/save/color chain.
- `ProfileEditor168::initialize @ 0x00427BD0` is **108/108**. Exact CProfileMenu ctor/state/render/dtor callers already own the 0x168 object. Current source publishes source `+0x08`, record limit `+0x164`, clamps shared `profile_editor_record_limit @ 0x00703588` to 1023, reads outer width at source `+0x114`, then advances the **working record cursor by 0x0C** before reading inner bias `+0x111` and x/y offsets `+0x118/+0x11C`. A flat struct view is semantically equivalent but compiles 105 bytes because VC8 folds the cursor adjustment; preserve the actual cursor ownership.
- Two nearby GameConfig roots remain deliberate stops rather than opportunities for codegen coercion. `save @ 0x00427390` already reaches **877/877 and 334/334 instructions**; the only normalized difference is the commutative checked-deque bound expression choosing `load +0x54; add +0x58` in target versus the reversed loads in fresh VC8. `global ctor @ 0x00427700` and its atexit dtor `0x0043DC70` exhibit a shipped-TU string lifetime topology that combines inline SSO initialization/teardown with EH-vector constructor/destructor helpers over the same two 0x1C string slots. Ordinary `std::string[2]` source does not reproduce that topology. Do not emulate it with placement-new, manual double destruction, fake aliases, or volatile/register tricks.
- The dirty `BattleObjectManager::update_background_runtime @ 0x00466F40` investigation is also bounded. The current ordinary C++ body is 1064/1074 with **308/308 instructions**. A truthful `next_level = effect_level + delta` local raises structural similarity to about 0.97 but lets VC8 over-tail-merge the three `delta=0; update(16)` branches and shortens the body further. The target additionally shrink-wraps EDI/EBX/EBP well after the prologue. Keep this as a TU/backend scheduling stop unless new static-type or source-partition evidence appears; do not manufacture liveness.

## CProfileKeyConfig runtime source-shape patterns (2026-08-28)

- The current `CProfileKeyConfig` 0x330 class closes three authored canonical-exact roots in one ordinary VC8 TU: constructor `0x0044BA90` (567), render `0x0044B340` (501), and virtual update `0x0044B860` (355), for **1,423 authored bytes**. The already-exact scalar deleting destructor and exact ProfileMenu state-three caller independently own the class identity and allocation extent; the 188-byte normal destructor remains deliberately review-pending on its documented scheduler-only store/load swap.
- Constructor member-init ownership is byte-significant. `TitleDesignResource`, two `UiTileA4` values, and `GuideOverlay[2]` must construct before `player_04` and `input_slot_32c` are published in the body, in that order. Putting `player_04` in the initializer list stores it before the target design construction; putting only `input_slot_32c` in the initializer list swaps the two late argument loads. Keeping both primitive assignments in the body matches the target once the texture-load result is modeled truthfully.
- Target `0x00404C90` behaves as a four-byte class value return at this call site. A `TextureHandleResult` value-return view lets VC8 use the normal hidden-sret slot and naturally reuse dead constructor argument storage. Rewriting it as an explicit `unsigned handle` out-parameter creates an additional local/frame lifetime and breaks the otherwise exact 567-byte constructor.
- Do not replace a real polymorphic source call with a manual `vtable[n]` facade merely because the target slot is known. In this constructor, viewing `TitleDesignResource` as a genuine virtual interface whose fifth slot is `load(const char *)` naturally schedules the vptr/method load between the `alternate_320`, `state_328`, and `count_324` stores. The existing inline manual-vtable facade keeps those stores together and misses the target. This is static-type/source evidence, not permission to write manual vtable evaluation in authored reconstruction.
- `CProfileKeyConfig::render` is 501/501 only when key slots are read directly in both the nonnegative check and the `%`/`/` tile expressions. Naming `int key = keys_2f0[i]` lets VC8 load then test; the target compares memory first and reloads the value inside the branch. The render also confirms a CTile call-site ABI of `render(float x, float y, int column, int row)` and the target double 176.0 literal at `0x006C22E0`.
- `CProfileKeyConfig::update` closes 355/355 by preserving the source CFG rather than cloning returns. The raw-input capture success path jumps to one final true epilogue, so ordinary source should wrap the remaining logic in `if (!capture_key_input()) { ... }` and return true once at the tail. The confirm path must not hold a long-lived PlayerSlot pointer: copy the 0x34 key block and then reload `player_04` for alternate publication, deck selection, profile save, and input rebinding. A named persistent slot pointer makes VC8 save EBP and shortens/reorders the target path.

## Fighter phase / profile availability exact wave (2026-08-28)

Eight additional ordinary-VC8 authored bodies close for **1,076 bytes**: the
FighterPhaseContext swap/pending-worker/two COM worker entries/release path at
`0x004642B0`, `0x00464D40`, `0x00464E50`, `0x00464E70`, and `0x00464F90`;
the embedded fighter state step/phase at `0x004652F0` and `0x004653F0`; and
`CProfileDeckEdit::available_count_for_index @ 0x00448DF0`.

The FighterPhaseContext layout lesson is that +0x34..+0x36 is a real three-byte
active array.  Modeling it as `unsigned short + byte` preserved offsets but
changed indexed byte scheduling in the 49-byte swap.  `unsigned char[3]`
recovers that body while the already-exact destructor, released-worker loop,
and reset path remain exact.  The 170-byte release path is then ordinary
`list<int>::push_back`, ownership zeroing, `vector<Fighter*>::clear`, and
`SetEvent`; no manual node operations are needed in source.

For the embedded +0x3D0 state, do not trust a relocation match when the literal
semantics disagree.  An exploratory probe used `0.025f`, but current target
`0x004653F0` loads `0x006CD228`, whose attested bytes are IEEE-754 `32.0f`.
Correcting source to `callback.apply(value, 32.0f)` naturally gives 45/45 exact.
The adjacent 248-byte step uses the exact orientation-sine helper and the
source-visible double constants 1.0, 480.0, 64.0 and 640.0.  Reject relocation
aliases between numerically different literals even if they could make address
bytes compare.

`CProfileDeckEdit::filtered_3cc` is a checked `deque<SpellTreeIterator>`, not a
deque of the local deck-count map iterator.  This source type is required by the
358-byte availability root, which looks up the selected spell key in the
per-character ScoreData count map first, then the common map, capping either at
99.  Updating the durable header changes the generated `_Tidy` symbol used by
the already-exact profile-deck destructor, but the correct
`deque<SpellTreeIterator>::_Tidy` is linker-ICF identical at the existing
physical target helper `0x00420A80`; mapping the correct template instance
restores the destructor without lying about the element type.

## CTitle constructor/update exact patterns (2026-08-29)

- `CTitle::CTitle @ 0x00425DF0` is canonical exact **577/577** and `CTitle::update @ 0x00425990` is canonical exact **862/862**, for **1,439 authored bytes** in one ownership island. Current RTTI/vtable ownership, the exact scene factory, exact title destructor/hooks/menu-wave helper, and the target-proved 0x28C allocation independently anchor the class.
- The durable title layout after `TitleDesignResource +0x1DC` is no longer opaque. `MenuCursorState` begins at **+0x210** (`item_count/page_size/input_counter/selection/window_start`), twelve `UiDesignObject *` menu rows occupy **+0x224..+0x253**, twelve wave offsets occupy **+0x254..+0x283**, and the tail dwords are `+0x284/+0x288`; exact update calls `MenuCursorState::update @ 0x0041FA20` directly on `this+0x210`, while render indexes the row/wave arrays in parallel.
- Constructor static-type visibility is byte-significant. Existing `Title.hpp` keeps destructor-facing 0x94/0xA4 color facades because they are required by the already-exact 161-byte destructor. The constructor TU instead uses a constructor-facing physical view with a four-byte non-polymorphic color base and real polymorphic CSprite/CTile-derived members. This naturally emits `member+4 = 0` before derived-vptr publication and the shipped EH cleanup calls. Do not collapse the two views into one header merely for aesthetics; that changes already-exact destructor codegen.
- Texture-result lifetime closes the constructor stack coloring without fake locals. `width` and `height` live across both texture loads, but **each load owns its own lexical `unsigned handle` local**. VC8 colors those two disjoint handles to one slot and places generated EH saved-this in the target slot; one handle spanning both loads or two full width/height/handle triplets move saved-this by four bytes. The source then loads `base.bmp`, `menu_item.bmp`, `title.dat`, binds IDs 100..111, zeroes all wave offsets, and initializes title mode with physical switch block order ending **case 8 before case 7**.
- `CTitle::update` is a two-level sparse state machine. The outer scene-mode dispatch has physical source order **8, 9, 12, 6**, then default. Case 6 must be written as `player2_flag == 0 ? mode 0 : mode 3`; the logically inverted spelling is semantically identical but changes one target `jnz` to `jz`. The inner cursor selection 0..11 uses ordinary typed new for CMenuConnect **0x118C**, CMenuReplay **0x280**, CMenuMusic **0x1EC**, CMenuResult **0x1148**, CProfileMenu **0x8EC**, and CMenuConfig **0x28C**. VC8 naturally emits all six allocation-cleanup EH states and both target jump tables; no manual placement construction or vptr stores are needed.
- `CTitle::render @ 0x00425740` remains a deliberate non-acceptance at **417/417**. The truthful source reproduces the three separate cosine calls, x87 control-word byte conversions, 1.5/1.0 selected-sprite scaling, and all 12 tile renders; the first canonical residual is only a selected-index load scheduled before the two scale stores, and alternate truthful sprite static types do not change it. A pointer-induction rewrite selects the other adjacent array as base and worsens the body. Do not add fake alias barriers, volatile/register state, or invented setters to reorder one load.
- `CSelectScenario::CSelectScenario @ 0x00425350` is canonical exact **862/862**. The physical `scenario_ids_2c` member is the target-backed 0x10-byte checked integer-vector owner, while the two already-exact checked-index consumers retain a local `std::vector<int>` operation view. That split preserves the constructor/destructor lifetime topology without weakening bounds-checked consumer semantics; replaying the existing destructor and flow units after the shared-header change keeps all four affected methods exact.
- Constructor stack coloring comes from ordinary source lifetime. `width` and `height` span the three texture loads, but one lexical `handle` block ends immediately after them; extending the handle through design loading grows the frame. The final tree residual was also lifetime-shaped: preserve the iterator returned by `find`, compare its node against the tree head first, and materialize the node local only after the end check. Declaring the node before that check makes VC8 copy the same pointer between registers and leaves the former two-byte `mov ebp,ecx` surplus. These are source object lifetimes, not dummy locals, register coercion, alias barriers, or hand-written tree internals.


## CMenuConnect state-runtime exact patterns (2026-08-29)

- One current CMenuConnect ownership wave closes **5 authored canonical-exact methods / 1,226 bytes**: `reset_state @ 0x004422F0` (158), `finalize_state @ 0x00442530` (150), `finalize_message @ 0x004425D0` (312), `update_state_one @ 0x00442790` (322), and `update_state_six @ 0x004428E0` (284). The already-exact virtual `CMenuConnect::update @ 0x00444B90` directly dispatches state one and state six, while state one directly owns reset/finalize calls, so these are class-authored bodies rather than shape-selected helpers.
- `CMenuConnect` has two real `MenuCursorState` records at **+0x3F8** and **+0x40C**. `reset_state` binds them to the combined-menu input counters `+0x38/+0x3C`, initializes `(item_count,page_size)` as `(5,0)` and `(10,0)`, and derives the second selection as `9 - (port / 10000) % 10` from dword **+0x428**. Keeping native `MenuCursorState` fields produces 158/158; no hand-expanded cursor ABI is needed.
- Connection ownership is stable across the utility/message bodies. The session pointer is **+0x04**; the selected endpoint is a real `MenuString28 @ +0x42C`; port is `+0x428`; a one-byte profile/include decision lives at `+0x3B8/+0x3B9`; the session's status subobject is **+0x3B4** and ready dword is **session+0x4D0**. `finalize_state` uses a genuine 0x10-byte `ConnectKey16` hidden-return/copy lifetime before `configure_412e10`; target code naturally reuses that value without manual register control.
- `finalize_message` is `/GS` and uses a genuine **0x28-byte packet**: include byte, profile-length byte, then profile payload storage. It optionally reads `configex.ini` / section `network` / key `clientport` through the target-backed config receiver `0x00703598`, configures the same session status object, copies the selected PlayerSlot profile string, then sends to endpoint/port. A GS- build shrinks the body from target 312 to 293, so this security-cookie topology is source/TU evidence, not optional compiler noise.
- `update_state_six` likewise requires `/GS`: two real `MenuString28` locals make the target 284-byte body and its two unwind cleanups; GS- is only 250. The successful selection path loads PlayerSlot 0's profile, publishes color `(160,160,255)`, closes config, and clears outer state `+0x08`; cancel only clears that state. Do not replace the two strings with raw buffers or one reused temporary.
- `update_state_one` is a three-substate machine keyed by signed byte **+0x09**, with GuideOverlay visibility at `+0xD9C/+0xE44/+0xEEC`. Substate 0 performs address entry; substate 1 handles profile-message results and publishes byte `+0x3B9`; substate 2 handles connection success or cancel/teardown. The successful path publishes scene mode 8 / next-scene mode 4 before setting substate -1. Ordinary source is 322/322 under either GS setting.
- Keep current helper identity precise. `clear_network_session` is **0x0043B0A0**. During fresh replay, accidentally mapping it to nearby `0x0043CE70` made state-one/finalize/message all fail at their first clear-session call while reset/state-six stayed exact. The fail-closed comparator correctly exposed the bad semantic mapping; never paper over that with relocation aliases.


## CMenuConnect constructor / CNumber runtime exact patterns (2026-08-29)

- `CMenuConnect::CMenuConnect @ 0x00444190` is canonical exact **728/728** and `CNumber::render @ 0x00414F10` is canonical exact **312/312**, adding **1,040 authored bytes** in one connect/UI ownership continuation. Current CMenuConnect RTTI/vtable owns the constructor; the ctor itself publishes RTTI-owned `CNumber` and `TNumberValue<int *>` identities. Both accepted bodies are ordinary pinned-VC8 C++ with no inline/native assembly, copied target bytes, `register`/`volatile` forcing, fake liveness, padding-for-codegen, or manual vtable stores.
- The constructor closes CMenuConnect's physical extent at **0x118C**. The stable member chain is DesignResource `+0x00C`; texture handle `+0x060`; tile `+0x064`; CNumber `+0x108`; sprites `+0x1D4/+0x268`; second texture handle `+0x2FC`; tile `+0x300`; checked `std::deque<std::string> +0x3A4`; a real separate dword **`+0x3B8`**; primary `MenuCursorState +0x3BC`; existing state cursors `+0x3F8/+0x40C`; IPv4 words `+0x420`; port `+0x428`; `MenuString28 +0x42C`; wide endpoint storage beginning `+0x448`; owner/config pointer `+0xC48`; and eight `GuideOverlay` values from `+0xC4C` through the end of the object. Native VC8 deque size in this TU is 0x14, so treating `+0x3B8` as deque padding shifts every later field and is wrong.
- Two four-byte texture members need a real zeroing value-wrapper constructor. Leaving them as raw `unsigned` removes target stores at `+0x60/+0x2FC` and shortens the constructor. This is the same source ownership pattern already exact-backed by scene texture handles: trivial source types can still be byte-significant when member initialization is visible before throwing calls.
- CNumber RTTI resolves the vtable at `0x006C1B60`; the owner is **0xCC**. Its prefix is `glyph_width +0x04`, `spacing +0x08`, `scale +0x0C`, an opaque dword `+0x10`, decimal column count `+0x14`, value-adapter pointer `+0x24`, and a CTile-compatible 0xA4 member at `+0x28`. CNumber construction lets the embedded tile construct first and only then clears the adapter pointer. `TNumberValue<int *>` is an 8-byte polymorphic adapter whose current RTTI vtable is `0x006C1BC0`; CMenuConnect binds it to primary cursor selection at `+0x3C8`.
- `CNumber::render` is a useful decompiler-ABI correction. Hex-Rays reports a residual `char` return because AL still contains the suffix/tile-call residue, but all current callers ignore the value and a `char` source return creates a duplicate zero-return epilogue. Declaring the method **`void render(int value, float x, float y, int digits, char sign_mode)`** produces 312/312 exactly. Keep `current_x = x - scale * glyph_width` as a named local rather than mutating the x parameter; VC8 then colors it into the dead value-argument slot used by the target. In the variable-digit loop use the structured `if (value < 10) value = -1; else value /= 10`; the fixed-digit branch decrements the requested digit count and divides after each draw. The signed suffix is 2 for negative numbers, 1/0 from the caller flag otherwise, and nonzero suffix renders glyph `suffix + 9`.
- The last constructor bytes depend on a previously exact-backed caller ABI, not on an artificial scheduling barrier. `hide_profile_message @ 0x0043F8D0` ignores ECX internally, but menu callers invoke it as a member view. Writing `hide_profile_message_43f8d0()` keeps the CMenuConnect receiver live before the call and closes the target tail; a global cdecl view is two bytes short. Likewise the primary cursor input pointer must be published with explicit branch-local if/else stores; a ternary common store shortens/reorders the target.
- `CMenuConnect::render @ 0x004418D0` remains a deliberate non-acceptance. Ordinary source already closes menu-list/cursor/design/footer rendering, state-1 number/cursor rendering, state-2 IP/port number loops, state-3/4 list tile rendering and the eight-GuideOverlay tail. The remaining residual is one branchless digit-position register-allocation block (`target EDI=selection, EBP=sprite`; standalone VC8 keeps selection in EAX and sprite in EDI). Several normal arithmetic/if spellings canonicalize to the same fresh block. Do not repair it with fake uses, `register`, `volatile`, assembly or invented barriers.

## CMenuConnect endpoint/history exact patterns (2026-08-29)

- `CMenuConnect::parse_endpoint @ 0x00442E30` is canonical exact **269/269** and `CMenuConnect::finish_transition @ 0x00443D70` is canonical exact **808/808**, adding **1,077 authored bytes** in the same endpoint/history ownership island. The exact constructor already anchors port `+0x428`, endpoint string `+0x42C`, and nested owner pointer `+0xC48`; exact virtual/state callers independently own both private methods.
- The endpoint parser takes a caller-proved **`const std::string&`**. Keep the input as a native VC8 `std::string` so the target SSO `capacity < 16 ? object+4 : heap_ptr` c_str branch is emitted naturally. It copies at most 1023 bytes into a 1024-byte local, uses `char delimiter[4] = ":"`, `strtok_s`, inline `strlen`, and `atol`; the missing/empty-port fallback is 10800. The null-host branch calls the same out-of-line `std::string::assign(const char*, unsigned) @ 0x004021C0` with the canonical empty string at `0x006D69D6` and count zero.
- For the parser destination string, a declaration-only `assign_bytes(const char*, unsigned)` view is the truthful TU boundary for the already-established `std::string::assign` specialization. Writing `endpoint.assign(empty, 0)` against the full VC8 header makes overload/template selection choose a different inline path and grows the body from 269 to 339; that is a source-visibility mismatch, not permission to hand-edit string internals.
- The history finalizer formats `"%s:%d"`, searches the nested checked `deque<string>` at **`*(this+0xC48) + 0x48`**, erases a matching endpoint, pushes the current endpoint to the front, and trims the deque to five entries. Two source-lifetime details are byte-critical: the inserted value is **`push_front(std::string(source))` as an unnamed temporary**, and the nested deque must remain a direct member expression at each use. Naming the second string local moves the first mismatch to the key-string stack slot; factoring the deque through a helper/reference changes receiver allocation and shortens/reorders the target.
- `finish_transition` requires `/GS` and the current target handler `0x006B952D`; its endpoint format is `0x006C1C4C`. No inline/native assembly, register/volatile forcing, dummy liveness, manual deque nodes, copied bytes, or relocation aliases are used.
- Nearby private wide-format substates `0x00442CE0`, `0x004424D0`, and `0x004430D0` remain non-acceptance. Current target routes `swprintf_s`-like formatting through helper `0x004418B0`, which keeps cdecl varargs on the stack but carries buffer size 260 in EDX—a target-private/LTCG calling convention. A pinned VC8 truth probe of ordinary `swprintf_s(buffer,260,...)` passes size on the stack instead. Do not invent a fake fastcall/private signature to claim these authored roots exact.

## SceneRenderCluster timeline exact patterns (2026-08-29)

- The Opening/Ending shared scene-timeline continuation closes **3 authored canonical-exact roots / 1,036 bytes**: `apply_scene_easing @ 0x00450120` (131), `SceneRenderCluster::update @ 0x00451400` (801), and `SceneRenderCluster::cleanup @ 0x00452E60` (104). The already-exact cluster constructor and exact Opening/Ending callers/destructors independently own the 0x94 cluster and both runtime methods; all accepted bodies are ordinary pinned-VC8 C++ with no inline/native assembly, copied target bytes, register/volatile forcing, fake liveness, or padding.
- The constructor-backed physical composition remains `SceneTimelineRegistry +0x00`, `map<int,void*> +0x4C`, `list<0x7C record> +0x58`, `map<int,20-byte timeline value> +0x64`, and `std::string +0x70`. In `update`, the timeline map entry is physically `key + start + end + target + easing + type`; the interpolation helpers receive `&it->first`, so the checked tree pair itself is the 24-byte interpolation entry view. The final list-record dwords are quotient `+0x74` and divisor `+0x78`.
- Checked-iterator source lifetime is byte-significant. Keep repeated direct `it->first` / `it->second` expressions. Caching `it->second` or `&it->first` suppresses target iterator-owner/end checks; explicitly testing `objects.find(key) != end()` introduces another 8-byte iterator temporary. The exact spelling is direct `objects.find(it->first)->second`, allowing VC8's checked dereference to emit the shipped validation path naturally.
- The target jump table establishes source/block order **3, 4, 7, 8, 9, 5, 6, 10, 11, 12, 13**. Preserving this order lets cases 3/4/5/6/11/12/13 share the float-interpolation tail and cases 7/8/9/10 keep their byte-interpolation calls at the target offsets. Equivalent numerically sorted case source changes branch distances and section layout even when semantics are unchanged.
- Timeline smoothing must retain a real two-step lifetime: `delta = GetTickCount() - start_tick`, then `elapsed = (delta + old_elapsed + 16) >> 1`. The one-expression spelling schedules the old elapsed load before the subtraction. Entry dword `+0x8C` is cleared every update and type 16 sets it to one; it is cluster state, not an animated-object byte.
- `SceneRenderCluster::cleanup` is exactly `values.clear(); records.clear(); objects.clear(); timeline.script.clear_commands();`, but the 0x7C list record must have a **nontrivial destructor**. With a trivial record, VC8 inlines the list free loop and grows the authored root to 139 bytes; the truthful nontrivial lifetime keeps the shipped out-of-line list clear and reproduces 104/104.
- Keep `0x004501B0` and `0x00450290` pending. Their byte/float interpolation algorithms, unsigned progress conversion, easing call, and branch topology are recovered, but standalone VC8 remains 216/211 and 162/160 with target EDX/EDI versus fresh ECX/EDX allocation. Writable/const pointers, short-circuit `start` assignment, and unused-this/member views are codegen-neutral; do not force the last register schedule without stronger original-TU evidence.
- Nearby `0x00451200/0x004505E0` are checked tree insert/node machinery, while `0x00451B70/0x00451BD0/0x004505A0` plus polymorphic copy `0x00450640` exhibit the VC8 `deque<0xE8 record>` map/offset/size/block pattern. Treat these as generated-origin candidates to prove separately rather than using them as authored progress filler.

## Replay-slot / synchronized-input / scenario lifetime exact wave (2026-08-29)

- This wave promotes **8 authored canonical-exact functions / 1,424 bytes**: `SynchronizedInputState::pop_control_word @ 0x00428930` (100), `BattleInputGate::publish_synchronized_control_bits @ 0x004289A0` (240), `FixedSlotEnvelope::serialize @ 0x00428B80` (526), `RenderTimelineEntry::RenderTimelineEntry @ 0x00450E30` (111), `ScenarioEffectOwner194::advance_until_ready @ 0x00454F10` (18), `ScenarioTextEntryDC::ScenarioTextEntryDC @ 0x00455360` (95), `BattleInputGate::peek_front_control_word @ 0x004553C0` (92), and `CScenarioData::trim_render_entries_and_begin_fade @ 0x00455CD0` (242). All are ordinary pinned-VC8 C++ with strict target relocation replay; none uses assembly, copied target bytes, fake liveness, register/volatile forcing, padding-for-codegen, or ABI lies.
- `FixedSlotEnvelope` is exactly **0x50 bytes**. The `FixedBattleSetupSlot +0x00` prefix is 0x3C and owns two native checked `deque<short>` payloads at +0x08/+0x1C; the envelope adds a third at +0x3C. Serialization order is side value byte, side byte +2, payload count/elements, side byte +6, side byte +4, token +0x30, repeated for two sides, followed by tail bytes +0x32/+0x33/+0x34, dword +0x38, and the private payload count/elements. Keep a single function-scope `payload_count` and explicit begin/end iterators. Those are real source lifetimes: VC8 reuses the now-dead writer argument slot for both counts and computes each checked end once, reproducing the target 0x14-byte frame and all 526 bytes.
- The synchronized input owner uses `ShortDeque8 controls_3c` with checked block/head/count fields. `pop_control_word` reads the checked front, advances/wraps the head, decrements count and resets head on transition to empty; its empty path writes zero. `BattleInputGate` holds the owner pointer at **+0x104** and `CriticalSectionWrapper` at **+0x124**. The 92-byte method is deliberately a non-consuming peek: exact callers obtain this same gate through `get_player2_input @ 0x0043B060`, peek, and later lock/pop the same queue. Do not rename it as a consumer merely because it returns the front word.
- `publish_synchronized_control_bits` uses flag byte **+0x0F0**: bit 0 locks/pops the first word, publishes low ten bits to +0x68, and publishes control masks 0x8000..0x0400 as menu inputs 59/60/61/63/64/65; bit 1 locks/pops a second word and publishes its low ten bits to +0xD0. Exact `initialize_round_state @ 0x00470BC0` independently calls this method at 0x004289A0; the different `update_local_practice_battle_controls` target remains 0x00428070. The unsigned predicates `(control_word & mask) > 0` naturally select the target `test/setnbe` shape and express the real boolean condition; they are not a synthetic code-generation device.
- `RenderTimelineEntry` is a complete **0x7C-byte** value with native `deque<CSpriteEx>` members at +0x00 and +0x14. Primary transforms occupy +0x28..+0x40; secondary translation/UV fields +0x44..+0x50; alpha/flag/six color bytes +0x54..+0x5B; secondary scale/rotation +0x5C..+0x64; blend/sampler/frame/duration +0x68..+0x78. The constructor's 111-byte exact store order independently closes this layout and complements the already-exact render/list ownership.
- Scenario lifetime evidence closes three reusable contracts. `ScenarioEffectOwner194` is polymorphic and slot **+0x08** is a boolean advance operation that is called until ready. `ScenarioTextEntryDC` is **0xDC bytes**, with `UiSprite94 +0x04`, VC8 string +0x98, numeric array +0xB8 and flags +0xD4; the exact constructor initializes values 5/2 to 30/400 and clears values/flags 3/4 plus its resource handle. `ScenarioRenderEntry9C` is a nontrivial list value: destruction releases title handle +0 and lets `UiSprite94 +0x04` publish the IColor vptr. The exact `/GS` trim loop uses ordinary checked `list::pop_front` until one entry remains, then stores fade delta -20 or -4 in byte +0x99.
- Keep `ScenarioResultResource::update @ 0x00454A50` and constructor `0x00454C70` pending. Truthful update source is 403/403 but retains a pure ESI/EDI allocation swap; the truthful constructor is 297 bytes against target 298 with CTile/EH-state store scheduling and a final-loop register swap. The abandoned probes and their unaccepted literal/EH/string relocation rows were removed. Do not force either root with fake aliases, inert locals, register directives, assembly or padding.

## Shared Fighter command / extended-state exact wave (2026-08-29)

- The current per-character Fighter vtable family closes **3 authored canonical-exact roots / 1,456 bytes**: `build_command_flags @ 0x00476B10` (497), `handle_system_command @ 0x004773A0` (613), and `reset_extended_state @ 0x004776D0` (346). Current data/vtable references and the repeated character-constructor call graph own these shared bodies; all accepted source is ordinary pinned-VC8 C++ with no assembly, copied bytes, volatile/register forcing, fake liveness, padding, or manual vptr writes.
- `build_command_flags` fixes two shared Fighter bitmasks at **+0x724/+0x728**. The matcher is the normal member `0x00463500`; the exact pattern set is `NRNR`, `NLNL`, `N09`, `N07`, `N08`, `DD`, `421X`, `214X`, `412X`, `623X`, `236X`, `4136X`, `6314X`, and `2N2X`. Keeping the tests as direct member calls naturally reproduces the target's first-call scheduling where arguments are pushed before the two mask zero stores.
- `handle_system_command` uses the target's ordinary sparse numeric switch and VC8 naturally emits the shipped two-level byte-index jump tables. Case 200 is byte-significant: spell it as two early returns (`if (state==99 && sequence==2) return 0; return 1;`). A ternary emits a private zero-return block and grows the callable by five bytes; the two-return spelling lets VC8 tail-merge the true path back to the existing case-100 zero epilogue. Cases 180..182 share exact global-pair/stage-music/BGM services and the target-owned `data/bgm/st05.ogg` literal.
- The same system-command root extends the shared Fighter physical tail: action/sequence `+0x13C/+0x13E`, short `+0x730`, byte `+0x759`, dwords `+0x75C/+0x760`, and word `+0x776`. Its shared-event receivers are the already-exact global battle setup state and transition-mode byte; do not invent duplicate globals solely for source naming.
- `reset_extended_state` proves a larger **0x7A4** extended Fighter view. After the shared base reset `0x00460200`, preserve the target-observed sparse field publication order across floats, words, bytes and dwords. VC8 then naturally keeps one x87 zero and `EAX=0` live through the whole reset and reproduces 346/346. Replacing this authored sparse reset with `memset` loses the shipped store widths/order and is not equivalent source evidence.
- Keep nearby `Fighter action-state updater @ 0x0045AF60` review-pending. Ordinary source reproduces the body exactly through the positive-vertical branch, then remains 14 bytes short because one target CFG distinction is not yet explained by truthful source. A redundant nested condition is optimized away; do not introduce a `goto`, volatile/register state, dummy uses, or assembly merely to synthesize the missing block.
- Keep `BattleObjectManager::render_tail @ 0x004666A0` pending at **670/670 with a 491-byte canonical-exact prefix**. Recovering real quad edge locals (`top/right/bottom` and UV edges) closes the x87 geometry lifetime and total size; only one independent `sar eax,2` versus `fst arg1[2]` scheduler permutation remains. `/4` source spelling produces the wrong signed-division correction, so stronger original-TU evidence is required rather than a barrier.

## Fighter input / ACK_INFO packet exact patterns (2026-08-29)

- The current Fighter input continuation closes **3 authored roots / 708 bytes**: bound-input sampler `0x004631C0` (480), scripted-input snapshot reset `0x004633A0` (57), and CPU/scripted selector `0x004633E0` (171). The 0x6B0 binding points at a polymorphic input source whose eight counters live at source `+0x38..+0x54`; the Fighter publishes them at `+0x6B4..+0x6D0`. When binding byte `+0x04` is zero, adjacent counters are min-filtered when their signed difference is under two, then merged with source counters `+0x50/+0x54`; this branch is the target physical fallthrough. Reversing that high-level branch changes the whole second half of the 480-byte body.
- Falling-edge state at Fighter `+0x6F4..+0x708` must remain six explicit `if (current != 0 || old == 0) edge = 0; else edge = old;` branches. Equivalent ternaries let standalone VC8 share a common zero value and shorten/reorder the target. Snapshot reset `0x004633A0` independently proves dwords `+0x6D4..+0x6F0` plus byte timer `+0x70C` are one authored input-state family.
- The CPU selector reads shared input-policy dwords `+0x18/+0x1C`. Vertical modes 0/1/2 publish 0/+1/-1. Horizontal modes 0/2 reuse exact `Fighter::classify_fighter_x_boundary @ 0x00435910`; mode 1 compares x against target-owned **630.0f @ 0x006C2CB4** and **650.0f @ 0x006C2CB0**, while modes 3/4 publish zero. The two float literals are independently attested from the canonical PE.
- The adjacent authored `update_fighter_scripted_input_state @ 0x00463E40` remains a deliberate **751/753** optimizer/TU stop. Its 10-bit packed input, `+0x6D4..+0x6F0` snapshot, timer `+0x70C`, native checked `deque<short> +0x710`, unconditional `push_back`/`pop_front`, state dispatch and probability clear paths are recovered. The shipped body retains a logically redundant second `cmp action,300` that structured, direct-member and common-tail source forms all optimize away; do not invent a dependency or assembly for two bytes.
- The ACK_INFO packet continuation closes **serializer `0x0044DF20` (292) + parser `0x0044E0A0` (183)** on the same 0x40 type already owned by exact Client/Server senders and dispatchers. The object has a four-byte non-polymorphic result base, derived vfptr, side values/status bytes, two native checked `deque<short>` members at `+0x0C/+0x20`, peer bytes `+0x34/+0x35`, tail bytes `+0x36/+0x37`, dword `+0x38`, and sequence byte `+0x3C`.
- Serializer element access is **`deque::at(index)`**, not `operator[]`: the target has a real `_Xran` range failure before the checked deque indexing path. Keeping signed side index plus direct `values_00c[side]` expressions lets VC8 form the target induction pointer directly on each deque's size field. The fresh COMDAT has one trailing `int3` after the reviewed 292-byte function boundary; it is not authored-byte progress. Parser count is an unsigned byte: test `count > 0` before zero-extension, then use a real `int value` local for each 16-bit element before `push_back`. Count above 20 clears the two tail bytes and returns the count; success leaves the low byte of tail dword `+0x38` as the `char` return, matching the existing caller ABI.
- Two nearby source-shape boundaries should be reused rather than restarted: shared Fighter render geometry `0x0045E6E0` reaches **570/570** with every instruction from relative `+0x21` through RET aligned; only `fild frame_x` versus the shrink-wrapped `push edi` is scheduled in the opposite order. `CSprite::set_texture_options @ 0x00406A90` reaches the exact 334-byte boundary and 104-byte exact prefix under the target-backed dead-parameter/texture-size lifetime, but legal UV publication variants settle on different x87 spill topologies. Neither should be forced with volatile/register state, fake dependencies, inline assembly, or copied bytes.

## Background transition exact pattern (2026-08-29)

- `BackgroundBase::step_transition @ 0x004656A0` is canonical exact **825/825** over the current 0x64 class layout. States 0 and 2 add `transition_value_60` to accumulator `value_4c`, clamp at 1.0f/0.0f and publish terminal state 1/3 respectively. Each arm then walks `sprites_b_34` followed by `sprites_a_14` through native checked `vector<CSpriteEx>` indexing and sends `int(value * 255.0) << 24 | 0x00FFFFFF` through the virtual color slot.
- VC8 reaches the shipped register allocation only when the loop index is initialized independently inside each switch arm. This lets the compiler hoist one zero register across the two arm-local loop pairs without any register declaration, fake dependency or manual vtable access. Moving the index initialization outside the switch produces a truthful but different 831/825 shape.
- Keep the adjacent `set_transition @ 0x004654F0` pending. Its ordinary switch and two alpha loops reach the reviewed 407-byte callable, but standalone VC8 swaps only the byte-alpha spill and x87 control-word spill (`[esp+0x1C]`/`[esp+0x20]`). `initialize_common @ 0x004664A0` now has target-style EH after giving the 0xE8 prototype a real named lifetime, yet remains 507/512 with a 0x1FC frame versus target 0x200 because the fresh TU materializes an extra by-value polymorphic copy. Manager update `0x00466F40` remains 1064/1074 on early clamp register scheduling. Preserve these as LTCG/TU stop conditions; do not add inert locals, padding, manual vptr stores or assembly.

## Network packet leaf/runtime exact pattern (2026-08-29)

- The packet continuation closes **10 authored roots / 682 bytes** in two truthful compiler partitions. The `/GS` unit owns `0x0044D700/0x0044D770/0x0044D7D0/0x0044D800/0x0044D840/0x0044D860/0x0044D920/0x0044DAB0/0x0044E160`; the non-`/GS` unit owns request pump `0x0044D880`. The split is target-observed: Replay decompression needs a 256-byte protected local, while the 16-byte type-11 request sender has no cookie frame.
- Client primary-owner offsets remain send service **+0x3B4**, shared wire buffer **+0x5B4**, input/replay sink **+0x6B4**, tag **+0x6B8**, sequence **+0x6BC**, metadata **+0x6C0**, and checked `deque<short>` **+0x6C8**. `CNetworkClient_send_simple_packet @ 0x0044D770` must keep `packet->serialize(buffer)` nested directly inside the outer send call; extracting the size changes VC8 argument pre-push order. `CNetworkClient_flush_replay_values @ 0x0044D800` uses the +0xDC critical section and naturally tail-leaves after forwarding +0x6C8 to the sink.
- `NetworkReplayState_reset_locked @ 0x0044D700` is a real scoped critical-section lifetime, not hand-written enter/call/leave. Its unwind handler is current **0x006B7C88** and releases the +0x124 lock if buffer reset `0x00428B70` throws. Ordinary RAII reproduces all 106 bytes and gives reusable proof that this network path was built with C++ EH visibility.
- Type-11 request packet is 0x10 bytes: a four-byte non-polymorphic result base, derived vfptr, sequence +0x08 and tag +0x0C. Its wire form is six bytes `{11, sequence32, tag8}`. Pump `0x0044D880` increments current dword **0x006FD384**, emits every fourth call, and requires a `PacketSerializeView *` over the concrete local so VC8 retains the target indirect serializer call. Assigning tag before sequence is source-significant: VC8 then performs the target sequence load before tag load and publishes sequence before base/vfptr/tag stores.
- INPUT packet is 0x24 bytes with sequence +0x08, tag +0x0C and checked `deque<short> +0x10`. Serializer `0x0044D920` writes type 3, sequence, tag, byte count, then elements only when size is below 100. Preserve the nested `if (size < 100) { index=0; if (size > 0) ... }`: it gives the target late EDI lifetime and out-of-line checked `at @ 0x0042D140`; combining the predicates changes both register ownership and branch lowering.
- Replay packet is 0x28 bytes with sequence/metadata/tag +0x08/+0x0C/+0x10 and deque +0x14. Parser `0x0044E160` decompresses `input+2` using byte length `input[1]` into a real 0x100 record `{u32 sequence,u32 metadata,u8 tag,u8 count,short[123]}`. Copy through a short pointer plus promoted remaining counter; `count > 100` clears sequence/metadata. EH xrefs **0x006BA56B/0x006BA5B3** own the exact 8-byte deque cleanup `0x0044DAB0`.
- Preserve four bounded non-acceptances. ACK_INFO ctor `0x0044DAC0` is target 41 versus fresh 43 because target keeps a zero in EAX across base publication while standalone source uses immediate stores. INPUT parse `0x0044E050` is 77/78 with the correct wire/deque semantics but a different initial cursor/prologue schedule. Replay consumer `0x0044E210` is 170/170 and relocation-equivalent but allocates `{this,lock,value}` to `{EBX,EBP,EDI}` instead of target `{EDI,EBX,EBP}`. CInputManagerEx ctor `0x0044D8F0` is 48/48 with exact vtable/deque calls but schedules `injected_mask_060=0` before `use_injected_input_064=true`, opposite the target. Do not use register declarations, volatile aliases, inert locals, manual vptr stores, copied bytes, or assembly to close these scheduler/TU differences.

## Network replay/base/adaptive continuation (2026-08-29)

- This continuation promotes **5 authored canonical-exact roots / 1,313 bytes**: Replay serializer `0x0044D990` (285), CNetworkBase constructor `0x0044DB10` (411), replay-save preparation `0x0044E2C0` (407), Client result-bearing packet send `0x0044E6A0` (48), and Server adaptive-delay update `0x0044F550` (162). They move the ledger to **1,118 exact / 186,186 authored bytes / 372 units; 1,049 excluded; 1,843 review-pending**; the complete aggregate cold replay passes **372/372** units with rc=0.
- Replay serialization mirrors the parser's real 0x100 decoded record. It writes sequence/metadata/tag/count and native deque values into the record then compresses only the used prefix into `output+2` with capacity 254. Bind `values_014` through a **const `std::deque<short>&`** before indexed access: this selects the checked const subscript COMDAT and gives the shipped load order. Non-const indexing is length-correct but permutes one checked load.
- `CNetworkBase::CNetworkBase @ 0x0044DB10` proves the complete 0x6DC construction topology independently of the exact destructor: critical sections +0x48/+0xDC/+0x25C; `ScenePacket0C[2] +0x64`; INPUT packet +0x90; `deque<short>[2] +0xB4`; two composed `CInputManagerEx + deque<short>` endpoints +0xF8; two queue slots +0x214; `UiSprite94[2] +0x27C`; 0x314 winsock state +0x3B4; replay deque +0x6C8. `sprite_handle_278(0)` belongs in the member-initializer phase while state +0x3A4/+0x3AC belongs in the constructor body. Keeping C++ linkage on `get_score_data()` preserves unwind state 9 and all ten reverse-cleanup states.
- Replay-save preparation reads target literals `configex.ini`, `network`, and `savereplay`, then uses a real scoped critical lifetime on owner +0xDC. It resets sink +0x6B4, walks checked `deque<short> +0x6C8` with a native iterator, forwards every unsigned-short value, builds the path from name buffers +0x04/+0x24, saves, and resets again. Ordinary RAII naturally produces handler **0x006BA52B** and the target dual-cookie `/GS` frame; a manual enter/leave body does not carry equivalent exception behavior.
- `CNetworkClient_send_packet_with_result @ 0x0044E6A0` differs from the earlier simple sender by passing packet +0x04 as the result pointer. Keep virtual serialization nested in the outer four-argument send call so VC8 pre-pushes result/option before serializing to +0x5B4. `CNetworkServer_update_adaptive_delay @ 0x0044F550` samples `sequence_098-high_water_6e8` only when it is below ten and sequence is positive. Direct repeated member reads of count +0x6E0 are source-significant: they preserve separate signed `count>0` and `count<20000` gates; a const local lets VC8 fold them into a different unsigned range test. The signed-byte result is lower-clamped to base delay +0x81 and upper-clamped to ten at +0x80; counts above 20000 reset +0x6E0/+0x6E4.
- Preserve two new non-acceptances from the same truthful source. ACK tail import `0x0044E6D0` is 78/78 with correct +0x25C locking and +0x21A/+0x23A/+0x3A6 publications but schedules the side1 store after the leave-receiver load instead of before it. Server input publication `0x0044F9A0` recovers the real `push_front(previous/current)` plus paired `pop_back` history policy and reaches 404/409; its remaining masked-input/sequence schedule changes callee-save allocation. Do not close either with volatile state, fake dependencies, register forcing, assembly, or copied bytes.

## Battle-pair setup and compact state helpers (2026-08-29)

- Exact `BattleController::initialize_battle_fighter_pair @ 0x0046AE70` fixes both ownership and argument order for two previously unnamed helpers. `GlobalBattlePairState::initialize_pair_metadata @ 0x00427320` receives `&first.x_ec, &first.y_f0, &second.x_ec, &second.y_f0`; `copy_roster_metadata @ 0x00426B90` receives the same four pointer roles from exact roster preparation. The physical stores are deliberately non-monotonic: first X +0x44, second X +0x48, first Y +0x4C, second Y +0x50. Use named pointer roles and let VC8 schedule the stores; do not reorder source parameters to mimic offsets.
- The 106-byte initializer proves the state prefix around those pointers. Enable bytes are +0x2C/+0x2D/+0x2E; zeroed float targets are +0x30/+0x34/+0x38/+0x3C/+0x40; +0x14 starts at 1.0. It calls the current first synchronizer `0x00426BB0`, copies source floats +0x00/+0x04/+0x08 into +0x18/+0x1C/+0x14, calls the first synchronizer again, then calls the second updater `0x00426DF0`. This ordinary member ordering gives 106/106 without stack shaping.
- `PairInputState::reset @ 0x00428030` closes a real 0x30 prefix, not an opaque byte array: bytes +0x00/+0x2C/+0x2E and dwords +0x04..+0x28 are independently used. The exact body depends on natural constant sharing (`EAX=0`, `EDX=1`, then `EAX=3`), which follows from typed assignments in target order. `FighterPhaseContext::finish_fighter_slot_setup @ 0x00465100` independently confirms three Fighter pointers at +0x28 and three active bytes at +0x34; first activation calls exact reset/publication body `0x00465040`.
- BattlePhaseBlock has a polymorphic scenario runtime embedded at +0x68. `run_scenario_step @ 0x0045A290` and `run_458cd0 @ 0x0045A2A0` are ordinary virtual tail forwards to slots 2 and 3. Model the embedded object and call virtual methods normally: an explicit function-pointer facade is unnecessary and would discard useful type evidence.
- `Fighter::reset_for_roster @ 0x0045F640` reads the previous peer from +0x170. When non-null, owner +0x658 vslot 8 receives `(previous,new)` before +0x170 is replaced. `Fighter_copy_action_word_176_to_174 @ 0x0045B250` is a void publication helper: all inspected current callers discard AX. Declaring a guessed `short` return makes standalone VC8 insert `movzx eax,word ptr [...]`; the truthful void assignment naturally emits target `mov ax` followed by the 16-bit store. Residual register contents alone are not return-type evidence.
- Two raw-identical retained leads were deliberately not promoted. `Fighter_face_opponent_and_flip_horizontal_velocity @ 0x00478E60` reaches exact comparisons/CFG but standalone VC8 schedules the direction store before the target's velocity load; `Udonge_reset_action_visual_state @ 0x005D06D0` differs only because standalone VC8 encodes the final -1 byte as an immediate while target reuses AL. These are TU/scheduler source-shape evidence, not permission for volatile/register forcing, expression side effects invented solely for codegen, inline assembly, or copied bytes.

## Input/connect/collision exact continuation (2026-08-29)

- This continuation promotes **4 authored canonical-exact roots / 596 bytes**: connection-key value return `0x0043ABA0` (40), network-session teardown `0x0043B0A0` (70), selected-input rebinding `0x0043B640` (153), and float collision-extent accumulation `0x0046B440` (333). They move the ledger to **1,122 exact / 186,782 authored bytes / 376 units; 1,049 excluded; 1,839 review-pending**. Each root has a target-pinned typed-re packet and a fresh canonical comparator result; the complete aggregate cold replay passes **376/376** units with rc=0.
- `get_connect_key @ 0x0043ABA0` proves a hidden-sret ABI. Source `ConnectKey16 get_connect_key()` naturally receives the caller-provided return buffer in `[esp+4]`, copies the four target dwords from immutable `0x006C161C`, and returns that buffer in EAX. The earlier `ConnectKey16 *get_connect_key(ConnectKey16 *)` declaration happened to be call-compatible but generated the wrong load/store schedule in the callee; repair callers to direct value initialization and replay them. VC8 keeps those callers byte-identical. For relocation validation, split the four nonzero aggregate addends into four independently attested dword keys; one 16-byte literal cannot truthfully validate at base, base+4, base+8 and base+12 simultaneously.
- `clear_network_session @ 0x0043B0A0` closes the current control-plane teardown: call the +0x3B4 shutdown member, release the +0x278 title handle through manager `0x00702798`, invoke the session's virtual scalar deleting destructor, then clear pointer `0x006FBD5C`. Two source identities are genuine: a `NetworkShutdownStateView *` for the member receiver and a `CNetworkBase *` for owner lifetime. Keeping both lets VC8 load the global into EAX for the null guard, preserve the base in ESI only inside the live branch, and use ECX for the derived member. Collapsing to one base local hoists `push esi`; repeating the global access reloads it after shutdown. Neither alternative represents the target owner dataflow.
- `rebind_selected_input @ 0x0043B640` queries active source from combined input `0x006FCF80` only when the selected pointer is null. If both source bytes agree, `require_distinct` writes sentinel -2 and returns; otherwise a real `switch(slot)` clears the opposite pointer/source for cases 0 and 1. The switch is semantic, not a codegen ornament: it produces the target `mov/sub/sub` decision tree and reuses zero EAX in case 1. Nonnegative sources index the exact checked 0x68-stride table at `0x006FCA2C`; negative sources select fallback input `0x006FBD60`.
- `CollisionContext::accumulate_float_collision_extents @ 0x0046B440` is the float analogue of the accepted integer accumulator. For each AABB, compare left/right orientation, raise integer `extent_1c` from the greater horizontal edge and lower `extent_24` from the lesser edge; then raise `extent_20` from both tops and lower `extent_28` from both bottoms. Ordinary float comparisons followed by `static_cast<int>` reproduce all target x87 `fcompp/fnstsw` tests and `__ftol2_sse` calls. No volatile loads, manual x87, copied bytes, register directives or padding are required.

## Input merge / profile UI pop exact continuation (2026-08-29)

- This continuation promotes **2 authored canonical-exact roots / 1,104 bytes**: `CInputManagerCluster::merge_menu_input_sources @ 0x0040A0F0` (1003) and `pop_profile_ui_menu @ 0x0043FF40` (101). They move the ledger to **1,124 exact / 187,886 authored bytes / 378 units; 1,049 excluded; 1,837 review-pending**. Both complete boundaries were reconciled from target-pinned typed-re packets and fresh-comparison reports before promotion.
- Input merge has a real local `CInputManager hold`. `std::memset(&hold, 0, sizeof(hold))` naturally lowers to the target's ten zero stores; member-wise initialization changes the schedule. The outer source/index loops and ten button-counter loop use signed `int` latches, matching the target signed `jl` exits. Preserve each checked source access as **`sources.begin()[i]`**. In this VC8 checked-deque implementation, that spelling creates the target iterator owner validation followed by indexed access; `.at(i)` selects another helper path, while direct unchecked storage arithmetic loses the target checks. The button merge keeps the greatest-magnitude signed counter for each direction/button and retains the previous merged value on equal magnitude.
- `pop_profile_ui_menu @ 0x0043FF40` proves the global menu owner is a native checked `std::list<ProfileUiMenuBase *>`. The exact body is the ordinary sequence `item = menus.back(); delete item; menus.pop_back();`: checked `back()` validates the sentinel, virtual scalar deletion owns the pointed object, and `pop_back()` emits the target erase-one node lifetime. Do not replace the list with a hand-shaped sentinel facade merely because the final machine code exposes its links.
- Keep input frame candidate `0x0043B4A0` pending as one linked codegen problem with isolated/aligned tail `0x004400D0..0x0044016F`. All code xrefs to `0x004400D0` are internal jumps from `0x0043B547/0x0043B563/0x0043B56C`; the 160-byte tail ends before the next function at `0x00440170`. Truthful recovered source reproduces its state/snapshot semantics and instruction structure, and the 209-byte primary region becomes exact when connected to that tail, but standalone VC8 assigns the two live values to EAX/ECX opposite the target and produces 159/160 bytes. Do not promote only the hot chunk, create a fake tail helper, or force the register allocation.
- Input/match initializer `0x0043B8F0` now has a complete **286-byte** boundary and exact switch/fallthrough topology, including the duplicated call/pop/return tail. Its remaining first-instruction residual is original-TU scheduling: target preserves the second argument before evaluating the first, while standalone VC8 evaluates the first argument first. Scenario-runtime relocation addends +72/+73/+74 are target-backed facts, not exactness evidence. Leave the root review-pending until compatible TU/LTCG provenance explains that scheduler decision.
- Input-cluster candidates `0x0040A550` and `0x0040A6C0` remain dynamic-stack-alignment/original-TU stops. Natural source establishes their behavior but the standalone object cannot reproduce the target aligned-frame topology without inventing an aligned local. Preserve the evidence and do not introduce dummy alignment storage, register/volatile coercion, assembly, or padding.

## Outcome / MenuEnd / sound / NetworkWinsock exact continuation (2026-08-30)

- This continuation promotes **6 authored canonical-exact roots / 1,395 bytes**, moving the working checkpoint to **1,182 exact / 196,619 authored bytes / 412 exact units; 1,049 excluded; 1,779 review-pending**. The roots are `SoundVolumeService::dispatch_wave_handle @ 0x00401D50` (106), `CNetworkWinsock::shutdown @ 0x00412FD0` (175), `NetworkSendState::publish_callback/check @ 0x00413360/0x00413390` (39/143), `CMenuEnd::initialize_runtime @ 0x004404F0` (486), and `CollisionContext::dispatch_outcome_path @ 0x0046D830` (446). Every promoted body is ordinary pinned-VC8 C++ with canonical target relocation replay; no inline/native assembly, copied bytes, volatile/register forcing, fake liveness, padding-for-codegen, or fabricated private ABI is used.
- The sound dispatch closes a narrow application-owned handle path without claiming the unresolved handle manager internals. `SoundVolumeService +0x64` is the handle-manager receiver; lookup returns a record whose DirectSoundBuffer8 pointer is +0x04. The exact body queries status, conditionally stops when bit 0 is set, rewinds to position zero, then plays. The neighboring master/per-handle volume setters `0x00401C20/0x00401CE0` remain pending: their effective-gain/threshold/DirectSound semantics are closed, but the shipped TU uses an 8-byte-aligned EBP/x87 frame that ordinary float/double source variants do not reproduce. Do not invent an aligned dummy or force a compiler frame profile.
- `NetworkSendState` now has a target-backed physical core: callback pointer **+0x80**, raw Win32 critical section **+0x88**, checked `std::vector<0x20 record> +0x108`, and a second raw critical section **+0x124**. Record state is +0x10 and age is +0x14. `publish_callback` is a normal lock/store/unlock sequence. `check(index)` deliberately performs two checked vector accesses: state -1 returns true immediately after unlock; otherwise the result is `age > 10`. Collapsing the accesses into unchecked pointer arithmetic loses target validation.
- `CNetworkWinsock::shutdown` closes important ownership inside the independently exact-backed 0x314 member at `CNetworkBase +0x3B4`: socket **+0x28**, three 8-byte thread handles **+0x5C/+0x64/+0x6C**, running byte **+0x74**, heap buffer **+0x78**, a fourth thread handle **+0x13C**, and event **+0x1E0**. The exact teardown calls the existing 73-byte `BattleThreadHandle::wait_for_exit`, performs `shutdown(socket,1)`, `shutdown(socket,0)`, `closesocket`, decrements process-wide Winsock refcount `0x00702280` under critical section `0x006FA264`, calls `WSACleanup` on transition to zero, then frees +0x78. WS2_32 is ordinal-imported in this PE: FirstThunk 0x006C0264 fixes `closesocket` ordinal 3 at 0x006C0270, `shutdown` ordinal 22 at 0x006C0280, and `WSACleanup` ordinal 116 at 0x006C0294. Use the actual PE ordinal/IAT evidence rather than guessing import names from call arity.
- `CMenuEnd::initialize_runtime` completes the result/end bootstrap around the already exact CMenuEnd ctor/update/render/lifetime. The game-mode switch selects a two-element character-key pair from `MatchSetup`; up to 32 round-history display names are selected through ScoreData and appended with `<br>`; the result becomes a 512x512 text texture and a 512x16 shared tile before story progress is persisted. A real two-key local plus native `std::string` lifetimes and the target hidden-result texture API produce the complete 486-byte `/GS` body.
- `CollisionContext::dispatch_outcome_path` is a 446-byte authored dispatcher over the already exact `try_outcome_path_a` and pending `try_outcome_path_b`. Keep the target physical switch ownership: case 1's nonterminal action falls through to the common scratch reset; cases 3/5/2/4 choose their action then reset; case 6 returns directly on a nonterminal A-path result and otherwise chooses 0x8F/0x91 from the stage-surface predicate. Ordinary virtual action dispatch plus the exact FighterActionScratch reset reproduce all bytes. `try_outcome_path_b @ 0x0046D610` remains a separate 530/542 register-allocation stop and is not implied exact by the dispatcher.
- Preserve the bounded negative evidence from this pass. `CMenuConnect::update_state_middle @ 0x00444490` now has target-shaped case-2 and case-5 `switch(update_profile_message())` lowering plus the real case-5/case-10 common session-cleanup tail, but standalone VC8 remains **708/699** because the shipped TU keeps a semantic zero in EBX and duplicates status/result tails differently. Function-scope and branch-local zero values are optimized away; do not use `register`, volatile, fake aliasing or gotos to retain EBX. `CProfileKeyConfig::capture_key_input @ 0x0044B540` remains **785/790 with a 389-byte exact prefix** after fully recovering keyboard/gamepad scanning and `g_direct_input_backend.gamepad_count == base+0x120`; remaining count/tail allocation is TU-only. `LoadingWatch input configure @ 0x0042BC40` stays **137/140** on AL/CL ownership even with a truthful shared active-byte value. Replay zlib wrappers `0x004067F0/0x00406890` have target-equivalent stream logic, but only the shipped image has the 8-byte aligned EBP frame; `z_stream` itself provides no 8-byte member evidence, so do not add synthetic alignment.
- Spell-sequence preparation `0x0045DC90` also remains pending but its local ownership is stronger: the stack value is a real 0x98 `{short, ushort, CSprite}` record; target construction is the exact CSprite-style `vptr 0x006C0668` plus zero color-base dword, with real EH cleanup. Correct dataflow writes record id directly into the local, writes value_1e before overriding field_02 with 1 for selector 1, uses width/height globals 0x006FA6C4/0x006FA6C0 and exact sprite texture setup, then rereads live_count when publishing state_55a. The current opaque `CSprite` tail makes standalone `/GS` over-protect the local; do not fix that with padding or a fake non-buffer type without target-backed field evidence.

## CTile / sprite / audio / profile-deck lifetime exact continuation (2026-08-30)

- This continuation promotes **13 authored canonical-exact roots / 1,052 bytes**, moving the working ledger to **1,195 exact / 197,671 authored bytes / 417 units; 1,049 excluded; 1,766 review-pending**. The roots are `CTile` copy construction `0x004396A0` (164), `CSprite` construction `0x0040ADA0` (16), BattleBaseEffect ctor/dtor/shutdown `0x00417FB0/0x00417FC0/0x00418040` (16/33/33), DirectSound base/derived lifetime and initialize roots `0x00418280/0x004182E0/0x004182A0/0x004180D0/0x00418310/0x00418180` (17/33/53/30/160/253), and ProfileDeck resource-group ctor/dtor `0x0042E880/0x0042E900` (118/126). Fresh canonical replay of all five new units is zero-difference.
- `CTile` copy construction is ordinary implicit C++ copy semantics over a polymorphic sprite base plus nine float extension fields. The target naturally uses a base `rep movsd` followed by nine x87 scalar copies; do not replace it with copied machine bytes or a hand-built bulk copy. The 16-byte `CSprite` constructor is independently owned by existing array-construction callers and clears its color value before publishing the current sprite vtable.
- BattleBaseEffect has an 8-byte owner with one COM-style resource pointer at +0x04. Constructor/destructor/shutdown are normal resource lifetime; `initialize` remains deliberately outside the exact unit because the target helper consumes a private EAX live-in. DirectSound base is 0x0C (`resource +0x04`, state +0x08), derived is 0x10 with a second resource at +0x0C. Natural virtual construction/destruction and COM release reproduce the accepted lifetime wrappers and both initialize methods. The adjacent derived constructor `0x004180B0` is **20/20 length-compatible but TU-scheduled**: target clears +0x08 before publishing the derived vptr, while fresh VC8 orders the inherited +0x04/+0x08 stores differently. Reordering the base constructor breaks the already-exact base root, so leave it pending rather than forcing stores or vptr writes.
- `ProfileDeckResourceGroupNative20` is a real native checked `std::deque<unsigned>` at +0x00 and `std::map<int,SpellRecordView>` at +0x14. Letting VC8 own ordinary constructor/destructor lifetime naturally selects the current `_Buynode`, tree erase, deque tidy and EH topology; do not hand-code tree/list teardown.
- Current `FileReaderOwner::open @ 0x0040CF40` establishes one important 1.06a semantic correction even though the root is not yet exact: the plain-reader `CreateFileA` desired-access argument is **1 (FILE_READ_DATA)**, not retained-source `0x80000000` (GENERIC_READ). Correcting the source changes fresh size 215 -> 212 against target 213 and leaves all ten already-exact FileReader/package-reader roots unchanged. The remaining mismatch is constructor-expression/TU scheduling: target routes failed `new` directly to the shared failure join and delays package-reader vptr publication, while standalone VC8 creates a local null-construction join. Do not synthesize this with fake null behavior, manual vptr stores, register coercion, or assembly.
- Raw byte identity is not authored-origin proof. Two cached zero-diff leads, `0x0046EA60` and `0x00421F30`, were rejected from the authored numerator because current origin evidence identifies them as VC8-generated `std::_Tree::find` and `std::list::_Buynode` COMDATs respectively. Always close function ownership/origin before promoting a byte-identical retained candidate.

## Full-inventory generated-orphan census continuation (2026-08-30)

- A repository-wide scan of existing `build/**/*.obj` files is a useful candidate generator, not durable origin evidence. Cached objects may be stale, may have been built with unknown flags, and a review-only scan can hide an already-classified clone. The durable path is: map the lead back to a tracked source, put its exact symbol/address/size in a SHA-pinned origin manifest, let `function-origins.py` rebuild it with the pinned VC8 SP1 compiler, and require its relocation-masked hit set across all **4,010** current `functions.csv` spans to equal exactly the declared singleton or equivalence group.
- That replay closes **20 compiler-generated target functions / 2,624 bytes**. `scripts/probes/pat_vector_generated.cpp` contributes eight new PAT-family vector helpers at `0x004607F0/0x004608E0/0x00460F50/0x00461120/0x00461180/0x00461240/0x00461270/0x004613E0` (969 bytes). `scripts/probes/short_set_generated.cpp` contributes checked tree `_Max/_Min`, iterator increment, rotations, decrement, `_Buynode`, and recursive `_Erase` at `0x00432710/0x00432730/0x00432750/0x00432900/0x00432950/0x00432A40/0x00432AD0/0x00432B50` (563). Canonical-exact `src/ui/SceneRenderClusterConstructor.cpp` contributes `map<int,SceneMapValue20>::erase(iterator) @ 0x0041ECF0` (695). Tracked forced-instantiation probes add `list<unsigned>::splice @ 0x006B0420` (184) and four-byte `deque::back/push_front @ 0x00432BE0/0x00451AF0` (213).
- Prefer extending the manifest that owns the emitting source instead of creating a second rule for the same object family. The PAT and short-set leaves above therefore extend their existing manifests, while the two previously unmanifested forced-instantiation sources receive narrow manifests of their own. `function-origins.py` rejects overlapping rules, incomplete clone groups, target-size drift, unsupported relocations, weak non-relocation coverage, compiler/source drift, and any new same-fingerprint candidate.
- Template code shape and semantic template identity are different evidence classes. A four-byte-value `std::deque<unsigned long>` witness proves that a target body is a VC8-generated checked deque COMDAT when the complete fingerprint is unique; it does not prove that the shipped source literally named `unsigned long`. Record the origin exclusion while keeping the target argument's higher-level identity unknown. Likewise, identical 36-byte scalar-deleting-destructor shapes cannot distinguish `std::bad_alloc` from an authored polymorphic class once their relocation fields are masked; do not classify such clone-shaped bodies without independent vtable/callee evidence.
